# utility-page Gap Analysis

> 분석일: 2026-08-02
> 대상: `docs/02-design/features/utility-page.design.md` vs 구현 코드
> 분석: gap-detector
> **Match Rate: 94.4%** (42.5 / 45, 부분일치 0.5점) — 미구현 0건

---

## 1. 판정 요약

| 구분 | 항목 수 | 획득 | 비고 |
|------|:---:|:---:|------|
| §2 컴포넌트 구조 | 7 | 6.5 | 그라디언트 헤더 아이콘 미적용 (-0.5) |
| §3 데이터 계약 (훅 3종·카드 매핑) | 9 | 9.0 | 완전 일치 — `useToolCatalog()` / `useLlmModels(true)` / `useSkills({scope:'all', size:100})` |
| §4 유형 매핑·필터 | 3 | 3.0 | `toolKind` 설계 코드와 문자 단위 동일 |
| §5 검색 | 2 | 2.0 | |
| §6 새로고침 invalidate 3종 | 1 | 1.0 | 키 3종 완전 일치 |
| §7 로딩/에러/빈 상태 | 5 | 4.5 | 도구 탭 필터 0건도 "검색 결과가 없습니다" (-0.5) |
| §8 라우팅/네비 | 4 | 3.5 | TopNav description 문구 상이 (-0.5) |
| §9 삭제 검증 | 4 | 3.5 | 코드 잔여 참조 0, CLAUDE.md 4행 표류 (-0.5) |
| §10 테스트 T1~T9 + 네비 단언 | 10 | 9.5 | T6 "네트워크 요청 없음" 미단언 (-0.5) |
| **합계** | **45** | **42.5 → 94.4%** | |

## 2. Gap 목록

### 🔵 변경 (설계 ≠ 구현) — 모두 Low

| 항목 | 설계 | 구현 | 처리 |
|------|------|------|------|
| TopNav description | '…찾아보고 추가합니다' | '…미들웨어, 스킬을 찾아봅니다' (`TopNav.tsx:53`) | 구현이 더 정확(읽기 전용 페이지) → **설계 문서를 구현에 동기화** ✅ |
| 도구 탭 빈 상태 | 검색 0건만 안내 문구 | 유형 필터 0건도 동일 문구 (`index.tsx`) | 의도적 UX 개선 → **설계에 명시** ✅ |
| 헤더 아이콘 | 그라디언트 아이콘 계승 | 텍스트 헤더만 | 참고 이미지(utility.png)도 텍스트 헤더 → **설계에서 삭제** ✅ |

### 🟡 추가 구현 (설계 X, 구현 O) — 모두 무해

- `UtilityCard` `role="article"` + `aria-label` (접근성·테스트 쿼리)
- 모델 배지 톤 emerald(기본)·zinc(비활성)
- TopNav 테스트 '도구 연결' 부재 네거티브 단언

### 🔴 미구현 — 없음

### ⚠️ 설계 문서 자체 오류 (구현이 진실, 감점 제외)

1. §10 픽스처 `GET /api/v1/skills/list` → 실제 계약은 **POST** (`skillService.ts:15`). 테스트는 `http.post`로 올바름. → **설계 정정** ✅
2. T6 "네트워크 요청 없음" 단언은 §3 "훅 3개 항상 마운트"와 자기모순 → **"미들웨어 전용 API 없음(호출 자체가 존재하지 않음)"으로 정정** ✅

### 📄 문서 표류 (설계 범위 밖, 후속 처리)

`idt_front/CLAUDE.md` L491·541·542·545가 삭제된 파일(task-tool-connection.md, types/tool.ts, toolService.ts, ToolConnectionPage)을 계속 참조 → **UtilityPage 항목으로 교체** ✅

## 3. 분석 후 조치 (2026-08-02 즉시 반영)

- [x] 설계 §8 TopNav description 구현 문구로 동기화
- [x] 설계 §7 빈 상태 분기에 "유형 필터 0건 포함" 명시
- [x] 설계 §2 그라디언트 헤더 아이콘 문구 삭제
- [x] 설계 §10 skills/list 메서드 POST 정정 + T6 단언 문구 정정
- [x] `idt_front/CLAUDE.md` 삭제 파일 참조 4행 → UtilityPage 참조로 교체

> 조치 반영 후 설계-구현 잔여 갭은 "추가 구현(무해)" 항목만 남음.

## 4. 결론

- Match Rate 94.4% ≥ 90% → **Act(iterate) 불필요, Report 진행 가능**
- 코드 측 수정 0건으로 갭 해소(전부 문서 정정으로 처리)
- 검증: UtilityPage 9/9 · TopNav 5/5 · `tsc` 통과 · 전체 759/767 (실패 8건은 기존 사전 실패와 동일, 신규 회귀 0)
- 이월: 브라우저 수동 E2E(백엔드 기동 상태 실데이터 확인), 커밋(admin-nav-restructure와 TopNav 공유 주의)
