# Task Files Registry

> 원본: CLAUDE.md §13  
> **모든 새 task.md는 LOG-001을 의존성으로 참조해야 한다.**

---

| Task ID | 파일명 | 설명 |
|---------|--------|------|
| VEC-001 | src/claude/task/task-qdrant.md | Qdrant 벡터 저장소 |
| DOC-001 | src/claude/task/task-pdfparser.md | PDF 파서 모듈 |
| CHUNK-001 | src/claude/task/task-chunk.md | 청킹 모듈 |
| RET-001 | src/claude/task/task-retriever.md | 리트리버 모듈 |
| COMP-001 | src/claude/task/task-compressor.md | 문서 압축기 모듈 |
| EXCEL-001 | src/claude/task/task-excel.md | 엑셀 처리 모듈 |
| PIPELINE-001 | src/claude/task/task-pipeline.md | 문서 처리 파이프라인 |
| LOG-001 | src/claude/task/task-logging.md | 로깅 & 에러 추적 (필수 참조) |
| EVAL-001 | src/claude/task/task-hallucination-evaluator.md | 할루시네이션 평가기 모듈 |
| QUERY-001 | src/claude/task/task-query-rewriter.md | 쿼리 재작성기 모듈 |
| SEARCH-001 | src/claude/task/task-tavily-search.md | Tavily 웹 검색 도구 |
| AGENT-001 | src/claude/task/task-research-agent.md | Self-Corrective RAG 에이전트 |
| CODE-001 | src/claude/task/task-code-executor.md | Python 코드 실행 도구 |
| LLM-001 | src/claude/task/task-claude-client.md | Claude LLM 클라이언트 모듈 |
| AGENT-002 | src/claude/task/task-excel-analysis-agent.md | 엑셀 분석 에이전트 (Self-Corrective) |
| AGENT-003 | src/claude/task/task-research-team.md | Research Team 에이전트 (Supervisor + Web/Document Search 팀) |
| LANGSMITH-001 | src/claude/task/task-langsmith.md | LangSmith 모듈 |
| RETRIEVAL-001 | src/claude/task/task-retrieval-api.md | 문서 검색 API (RAG Retrieval) |
| REDIS-001 | src/claude/task/task-redis.md | Redis DB 모듈 (키-값 CRUD, Hash, List, 분산 잠금) |
| MCP-001 | src/claude/task/task-mcp-client.md | LangChain MCP 공통 클라이언트 모듈 (stdio/SSE/WebSocket) |
| ES-001 | src/claude/task/task-elasticsearch.md | Elasticsearch 공통 Repository 모듈 (index/search/CRUD) |
| HYBRID-001 | src/claude/task/task-hybrid-search.md | BM25 + 벡터 하이브리드 검색 (RRF 병합, API 포함) |
| CHUNK-IDX-001 | src/claude/task/task-chunk-index.md | 청킹 + BM25 키워드 추출 색인 API (ES 저장) |
| KIWI-001 | src/claude/task/task-kiwi-analyzer.md | Kiwi 한국어 형태소 분석기 모듈 |
| MORPH-IDX-001 | src/claude/task/task-morph-index.md | Kiwi 형태소 분석 + Qdrant + ES 이중 색인 API |
| RAG-001 | src/claude/task/task-rag-agent-api.md | ReAct RAG Agent API (하이브리드 검색 + LangGraph 에이전트) |
| MYSQL-001 | src/claude/task/task-mysql.md | MySQL 공통 Repository (Generic Base CRUD) |
| CONV-001 | src/claude/task/task-conversation.md | Multi-Turn 대화 메모리 관리 UseCase |
| PARSE-001 | src/claude/task/task-pdf-parse-service.md | PDF 파싱 공통 서비스 |
| INGEST-001 | src/claude/task/task-ingest-api.md | PDF 파싱+청킹+Vector 저장 통합 API |
| HTML-TO-PDF-001 | src/claude/task/task-html-to-pdf.md | HTML → PDF 변환 공통 모듈 |
| EXCEL-EXPORT-001 | src/claude/task/task-excel-export.md | pandas Excel 파일 생성 공통 모듈 + LangChain Tool |
| AGENT-004 | src/claude/task/task-custom-agent-builder.md | Custom Agent Builder (LLM 도구 자동 선택 + LangGraph Supervisor) |
| MCP-REG-001 | src/claude/task/task-mcp-registry.md | Dynamic MCP Tool Registry |
| OLLAMA-001 | src/claude/task/task-ollama-client.md | Ollama LLM 클라이언트 모듈 |
| AGENT-005 | src/claude/task/task-middleware-agent-builder.md | LangChain Middleware 기반 에이전트 빌더 |
| AGENT-006 | src/claude/task/task-auto-agent-builder.md | 자연어 기반 자동 에이전트 빌더 |
| AGENT-007 | src/claude/task/task-planner-agent.md | 공통 Planner Agent |
| AUTH-001 | src/claude/task/task-auth.md | 인증/인가 시스템 (JWT, RBAC) |
| CHAT-001 | src/claude/task/task-general-chat-api.md | General Chat API (ReAct + 검색 + MCP + 멀티턴) |
| CHAT-HIST-001 | src/claude/task/task-chat-history-api.md | 대화 히스토리 조회 API |
| LLM-MODEL-REG-001 | docs/archive/2026-04/llm-model-registry/ | LLM 모델 레지스트리 (llm_model 테이블 CRUD) |
| AGENT-SHARE-001 | docs/archive/2026-04/shared-custom-agent/ | 사용자 커스텀 에이전트 공유 빌더 |
| DI-WIRING-001 | docs/archive/2026-04/missing-di-wiring/ | main.py 누락 DI 배선 일괄 수정 |
| CUSTOM-RAG-TOOL-001 | docs/archive/2026-04/custom-rag-tool/ | 커스텀 RAG 도구 (에이전트별 검색 범위/파라미터 설정) |
| EMB-REG-001 | docs/archive/2026-04/embedding-model-registry/ | 임베딩 모델 레지스트리 (embedding_model 테이블 + 컬렉션 자동 차원 결정) |
| DOC-BROWSE-001 | docs/01-plan/features/collection-document-chunks.plan.md | 컬렉션별 임베딩 문서 및 청크 조회 API (문서 목록 + 청크 상세) |
| UNIFIED-UPLOAD-001 | docs/archive/2026-04/unified-pdf-upload-api/ | 통합 PDF 업로드 API (Qdrant 벡터 + ES BM25 동시 저장 단일 엔드포인트) |
| UNIFIED-UPLOAD-FIX-001 | docs/01-plan/features/fix-unified-upload-es-rdb.plan.md | 통합 업로드 ES/RDB 수정 (Kiwi 형태소 분석 + morph_text BM25 연동 + 문서 메타데이터 RDB 등록) |
| DOC-DELETE-001 | docs/01-plan/features/document-delete-api.plan.md | 문서 삭제 API (Qdrant 청크 + ES 청크 + MySQL 메타데이터 3중 동기 삭제, 단건/일괄) |
| AGENT-CHAT-001 | docs/01-plan/features/agent-chat-history.plan.md | 에이전트별 채팅 기록 관리 (conversation_message에 agent_id 추가, 에이전트별 세션 조회 API) |
| PDF-ANALYZER-001 | docs/archive/2026-05/pdf-analyzer/ | PDF 유형 분류기 (앞 N페이지 샘플링 → text/ocr/table/multimodal 분류, 라우팅 계층용 결과 반환) |
| PYMUPDF4LLM-001 | docs/archive/2026-05/pymupdf4llm-parser/ | pymupdf4llm 기반 Markdown PDF 파서 (구조 보존 파싱, 테이블 포함/제외 옵션, 전체 문서 단일 MD 출력) |
| RERANKER-001 | docs/archive/2026-05/reranker-module/ | Reranker 모듈 (Lost in the Middle 대응 PositionalReranker 양끝 배치 + RerankerInterface 전략 패턴) |
| PDF-ROUTING-001 | docs/archive/2026-05/pdf-routing/ | PDF 라우팅 모듈 (PDFDocumentType 기반 파서 자동 선택, Policy Pattern + string 매핑, graceful fallback) |
| PYMUPDF4LLM-META-001 | docs/archive/2026-05/pymupdf4llm-page-metadata/ | pymupdf4llm 페이지별 메타데이터 보존 (page_chunks=True, section_title/has_table 추출) |
| ADV-PARSER-001 | docs/archive/2026-05/advanced-document-parser/ | 고도화 문서 파서 (좌표 기반 요소 추출 → 7단계 레이아웃 분석 → 품질 점수 → fallback → section-aware 청킹) |
| RAGAS-001 | docs/archive/2026-05/ragas-evaluation/ | RAGAS 평가 모듈 (배치/실시간 평가, 테스트셋 CRUD, 9개 지표, 13 API 엔드포인트) |
| TABLE-FLAT-001 | docs/archive/2026-05/table-retrieval-enhancer/ | 표 검색 향상 모듈 (Parent-Child 청킹 표 플래트닝, 부모=원본 markdown 보존, 자식=의미 문장 변환, Strategy Pattern) |
| FIX-TOOL-BYPASS-001 | docs/archive/2026-05/fix-agent-creation-tool-bypass/ | Agent 생성 시 tool_configs 바이패스 수정 (명시적 도구 선택 경로 + prefix 정규화 + ValueError→422 분류) |
| MULTI-AGENT-001 | docs/archive/2026-05/multi-agent-composition/ | 멀티 에이전트 조합 (Sub-Agent 재귀 컴파일, 순환참조/중첩깊이/접근권한 3대 정책, Task 위임 방식) |
| AGENT-CHAT-MT-001 | docs/archive/2026-05/agent-chat-multiturn/ | Agent Multi-turn 대화 (RunAgentUseCase에 session_id 기반 히스토리 로드/저장/요약 통합, 하위호환 100%) |
| SEARCH-PIPE-001 | docs/archive/2026-05/search-pipeline-refactor/ | 검색 파이프라인 리팩터 (ToolMeta.category 기반 워커 분기 + Search Node 직접 실행 + Answer Agent 자동 주입) |
| NORI-001 | docs/archive/2026-05/nori-analyzer-integration/ | ES Nori 한국어 분석기 (mixed decompound + 18 stoptags POS filter, content^1.5 부스트, Zero-downtime 마이그레이션) |
| ADV-INGEST-001 | docs/archive/2026-05/advanced-ingest-pipeline/ | 고도화 PDF Ingest 파이프라인 (9노드 LangGraph, 5모듈 통합, Qdrant+ES 이중 색인, POST /api/v1/ingest/pdf/advanced) |
| ADMIN-RAGAS-001 | docs/archive/2026-05/admin-ragas-dashboard/ | Admin RAGAS 대시보드 (통계 카드+실행 목록+상세, 4 Admin API, AdminRagasPage 프론트엔드, 95% Match Rate) |
| AGENT-OBS-001 | docs/archive/2026-05/agent-run-observability/ | Agent Run 운영 관측성 M1 (5개 신규 테이블 ai_run/step/tool/retrieval/llm_call + llm_model 가격 컬럼, LangChain UsageCallback 단일 인터셉트, 사용자×LLM 토큰/비용 집계, LangSmith trace_id 영속화, 96% Match Rate, 118 tests) |
| AGENT-OBS-002 | docs/archive/2026-05/agent-run-observability-m2/ | Agent Run 운영 관측성 M2 (Tool Call Wiring — BaseTool callback-driven `on_tool_start/end` 후킹, `RunContext.tool_call_id` ContextVar 자동 set/reset, `ai_tool_call` 자동 INSERT + `ai_llm_call.tool_call_id` FK 자동 연결, 1 new + 1 modified prod file, 0 migrations, 98% Match Rate, 61 tests) |
| AGENT-OBS-003 | docs/archive/2026-05/agent-run-observability-m3/ | Agent Run 운영 관측성 M3 (Run Step Wiring — `WorkflowCompiler.add_node` 시점 decorator wrapping, `track_step` async 컨텍스트 매니저, `ai_run_step` 자동 채움 + `ai_tool_call.step_id` / `ai_llm_call.step_id` FK 자동 연결, SupervisorDecision.reasoning free win, 1 new + 4 modified prod files, 0 migrations, 99% Match Rate, 28 unit + 29 regression tests) |
| AGENT-OBS-004 | docs/archive/2026-05/agent-run-observability-m4/ | Agent Run 운영 관측성 M4 (Retrieval Wiring + Read APIs + Pricing PATCH — `InternalDocumentSearchTool._format_results` async + best-effort `record_retrieval`, 5 read APIs (`GET /agents/runs/{id}` + `/admin/usage/{users,llm-models,by-node}` + `/usage/me`), `PATCH /llm-models/{id}/pricing` + `cost_calculator.invalidate` use case 캡슐화로 M1 G1 영구 해소, `NodeUsageRow` + `aggregate_by_node` INNER JOIN, ★ /admin/usage/by-node 노드별 차지백 첫 노출, 9 new + 8 modified prod files, 0 migrations, 98% Match Rate, 42 new + 121 regression = 163/163 tests) |
| AGENT-OBS-005 | docs/archive/2026-05/agent-run-observability-m5/ | Agent Run 운영 관측성 M5 (Tavily Retrieval + Admin Run List + Aggregate Index — `TavilySearchTool._arun` 재구성 + best-effort `record_retrieval` with `collection_name="tavily_web"` + URL `[:150]` truncate + `metadata_json.url_full` 보존, `GET /api/v1/admin/runs?from=&to=&user_id=&agent_id=&status=&limit=&offset=` + `RunListFilters` 도메인 + `asyncio.gather(list, count)` 정합 캡슐화, V023 `idx_llm_call_created` 단독 인덱스로 집계 API 슬로우 사전 방지, ★ ai_retrieval_source 컬럼 재사용으로 internal+tavily 한 트리 자동 통합 (M4 RunDetailResponse 변경 0), 2 new + 7 modified prod files, 1 migration (인덱스만), 98% Match Rate, 19 new + 136 regression = 155/155 tests) |
| AUTH-CTX-001 | docs/archive/2026-05/agent-user-context/ | Agent User Context (사용자 신원·권한을 `AuthContext` frozen ValueObject + ContextVar로 Agent 런타임에 전파, `user_profiles` + permissions 마스터 3종 테이블 신설, LLM에는 whitelist prepend 블록(이름/부서/권한 라벨)만 노출, Tool/Repository 명시 권한 검증, 회원가입 schema 확장, 관리자 `/admin/users/{id}/permissions` 부여·회수 API, ★ "LLM은 의도 해석, 백엔드는 권한 집행" 원칙 확립 → 사내 데이터(연차/공지/HR 등) 도구 안전 확장 기반 마련 + 기존 `RunContext` 책임 분리로 관측성 계층 보존, 7 migrations V024–V030, 19/20 FR (FR-19 deferred), 초기 78%→Act 후 95% Match Rate, ~83 tests + 1872 passed 0 new failures) |
| AUTH-USER-REG-001 | docs/archive/2026-05/admin-user-registration/ | 관리자 사용자 등록 (프론트 `admin/users`에서 "사용자 등록" 버튼→모달로 직원 직접 등록, `POST /api/v1/admin/users` 즉시 `approved` + 전체 프로필(이름/직급/사번/입사일) + role + 부서, **단일 트랜잭션** `Depends(get_session)` 단일 세션을 User/Profile/Department 3 repo 공유 → 부서 검증 실패 시 전체 롤백, `GET /api/v1/admin/users` 전체 목록(부서명 포함, status/q/limit/offset), `AdminCreateUserUseCase` + `ListUsersUseCase` 신설 + `UserRepository.find_all`, 프론트 전체/승인대기 탭 + `UserRegisterModal`(8필드 + 부서 드롭다운 + 검증 + 백드롭 비차단/X버튼/Esc 닫기), ★ agent-user-context 자산 재사용으로 마이그레이션 0·신규 도메인 0·무회귀(register/승인/권한 미변경) + `list_all` id→name 맵으로 부서명 N+1 선제 제거, 34 tests(BE 23 + FE 11), FR 12.5/13 (FR-11 목록 필터 UI 후속), Check 1회 97% Match Rate) |
| ANALYSIS-NODE-001 | docs/archive/2026-06/analysis-node-agent/ | 분석 전용 노드 에이전트 (Supervisor 그래프 `category="analysis"` 전용 노드 — 엑셀 첨부 시 기존 `ExcelAnalysisWorkflow` 래핑, 없으면 검색결과/전체 대화 문맥 LLM 분석 후 결과만 반환→supervisor 복귀, `ToolCategory+="analysis"` + `SupervisorState.attachments` + `Callable` getter DI 역의존(None graceful fallback) + `function_node_ids`로 함수노드 판별 개선, Check 95%→Act-1 100%, 소스 9 + 테스트 12 케이스, 0 migrations, 엑셀 HTTP 업로드 계약은 후속) |
| CHART-BUILD-001 | docs/archive/2026-06/chart-builder/ | Chart Builder (분석/답변 텍스트 → LLM `with_structured_output` 수치 추출 → **Chart.js 네이티브 config(JSON)** 생성, General Chat `chat_answer_completed.charts`에 주입(프론트 `chat-chart-rendering` 이미 wired), `ChartConfig`=프론트 `ChartPayload`(`{type,data,options}`, 화이트리스트 bar/line/pie/doughnut/scatter/radar), 핵심 **추출↔표현 분리** — LLM은 데이터(`ChartDraft`)만·색상/options는 domain `ChartStylePolicy` 결정론적 조립(LLM hex 위임 회피), `_maybe_build_charts`가 기존 `VisualizationRoutingPolicy`/`Classifier` 재사용해 visualize 판정시만 빌드·실패/비-visualize/미주입 시 `charts=[]` graceful, DI REST+WS 공유 팩토리 1곳·상한 `settings.chart_max_count=3`, DDD domain→infra 참조 0, gap-detector 98%→설계 정정 100%, 소스 3 신규+5 수정+테스트 4 신규(25 pass) 0 migrations, 프론트 REST 타입 `/api-contract-sync`·Excel/Supervisor 연동은 후속) |
| WS-EXCEL-ATTACH-001 | docs/archive/2026-06/ws-agent-excel-attachment/ | WS Agent 엑셀 첨부 (`/ws/agent/{run_id}` 입구 단절 해소 — 다운스트림 `RunAgentRequest.attachments`+분석 노드는 이미 준비됨, **입구 한 곳만 연결**, 분석 노드 수정 0건. HTTP 업로드 `POST /api/v1/agent/attachments`→`file_id` 발급→WS subscribe `attachments:[{type,file_id}]` 참조→`AttachmentResolver` 소유자 검증 후 `file_path` 해석→기존 attachments로 전달→run 종료(정상/예외/disconnect) `finally` 자동 삭제 + `store.purge_expired` TTL 백업 정리(누수 방지), Thin DDD 4레이어 신설(domain `AttachmentType`/`AttachmentPolicy`/`StoredAttachment`/exceptions·infra `AgentAttachmentStore` tmp dir+사이드카 메타·app `UploadAttachmentUseCase`/`AttachmentResolver`·interfaces router+`ws_schemas`+`ws_router`), 보안 file_id uuid4·소유자(업로더==뷰어) 검증·경로 traversal 차단·확장자 allowlist·크기 제한, excel 우선·`AttachmentType` Enum OCP 확장 구조, 풀스택(프론트 `agentAttachmentService`+`useAgentRunStream` subscribe+`ChatInput` 엑셀 첨부 UI+`ChatPage` 연동) `/api-contract-sync`, gap-detector 97%→TTL sweep 보완 후 99% Match Rate, 레이어 위반 0·print 0, 백엔드 57+프론트 20=77 tests, 0 migrations) |
| MCP-REG-UI-001 | docs/archive/2026-06/mcp-registry-admin-ui/ | MCP 서버 등록/관리 Admin UI + 연결 테스트 (기존 백엔드 CRUD `/api/v1/mcp-registry` 재사용 + 연결 테스트 엔드포인트 `POST /{id}/test` 1개만 신규 — `MCPConnectionTestUseCase`가 `MCPCallClient.list_tools` 직접 호출로 실패 표면화·미존재 None→404·예외 ok=False+logger, 프론트 Admin 전용 `/admin/mcp-servers` 신규 화면(목록+SSE/Streamable HTTP 동적 폼+시크릿 병합+연결 테스트 패널+삭제) AdminRoute 보호, ★ 시크릿 병합 정책 — 수정 시 빈 시크릿 필드는 PUT 본문에서 제외해 기존 암호화 시크릿 보존·마스킹 `****` 재전송 안 함(P-3 요청바디 레벨 검증), DDD domain·infrastructure 무변경(application+interfaces+frontend만), gap-detector 98%→페이지 테스트 추가 100% Match Rate, 백엔드 5(1신규/4수정)+프론트 11(8신규/3수정), 0 migrations, 백엔드 23+프론트 17 tests, R1 백엔드 RBAC·저장전 테스트·사용자별 화면은 후속) |
| SKILL-BUILD-001 | docs/archive/2026-06/skill-builder/ | Skill Builder (재사용 능력 단위 Skill = instruction 지시문 + script_content 실행 스크립트(저장 전용) 저장·관리 — agent_builder/mcp_registry 동일 Thin DDD 4계층으로 `skill_definition` 도메인 신설, V033(trigger→trigger_text 예약어 매핑·ENUM visibility·FK departments SET NULL), visibility(private/department/public)+RBAC+soft-delete+fork, API 7종 `/api/v1/skills`(CRUD+my+list RBAC+fork), `SkillBuilderPolicy`(검증·none+script 충돌차단)+`SkillVisibilityPolicy`/`SkillForkPolicy`, Repository 평문 TEXT(cipher 없음), 프론트 `AdminSkillsPage`(목록+SkillFormModal 인라인+삭제+Fork+"저장 전용" 안내)+useSkills+skillService `/api-contract-sync`, script_content 실행 0(eval/exec/subprocess 0) 현 phase 위험 제로, gap-detector 97% 누락 0, BE 54+FE 11 tests, V033 적용 필수, 후속=skill-agent-integration(Phase A 완료)/skill-script-runtime(Phase B)) |
| AGENT-SKILL-001 | docs/archive/2026-06/skill-agent-integration/ | Skill-Agent 결합 Phase A (skill-builder가 저장만 하던 Skill을 에이전트 실행에 결합 — instruction 주입, script 실행은 Phase B로 격리. 별도 `agent_skill` 조인 도메인 Thin DDD 4계층 + V034(FK CASCADE×2 + uq_agent_skill), `SkillAttachPolicy`(MAX 3·중복)/`SkillInjectionPolicy`(sort_order 병합·40k 가드·부착0=불변), Attach/Detach(멱등)/List UseCase(권한=에이전트 수정 ∧ skill 접근 두 도메인 정책 app 계층 조합), ★ 주입 D3=`RunAgentUseCase._inject_attached_skills`가 `dataclasses.replace`로 supervisor_prompt 병합 — `WorkflowCompiler` 무수정·repo optional(None=기존 동작 유지)·user_context 최외곽 보존, 라우터 `/{agent_id}/skills` GET/POST/DELETE, 프론트 풀스택 `AgentSkillPanel`("script 미실행" 안내+max-3), gap-detector 97% 기능갭 0, D4 서브에이전트·Phase B script 실행은 Out of Scope, BE 27+FE 6 tests, V034 적용 필수) |
| LLM-MODEL-REG-002 | docs/archive/2026-06/self-hosted-qwen-endpoint/ | 폐쇄망 자체 호스팅 LLM(Qwen) 연결 지원 (LLM-MODEL-REG-001 확장 — 폐쇄망 GPU vLLM(OpenAI 호환) Qwen에 붙기 위해 모델별 커스텀 엔드포인트 `base_url` 지원. **신규 provider 없이** `provider="openai"` 재사용 + `llm_model.base_url` VARCHAR(500) NULL 컬럼(V035 additive ALTER) 한 축만 추가. `LLMFactory._create_openai`/`_create_ollama`가 base_url 있으면 `ChatOpenAI`/`ChatOllama`에 전달, `_resolve_api_key(allow_empty=bool(base_url))`로 인증 없는 vLLM은 `"EMPTY"` 더미키 통과. 엔드포인트는 env 아닌 **DB 모델별 저장**(멀티 모델/서버 대응)·api_key는 기존 `api_key_env` 참조 유지, Create/Update UseCase가 `request.base_url or None`로 빈문자열→None 정규화(self-host 해제). 풀스택 `/api-cotract`로 프론트 `LlmModel.base_url?`+MSW mock 동기화(등록 폼은 읽기전용이라 후속). ★ "vLLM OpenAI 호환 → base_url 한 칼럼만으로 폐쇄망 흡수", ★ "self-host 엔드포인트=평문 DB·인증키=env 분리" 교훈. gap-detector 100%(갭 0), 소스 8(+43/-8)+테스트 4(신규20+회귀40+router4=64 pass)+프론트 2(tsc 0), V035 적용 필수. 프론트 등록 폼 base_url 입력 UI·폐쇄망 실연결 검증(streaming usage_metadata)·임베딩 self-host는 후속) |
| AGENT-VISUAL-001 | idt_front/docs/archive/2026-06/agent-builder-visual-canvas/ | Agent Builder 비주얼 캔버스 (프론트 단독 — `/agent-builder` 스튜디오 좌측 "비주얼" 탭 `disabled`(준비중) 해제, 폼 상태 `AgentBuilderFormData`를 **React Flow(@xyflow/react v12)** 노드 그래프로 렌더. 에이전트(중앙 허브)+스킬/도구/서브에이전트/미들웨어/모델 6노드 + 색상별 점선 베지어 엣지 5개, ★ **단일 소스 동기화** — 폼·비주얼이 같은 `form` prop 공유로 별도 sync 로직 0·변경은 항상 `onChange`, ★ **모달 재사용** — 노드 "추가/설정" 버튼이 `LeftConfigPanel` 기존 `ToolPickerModal`/`SubAgentManagerModal`/`ModelSettingsModal` open 상태만 토글(완전 인터랙티브)·"Edit in Form"→폼 탭 전환, ★ **position 보존** — 폼 변경 시 `mergeData`로 노드 `data`만 교체·드래그 좌표 유지·"기본 레이아웃"으로 `DEFAULT_LAYOUT`+`fitView` 복원, 스킬·미들웨어는 표시 전용 `disabled` 플레이스홀더(향후 확장 지점)·노드 위치 비영속, 순수 함수 `buildNodes`/`buildEdges`/`buildModelLabel` 분리로 테스트 집중, ★ React Flow+jsdom 제약(노드 클릭 d3-drag 크래시·ResizeObserver 폴리필) → 콜백은 노드 단위 테스트·캔버스는 렌더 스모크로 분리, 의존성 `@xyflow/react@12.11.1` 추가·lazy 지연 로딩, gap-detector 100% Match Rate(6/6 MATCH, 편차 3건 모두 경미), 구현 421줄(6파일)+테스트 270줄(4파일·23 케이스)·LeftConfigPanel 수정, type-check/lint/agent-builder 51 tests 통과·회귀 0, 0 migrations. 스킬·미들웨어 실연동·위치 영속화는 후속) |
| AGENT-STREAM-001 | docs/archive/2026-05/agent-run-streaming-sse/ | Agent Run SSE Streaming (transport-독립 단일 코어 — `RunAgentUseCase.stream()`이 `AsyncIterator[AgentRunEvent]`만 yield + LangGraph `astream_events(version="v2")` 9 이벤트 매핑(`run_started`/`node_*`/`tool_*`/`token`/`answer_completed`/`run_completed`/`run_failed`), `execute()`는 stream() 소비자로 재구성 (Breaking change 0), 신규 `GET /api/v1/agents/{id}/run/stream` SSE 엔드포인트 + `get_current_user_from_query_token` 쿼리 토큰 인증 + heartbeat 15s + `request.is_disconnected()` cancellation + `tracker.fail_run(CancelledError)` 마감, `AgentRunEventSseFormatter` (format/format_error/format_heartbeat) wire bytes 직렬화, SSE 헤더 3종(Cache-Control/Connection/X-Accel-Buffering), ★ 어댑터 fixture 패턴(`astream_events` → 내부 `ainvoke` 호출)으로 기존 13 observability 테스트 코드 변경 0건 회귀 보존, ★ `main.py` 변경 0줄 (기존 `JWTAdapterInterface`/`UserRepositoryInterface` DI placeholder 재사용), 8 new + 5 modified files, 0 migrations, 0 domain 영속화 변경, 96% Match Rate, 58 new (Domain 8 + Formatter 11 + Auth 4 + Stream 26 + Router 9) + 154 regression = 212/212 tests) |
| AGENT-SCHED-001 | docs/archive/2026-07/agent-schedule/ | 에이전트 스케줄 (독립 도메인 `agent_schedule` CRUD 7종 + 외부 트리거 실행 엔진 — once/daily/weekly/cron 4유형(에이전트당 10개·cron 최소 10분), 지침 `{today}`/`{now}`/`{weekday}` 치환 후 실제 사용자 질문으로 `RunAgentUseCase` 전송(R9, 새 세션+ai_run 관측성), `POST /api/v1/internal/schedules/trigger`(X-Scheduler-Token, 빈값 503/불일치 401) + status 엔드포인트, ★ 포크 미복사(R5) — 별도 테이블 FK + 화이트리스트 복사 구조로 자동 배제 + 시그니처 가드 회귀 테스트, ★ `FOR UPDATE SKIP LOCKED` claim 선점으로 동시 트리거 이중 실행 차단·once 소진 자동 disable·misfire 백필 없음, ★ `ScheduleRunSink` 포트로 결과 처리 추상화(이력+새 세션, 알림 sink additive 가능), ★ 트리거 싱글턴은 session_factory만 보유(DB-001) — claim/실행/이력 짧은 트랜잭션 분리, last_run_at=성공/실패 무관 '마지막 실행 시도'(Act-1 A안), off→on 토글 시 next_run_at 재계산, V038 + croniter>=2.0, gap-detector 96%→98%, 테스트 111 pass, 배포 전 필수: V038 적용 + SCHEDULER_TRIGGER_TOKEN + 외부 cron 매 분 등록, 후속: agent-schedule-ui) |
| AGENT-COMPOSE-001 | docs/archive/2026-07/nl-agent-composer/ | 자연어 에이전트 컴포저 (nl-agent-composer — **자연어 한 문장 → 내부+MCP 도구 조합 에이전트 초안(무저장) 단발 API** `POST /api/v1/agents/compose`. 후보 수집(내부 `TOOL_REGISTRY` + MCP `tool_catalog` 개별 도구, 카탈로그 미동기화 시 mcp_registry 서버 단위 메타 폴백 D2) → 신규 `AgentComposer` LLM 1회 structured output(역량 분해 + coverage 근거 인용 + 도구 선택 + system_prompt + 이름 제안, D3) → **서버 측 보정이 최종 결정**: 환각 tool_id drop(FR-06) → `mcp:{srv}:{tool}`→`mcp_{srv}` 매핑·서버 단위 병합(min sort_order, FR-05) → MAX_TOOLS(5)·프롬프트 4000자 clamp(D7, 에러 대신 notes) → coverage(full/partial/none) 재산정 + `missing_capabilities[]`(사유+대안 안내). **DB 쓰기 0건** — 확정 저장은 기존 `POST /agents` 최소 확장 2건: ① `CreateAgentRequest.system_prompt` 프리필(값 있으면 PromptGenerator 스킵, D1/FR-09) ② `_build_skeleton_from_tool_ids` async 전환 + `mcp_` 분기로 MCP 레지스트리 메타 해석(FR-08 — 기존 `get_tool_meta` ValueError로 **mcp_* 저장 불가였던 잠재 버그 해소**, 미등록/비활성 422). ★ "LLM은 제안, 서버가 판정(`ComposePolicy` 순수 도메인 정책), 사람이 확정" 3단 안전 구조, ★ 생성 공통단(ToolSelector/PromptGenerator)·실행부(workflow_compiler) 무수정 완전 신규 모듈(`agent_composer` 4계층), ★ Plan 단계 file:line 코드 리딩으로 저장 경로 mcp_* 거부 버그 선발견 — 초안만 만들었으면 반쪽 기능이 됐을 것. gap-detector 95% Match Rate(Gap 5건 중 40줄 규칙·병합 sort_order 2건 Check 내 즉시 수정, Act 0회). 신규 src 6 + tests 6(1,443줄) + 변경 4(agent_builder schemas/use case, main.py DI, config `composer_max_candidates`) + .env.example, 마이그레이션 0. 신규 테스트 38 pass + 회귀 agent_builder 390·domain 1,923 그린, verify-architecture/logging/tdd 위반 0. 후속: E2E 수동 1회(compose→저장→run), 프론트 프리필 UI + `/api-cotract`(system_prompt 필드 동기화), MCP 개별 도구 단위 노출 제어(R1) |
| KB-SCOPE-001 | docs/archive/2026-07/knowledge-base-scoping/ | 지식베이스 스코핑 (knowledge-base-scoping — 벡터DB **물리 컬렉션(관리자 전용)/논리 지식베이스(사용자, payload 필터 단위) 분리**를 신규 경로로 병행 도입(기존 경로 무수정, 추후 갈아끼기). 사용자별 물리 컬렉션 난립 → 지식베이스 공유 곤란·권한이 컬렉션 단위로만 → 컬렉션 생성은 `POST /api/v1/admin/collections`(`require_role('admin')`, 기존 `CollectionManagementUseCase` 위임)로 제한, 사용자는 `knowledge_base`(kb_id=UUID PK) 논리 단위를 생성해 문서 업로드 시 청크 payload에 `kb_id`/`kb_name`을 주입 → **Qdrant payload + ES 문서 양쪽 격리**. 메타는 **MySQL 1차 소스 + payload 이중 기록**(추후 payload 단독 필터 대비), 권한은 기존 3단계 스코프(PERSONAL/DEPARTMENT/PUBLIC) 재사용하되 `KnowledgeBasePolicy` 독립 판정(`collection_permissions` 불연동, PUBLIC 쓰기=소유자+ADMIN만 D8). ★ **필터 키=불변 UUID kb_id**(이름은 kb_name 표시용 동봉 → rename 시 벡터 재태깅 불필요, D1), ★ **물리 컬렉션 배정을 `CollectionAssignerInterface`로 추상화**(현재 `UserSelectedCollectionAssigner`=사용자 선택+읽기권한 검증, 추후 AdminMapped로 구현체 교체, D6), ★ **업로드 재사용 = `UnifiedUploadRequest.extra_metadata` optional additive 필드** + `execute`/`_store_to_es` 양곳 `setdefault`(고정키 우선) → 기존 호출·테스트 무영향(D2/D3), ★ ES는 화이트리스트 body라 매핑(kb_id/kb_name keyword)+코드 병합 2곳 필수(Qdrant는 metadata 전체 복사라 자동 전파), ★ **soft delete**(status active/deleted)로 kb_id 추적 보존 — (owner,name) DB 유니크 없이 active 중복만 UseCase 차단(V037 선례, 후속 벡터 정리 대비). 이번 범위=KB CRUD + 지정 업로드 신규 경로. 의도 편차 3건(create not-found→422 §7.1 명세대로 / `find_all_active` 추가로 ADMIN 전체조회 / D10 payload index·D11 activity log·D12 문서수 집계 이연) 감점 제외. gap-detector 98% Match Rate(Low 4건=문서 표기 수준, High/Medium 0건). 신규 파일 12(domain 3+application 3+infra 2+api 2+db 1+models 1) + 기존 additive 수정 4(unified_upload schemas/use_case, es_index_mappings, main.py DI) · 마이그레이션 1(V040__create_knowledge_base). 신규 56 pass(policy 29+application 23+extra_metadata 4) + 라우터 배치 33 pass, 기존 회귀 0, verify-architecture/tdd 신규코드 위반 0·print 0·request_id 전파. 배포 전 필수: 기존 ES 인덱스 `PUT /{index}/_mapping` kb_id/kb_name keyword 1회 선반영(Design §6.5), V040 DB 적용. 후속: kb-rag-filter(`RagToolConfig.metadata_filter`에 kb_id 연동→에이전트 검색 격리), kb-vector-cleanup(삭제 시 Qdrant delete-by-filter+ES delete-by-query), collection-path-migration(기존 컬렉션 생성 경로 차단·백필·구 API deprecated), 프론트엔드 지식베이스 관리 UI + `/api-cotract`) |
| AGENT-LIMIT-001 | docs/archive/2026-07/agent-recursion-limit/ | 에이전트 반복 한도 (agent-recursion-limit — 그래프 실행 **반복 한도의 에이전트별 설정(DB) + 한도 도달 시 오류 대신 조기 답변**. 문제: LangGraph `recursion_limit` 미설정(시스템 기본 25스텝)이 자체 가드(하드코딩 10)보다 먼저 발동 → GraphRecursionError → RUN_FAILED 답변 불능 + 한도 도달 시 수집 정보 폐기. 해결: `agent_definition.max_iterations`(V045 additive, 기본 25, 10~1000)를 단일 소스로 `SupervisorConfig` 주입 + `graph_config["recursion_limit"]=×10+20` 파생(state 가드 선발동 보장) → supervisor `limit_reached` 플래그 → `route_to_worker_or_final` 확장(워커 미실행도 `final_answer` 경유) → 안내 포함 답변 + `ANSWER_COMPLETED.limit_reached` additive payload(이벤트 카탈로그 무변경) → RUN_COMPLETED. ★ `IterationLimitPolicy` 상수 1곳 집약(DEFAULT/MIN/MAX/RECURSION_STEP_FACTOR/SUB_AGENT_DIVISOR — 변경 용이 요구), ★ sub-agent 부모 절반(하한 5) 상속 + 서브 그래프 `ainvoke` config 미전달 동일 결함 발견·recursion_limit 파생 전달, ★ GraphRecursionError 안전망 — 축적 메시지 강등 답변, 불가 시에만 RUN_FAILED, ★ token_limit 가드 현행 유지. API create/update/get additive + fork 승계. gap-detector 98%(D1~D10 구현 일치 100%, 기능 Gap 0). 신규 테스트 38 + 회귀 660/660, 마이그레이션 1 + 소스 15 수정. 배포 전 필수: V045 DB 적용. 후속: Studio UI(idt_front)+`/api-cotract`, (선택) 라우터 422 테스트·token_limit 조기 답변 |
| DOC-EXT-001 | docs/archive/2026-07/document-template-extractor/ | 문서추출기 (document_extractor — 정형 문서(여신심의서 등) 양식을 등록해 채팅으로 자동 완성·파일 생성하는 도구 하나에 두 단계. **(A) 빌드타임 등록**(workflow_compiler 무관, stateless): 에이전트 빌더에서 PDF/Word 업로드 → MCP `pdf/doc→html` 변환 → LLM이 자동화 슬롯(값·생성) 추출·추천 → HITL 확정(거절/보강 refine·유휴 5분 재추천) → **프론트가 sample_value를 `{{key}}`로 토큰화(D2)** → 에이전트 생성 트랜잭션 편승 저장(`document_template`, `(agent_id,worker_id)` 종속). **(B) 런타임 실행**(workflow_compiler 경유): 전용 합성 노드가 지정 템플릿 로드 → 누적 컨텍스트(리서치 근거+대화) 분리 → **LLM은 JSON `{key:value\|null}` 값 결정만(D6, 계약 위반 재시도 후 파일 미생성)** → **순수 문자열 토큰 치환(재현성 100%)** → MCP `html→pdf/doc`(원본 포맷) 파일 생성 → 첨부 저장 → 인증 blob 다운로드 링크. ★ **GB6 하드 규칙**: 근거 없는 슬롯은 절대 채우지 않고 공란+`<mark>` 하이라이트(환각 금지). ★ "LLM=값 결정, 문서 변형=순수 치환" 책임 분리, ★ "등록(compiler 무관)/실행(compiler 경유) 분리", ★ "유니크 인덱스 대신 앱 레벨 정합(active soft-delete 후 insert)으로 재등록 충돌 회피", ★ COMPOSE_GUIDELINES를 domain 상수로 두어 infra→app 역참조 회피. 확정 결정 D1~D6 + GB6 100% 코드+테스트 반영, 아키텍처·컨벤션 100%(domain 순수·repository commit 없음·print 0). gap-detector 95%(Check) → Act로 프론트 G1(sessionStorage 드래프트 R4)·G2(슬롯 수동 추가·라벨 편집) 구현 → 99%. 신규 백엔드 18(domain 5+application 3+infra 8+api 1+db 1) + 프론트 5, 변경 백엔드 8 + 프론트 5, 마이그레이션 1(V037__create_document_template). 백엔드 117 + 전체 회귀 그린, 프론트 22 pass, tsc 0. 배포 전 필수: 변환 MCP 실도구 등록 + `/verify-mcp-connections` PoC(G3), V037 DB 적용(G4), `.env` `DOCUMENT_EXTRACTOR_*_TOOL_ID` 설정) |
| CHUNK-CLAUSE-001 | docs/archive/2026-07/clause-aware-chunking/ | 조·항 의미 경계 인식 청킹 파이프라인 (clause-aware-chunking — 문서 등록 1단계 rawchunk. ①조·항 경계 분할→②초과 시 토큰 분할→③overlap `ClauseAwareStrategy` opt-in 병행 도입, 경계 규칙(정규식)은 `chunking_profile` 테이블 관리자 CRUD + KB 단위 late-binding 오버라이드(V041/V042), 조=parent·항/호=child로 기존 parent_child 메타데이터 계약 완전 복제 → 하이브리드 검색 무수정, 페이지 결합+누적 offset으로 조의 페이지 경계 넘김 처리, 프로파일 부재 시 legacy 폴백으로 업로드 항상 성공, gap-detector 99%, 테스트 94 pass) |
| SECTION-SUM-001 | docs/archive/2026-07/card-section-summary/ | 카드 섹션 요약 파이프라인 (card-section-summary — 문서 등록 **2단계**: 카드 섹션(=clause-aware parent 조)별 LLM 키워드+3줄 요약 백그라운드 생성·저장. 업로드 후 인프로세스 잡(`asyncio.create_task`+`SessionScoped` 잡 스토어 독립 짧은 세션)이 섹션당 LLM 1회(structured output→JSON 폴백, vLLM 대응) → 동일 Qdrant 컬렉션 `chunk_type='section_summary'`(결정적 uuid5 ID 멱등 upsert, `section_ref`로 rawchunk 연결) + ES 전용 필드(`summary_text`/`summary_keywords`, BM25 미노출) 저장. 요약 LLM은 `chunking_profile.summary_llm_model_id`(V043, NULL=비활성) 관리자 지정 — `llm_model`(base_url vLLM) 재사용. 잡 문서당 1행(V044) + 상태/재시도 API(`GET/POST .../documents/{id}/section-summary(/retry)`, 실패분만 재처리·stale 복구). ★ 기존 검색 완전 격리 = Qdrant 어댑터 단일 초크포인트 must_not 가드 + ES 전용 필드 + doc_browse 제외. gap-detector 99%, 신규 14파일(1,211 LOC)+수정 15, 테스트 75 pass·회귀 0. 배포 전 필수: V043/V044. 후속: document-summary-routing(3단계)·summary-routed-retrieval·frontend·backfill) |
| DOC-SUM-001 | docs/archive/2026-07/document-summary-routing/ | 문서 요약 자동 체이닝 (document-summary-routing — 문서 등록 **3단계, 3부작 완결**: 섹션 요약 잡 completed 직전 `DocumentSummaryStep` 자동 체이닝(optional 주입, None=기존 불변). 섹션 요약 전량 → cap 이내 LLM 1회/초과 시 2계층 map-reduce → 동일 컬렉션 `chunk_type='document_summary'` 문서당 1 point(uuid5 멱등) + ES 기존 필드 재사용. 문서 키워드=섹션 키워드 빈도 집계(LLM 0회). completed=섹션 전량+문서 요약. ★ 재시도 free win(코드 0줄 — done_refs 스킵+failed==0 재진입으로 문서 요약만 재생성), ★ 마이그레이션/신규 API/ES 매핑 0, ★ 가드 요약 타입 집합 일반화(MatchAny). gap-detector 99%, 신규 1파일(322 LOC)+수정 12, 테스트 110 그린·회귀 0. 후속: summary-routed-retrieval(3계층 하강 라우팅 검색)·frontend·backfill) |
| ROUTED-RET-001 | docs/archive/2026-07/summary-routed-retrieval/ | 3계층 하강 라우팅 리트리버 (summary-routed-retrieval — **검색 파이프라인 4부작 완결**: `POST /api/v1/retrieval/routed` 신규 API. 임베딩 1회 → ①document_summary 벡터 top-K → ②섹션 요약 벡터(MatchAny)+BM25(`summary_text^1.5`/`summary_keywords`) RRF top-N(기존 RRFFusionPolicy 재사용, 요약 결정적 ID 3자 일치=병합 키) → ③`section_ref` ES ids query로 조(parent) 본문 확장 → 부족 시 하이브리드 폴백(조 단위 dedup, 실패 격리). **LLM 0회 결정론**, 라우팅 근거·`fallback_used` 관측 동봉. ★ 단계 3포트+VO 계약(구현 교체 자유), ★ `SearchFilter.metadata_any` additive(가드·equality 불변 회귀 고정). gap-detector 99%, 신규 12파일(776 LOC)+수정 3, 테스트 43·회귀 0, 마이그레이션/config 0. 후속: E2E 실측·rag-routed-integration(에이전트 연동)·routing-quality-eval(RAGAS)·backfill) |
| RAG-ROUTED-001 | docs/archive/2026-07/rag-routed-integration/ | 에이전트 RAG 도구 라우팅 opt-in (rag-routed-integration — **4부작의 첫 실사용 연결, 풀스택, 교차검증 구조**. 사용자 피드백으로 search_mode 값 추가안 폐기 → 독립 필드 `RagToolConfig.use_routed_search: bool=false`(tool_config JSON 저장, 마이그레이션 0). 도구 분기: true→`RoutedRetrievalUseCase`, **강등(4사유 reason 관측) 시 그 에이전트의 기존 search_mode 경로** — off 기준선과 일치해 토글 쌍 A/B 비교 성립. 필터 3분류(kb_id 매핑/viewer_department_ids 무시/visibility·custom 강등 — 권한 누수 차단), 결과 `[출처: 문서명 > 조 제목]`+요약 1줄+본문, record_retrieval metadata `search=routed`. 프론트 토글(기존 라디오 불변). gap-detector 99%, 수정 8파일·신규 프로덕션 0, 테스트 백 17+프론트 3·회귀 0(기존 도구 테스트 무수정 통과=기준선 증명). ⚠️ 토글은 생성 시만(Update 미지원 — 후속 agent-tool-config-update). 후속: 교차검증 실측·routing-quality-eval·backfill) |
