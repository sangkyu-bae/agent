# Archive Index — 2026-09

> 이 디렉토리는 PDCA 사이클이 완료된 피처 문서를 보관합니다.

| 피처 | 완료일 | Match Rate | 테스트 | 경로 |
|------|--------|-----------|--------|------|
| mcp-tool-auto-sync | 2026-09-01 | 98%(Structural/Functional/Contract 각 100% · Runtime 95% · Critical 0 · Important 2(G-01 동기화 버튼 실서버 복구 미검증 · G-02 is_active=false DB 반영 미확인 — 둘 다 실동작 MCP 서버 필요, QA 이관) · G-03(is_builtin 회귀 테스트 누락) Check 단계 즉시 해소 · 이터레이션 0 · SC 6/8 완전+2 부분 · FR 13/13 · UI 체크리스트 9/9) | 신규 26(SyncOutcome/hint_for/_redact 16 · Register sync 6경로 11 · Update sync 13 · is_builtin 보존 4(뮤테이션 검증) · DI 세션동일성 5 · 라우터 tool_sync 4 · 프론트 훅 2 + 관리화면 S-1~S-7) · 백엔드 영향범위 123 passed / 프론트 17 passed · tsc exit 0 · **회귀 0건**(백 29 failed·프론트 9 failed는 git stash baseline과 comm 대조 완전 일치) · 실서버 L1 4/4(**도달 불가 endpoint 등록 → HTTP 201 + tool_sync.ok=false 실증**, 검증 레코드 DELETE 204 정리) · 백 신규 6(sync_outcome.py + 테스트 5) 수정 6 / 프론트 수정 8 / 마이그레이션 0 / 의존성 0 — **MCP 서버 등록·수정 직후 SyncMcpToolsUseCase를 같은 세션으로 best-effort 호출해 tool_catalog에 자동 반영, 실패는 응답 tool_sync로 화면에 전달하고 서버별 [동기화] 버튼으로 복구**. Option C — 예외를 SQLAlchemyError(재전파)/그 외(흡수)로 **분류**. ★ Option A(`except Exception` 일괄)를 택했으면 세션이 PendingRollbackError로 남아 예외를 삼켜도 get_session 커밋 시점에 터져 **등록 API가 500** — "실패해도 등록은 성공"(FR-03)이 정확히 그 지점에서 깨진다. 성립 근거는 SyncMcpToolsUseCase가 list_tools(네트워크)→upsert(DB) 순서라 흔한 실패에서 세션이 무결한 것. ★ **사용자 지적이 설계 결함을 잡음** — Plan v0.1(자동 sync만)은 sync 1회 실패 시 복구 수단이 "수정창 열어 그대로 저장(FR-02가 우연히 재실행)/삭제 후 재등록/curl"뿐인 **막다른 길**이었고, v0.2에서 FR-09~FR-12(응답 tool_sync + 상시 노출 동기화 버튼 + amber 배너) 편입으로 해소. 버튼은 실패 여부 무관 상시 노출이라 MCP 서버가 나중에 도구를 추가한 경우의 재동기화도 겸함. ★ 중첩 tool_sync 채택 이유 = `null`(sync 미수행, GET)과 `{ok:false}`(시도 후 실패)를 구분 — 평면 필드면 못 가림(실 OpenAPI `anyOf[ToolSyncResultResponse, null]` 확인). ★ 세션은 DB-001 "요청 1건=세션 1개"로 공유(별도 세션안은 규칙 위반) — test_mcp_registry_di_wiring.py가 UseCase 그래프 순회로 `_session` 객체 id 집합 크기 1을 단언, repo 인스턴스 2개는 무방/세션 2개가 금지. ★ 통과하는 테스트를 믿지 않고 **뮤테이션 검증** — SyncMcpToolsUseCase가 is_builtin=True를 넘기도록 변조하니 4건 중 3건 실패 확인 후 원복(git diff 0줄). ★ 회귀 판정은 pass 개수가 아니라 git stash로 변경 전 재현 후 FAILED 목록 comm 대조. 함정: **Design §6.3이 `err.response.status`로 적었으나 authApiClient는 ApiError(message, status)로 reject → `err.status`가 정답**(S-5 테스트가 포착, 설계가 기존 인프라 코드를 열어보지 않은 실수) · createServer는 axios 응답이 아니라 McpServer를 직접 반환(res.data ✗) · 행 단위 비활성은 `syncingId` state로(기존 [테스트] 버튼의 전역 isPending 패턴은 모든 행을 잠금) · 등록 후 목록 재조회로 tool_sync가 null이 되므로 실패 정보는 페이지 로컬 state 보관 · MCP endpoint의 `?api_key=` 유출 대비 `_redact()` 로그 마스킹 · 삭제는 FK ON DELETE CASCADE로 자동 정리라 sync 불필요. 미이관: G-04 execute() 40줄 초과(register 66/update 80, **변경 전 52/61로 기존 위반**) · G-05 apiError() 헬퍼가 err.response.data.detail 읽어 항상 fallback(기존 버그) · G-06 /tool-catalog/sync 500 그대로(FR-07 무변경 의도적 수용) · G-07 CatalogTool.mcp_server_name 프론트 타입에만 존재 | [mcp-tool-auto-sync/](./mcp-tool-auto-sync/) |
| agent-model-benchmark | 2026-09-02 | 97.1%(Check 88.6% → Act-1 94.3% → Act-2 97.1% · Structural 100% / Functional 91.4% / Contract 100% · 정적 전용 공식(백엔드 미기동으로 Runtime 축 없음) · Critical 0 · Important 5(G-1 실제비용 · G-2 실험조건 · G-5 부분실패 미검증 · G-11 목록 오염) 전부 해소 · Minor G-3(행별 스피너)·G-4(빈 상태) 미수정, G-6(응답 형태)·G-7(422)·G-9(Design 내부 모순)는 **문서 정정 대상**으로 분류 · 이터레이션 2 · SC 5/7 완전+1 부분+1 미달 · FR 15/15 · Decision Record 12/12) | 신규 131(도메인 정책 31 · UseCase·실행기·소유권·상세 33 · 저장소 통합(SQLite 실DB) 13 · L1 API TestClient 19 · 모델 오버라이드 회귀 13 · 프론트 22) · **회귀 0건**(git stash 후 FAILED 목록 comm 대조 ×4회, 백 58 / 프론트 9는 전부 baseline) · tsc 0 errors · ESLint 0 · 백 신규 11 수정 17 / 프론트 신규 5 수정 4 / 마이그레이션 2(V067·V068) / 의존성 ragas([eval] extra 선언만) — **동일 에이전트를 모델 N개로 순차 실행해 품질·비용·지연·도구F1 4축을 모델×지표 매트릭스로 비교**. Option C — evaluation_sweep 부모 테이블 + 얇은 도메인 + 기존 BatchEvalExecutor 재사용. ★ **Plan 조사가 기능의 전제를 뒤집음** — `target_executor._run_agent()`가 `config.llm_model`을 **무시**하고 에이전트 자신의 모델로 실행 중이었다(`llm_model`은 rag 대상 답변생성 전용). 못 찾았으면 '모델 파라미터 넘기면 되겠지'로 설계해 무효 구현. ★ **judge 주입도 죽은 코드였다** — `RagasEvaluatorAdapter._llm_model`은 대입만 되고 어디서도 안 읽혀 RAGAS 자체 기본 judge가 채점 중. `_attach_judge()`로 LangchainLLMWrapper를 metric에 실제 주입해야 했다(Design이 상정한 '인자 전달'보다 큰 작업). ★ **`temperature_override=0.0`은 falsy** — `or`로 쓰면 에이전트 값(0.70)으로 새어 재현성 NFR이 조용히 깨진다. `is not None` 판정 + 전용 테스트로 고정. ★ `_begin_observability`를 안 고치면 `ai_run.llm_model_id`에 **원본 모델**이 찍혀 스윕 비용·토큰이 통째로 엉뚱한 모델에 귀속(FR-02). ★ **ORM에 교차 모듈 ForeignKey 금지** — DDL엔 FK가 있어도 `ai_run.llm_model_id`(agent_run.py:25)는 plain 컬럼. 넣었더니 부분 임포트 create_all이 NoReferencedTableError로 깨짐(픽스처 10여 곳). ★ **D8을 대시보드에만 적용하고 사용자 목록을 놓쳐 QA에서 G-11 적발** — 모델 5개 스윕 1회 = '평가 실행' 목록에 5행 추가. 같은 원칙이 적용될 지점을 설계 시점에 열거하지 않은 탓. 제외가 곧 **기능 도입 이전 의미의 복원**이라 무조건 필터가 정답(숨겨지는 기존 데이터 없음). ★ `_save_assistant_message`는 정상+강등 2경로에서 호출 → persist 게이트를 호출부가 아닌 **메서드 내부**에 둬야 한 쪽 누락이 없다. ★ **agent 대상 허용 메트릭은 3개뿐** (`TARGET_METRICS['agent']` = answer_relevancy/correctness/similarity) — Design §4.2 예시가 쓴 faithfulness·context_*는 실제로는 422. 설계가 정책 코드를 안 열어본 실수, 프론트 SWEEP_AVAILABLE_METRICS로 원천 차단. ★ `0.0`(측정된 0점)과 `None`(측정 불가)을 타입 수준 분리 — `mean_ignoring_none` + '—' 렌더를 테스트로 고정, 섞였으면 매트릭스가 의사결정을 오도. ★ ragas 설치는 **openai 3.3.1 → 2.54.0 메이저 다운그레이드** 유발(langchain-openai 허용범위 `>=2.45,<4` 안이긴 함) → base 아닌 `[eval]` extra로 분리, 로컬 .venv 무변경. 함정: `kickoff`은 fire-and-forget이라 순차 오케스트레이션에 못 씀 (`run_now` 공개 진입점 신설) · `_run_one_model` 자체 guard 없으면 한 모델 실패가 뒤 모델을 통째로 건너뜀 · expected_tools는 `evaluation_testset.cases`가 JSON이라 무DDL이지만 `TestCase` VO에 필드가 없어 매핑 시 유실 · docstring 몇 줄로 `_save_assistant_message`가 35→41줄 되어 40줄 규칙 위반(줄여서 원복) · 프론트 '모델0'이 체크박스 라벨과 judge option 양쪽에 있어 쿼리 모호(within으로 범위 축소). 미검증(환경 제약): 품질 축 RAGAS 미실행(ragas 미설치) · 재현성 NFR ≤5%p(L3-2 미실행) · 비용추정 ±30%(측정 수단은 G-1로 확보) · 종단 실행 0회(서버 미기동·Playwright 미설치) | [agent-model-benchmark/](./agent-model-benchmark/) |
| fix-mcp-tool-call-not-reaching-server | 2026-09-02 | 96.4%(Check 84.4% → Act-1 96.4% · Structural 100% / Functional 91% / Contract 100% · 정적 전용 공식(에이전트 E2E 미실행) · Critical 1(G-01) 해소 · Important 1(G-02)·Minor 1(G-03) 해소 · 미해결 0 · 이터레이션 1 · SC 4/5 완전+1 부분(SC-01 실서버 call_tool 수신은 사용자 실행 대기) · FR 7/7(FR-04는 전체 바인딩으로 대체)) | 신규 26(policy build_tool_name 8 · factory 도구선택·계측·배선예외 17 · compiler 실패격리 4 · adapter 실행로그 4 · registry 로드로그 2 · DI 배선계약 6, 중복 제외) · **회귀 0건**(전체 스위트 3회 측정 8570→8578→8583 passed, failed 58건·파일분포 불변) · 백 신규 1(domain/mcp/exceptions.py 17줄) 수정 5 / 테스트 신규 1 수정 4 / 마이그레이션 0 / 의존성 0 — **에이전트가 MCP 도구를 호출해도 서버에 요청이 도달하지 않던 문제**. Option C — 규칙은 domain policy에, 배선은 기존 구조 유지, 신규 프로덕션 모듈 최소화. ★ **계측을 수정의 선행 게이트로 둔 것이 이번 사이클의 전부였다** — 코드 리딩으로 결함 후보 3개(D1 레거시 첫-도구 폴백 / D2 OpenAI 64자 상한 / D3 DI 배선 회귀)를 뽑았으나 증상 설명력이 갈렸다. 계측 없이 착수했으면 D2를 고치고 '증상 그대로'를 맞았을 것 — **실측 결과 D2는 이 서버에서 미발생**(최장 노출명 59자, 상한 64). ★ **DB의 실제 워커 정의를 열어본 것이 결정타** — `agent_tool` 행의 description은 `scrape_url`/`scrape_urls`/`extract_structured` 3개를 안내하는데 `is_server_level` → `return tools[0]`으로 **`scrape_url` 1개만 바인딩**. 나머지 2개는 LLM 도구 목록에 아예 없어 호출이 성립하지 않고 MCP 서버로 요청이 나가지 않는다 — 증상과 완전 일치(D1 확정). 코드만 봤으면 '첫-도구 폴백은 의도된 설계'로 넘어갔다. ★ **실측이 Design 결정을 뒤집었다** — §6.1 E6 '첫-도구 폴백 유지 + 경고'는 경고 로그를 남길 뿐 사용자 증상을 해결하지 못한다. `create_all_async` 신설로 서버 단위 참조는 **도구 전체**를 워커에 바인딩(`create_agent(tools=worker_tools)`), 사용자 승인 후 Design v0.2 §6.3에 근거와 함께 기록. `create_async`(단수)는 미들웨어 에이전트 경로 하위호환으로 유지. ★ **단순 절단이 이번 증상을 재생산한다** — 접두부 `mcp_{uuid}_`가 41자 고정이라 도구명이 길면 앞 64자가 전부 같아져 서로 다른 도구가 같은 이름으로 잘리고 LangChain 도구 목록에서 하나가 가려진다. `build_tool_name` = `[:59] + '_' + sha1(원본)[:4]`로 결정적 충돌 회피(현 서버명은 59자 이하라 **불변** → `ai_tool_call.tool_name` 관측 기록 연속성 유지). ★ **Gap 분석이 내 구현의 설계 위반을 잡았다(G-01)** — Design §6.2가 '배선 오류(E1/E2)는 격리하지 **않는다**'고 명시했는데 `except Exception` 한 줄이 그걸 삼켰다. MCP 1개 + 일반 1개 조합에서 배선이 사라지면 MCP 워커만 조용히 제거되고 그래프는 정상 컴파일 — **이 사이클이 없애려던 실패 모드를 그대로 재생산**. U14 전체실패 가드는 워커가 전부 MCP일 때만 작동. `McpWiringError(ValueError)` 신설 + compiler `except McpWiringError: raise`로 해소. **Design의 부정형 조항('~하지 않는다')은 긍정형 요구보다 놓치기 쉽다 — Do 시작 시 따로 뽑아 체크리스트화할 것.** ★ **테스트가 Design보다 느슨했다(G-02)** — `pytest.raises(match='MCP tool not found')`는 부분 일치라 §8.2 U7의 '메시지에 실제 도구명 목록 포함' 요구를 통과시켰다. `assert x in str(exc.value)`로 내용까지 단언해야 한다. 에러 메시지에 `(available: [...])` 추가. ★ **뮤테이션으로 가드 실효성 검증** — main.py의 MCP 배선 2줄을 지워 3건이 실패하는 것을 확인한 뒤에야 '회귀를 잡는다'고 말했다(Gap 수정 후 재검증 포함 2회). ★ **회귀 판정 범위를 명시하지 않고 '회귀 없음'이라 말한 실수** — 타겟 스위트(5,525) 기준이었는데 전체 스위트엔 기존 실패 58건이 있었다. 전/후 비교로 무관함을 확정했으나 baseline을 먼저 잡았어야 했다. 함정: `excel_export`는 `_create_excel_generator_node` 전용 분기라 U13의 '정상 워커'로 못 씀(create_agent 미호출 → `python_code_executor`로 교체) · 미들웨어 팩토리는 MCP 미지원이라 가정한 테스트가 실패 — 실제로는 main.py:4642에 이미 배선돼 있었고 그 테스트가 실서버로 나가고 있었다(속성 검사로 교체) · `create_session`은 tool_registry(①)엔 request_id를 넘기나 tool_adapter(③)엔 안 넘겨 로그 체인이 끊김(G-03) · 작업트리의 tool_factory/workflow_compiler/main.py가 세션 중 수정본↔HEAD를 두 차례 오감(원인 미규명, Design §11.2에 브랜치 분리를 선행 필수로 명시). 미이관: `WorkflowCompiler.compile` 455줄(변경 전 ~427줄 **기존 위반**, 이번에 28줄 추가) · **MCP 도구별 inputSchema 노출(다음 사이클 1순위)** — 모든 MCP 도구가 공통 `{arguments: dict}` 스키마라 요청 '도달'은 해결됐어도 LLM이 인수를 잘못 담을 여지가 남음 · api 라우터 기존 실패 24건(`AssembleAuthContextUseCase not initialized`, MCP 범위 밖 별개 DI 이슈) · SC-01 종단 확인(에이전트 실행 후 Scrap MCP `call_tool` 수신 육안 확인) | [fix-mcp-tool-call-not-reaching-server/](./fix-mcp-tool-call-not-reaching-server/) |
| worker-context-injection | 2026-09-03 | 100%(Check 94.5% → Act-1 95.1% → Act-2 100% · Structural/Functional/Contract/Runtime 각 100% · 런타임 포함 공식 · Critical 0 · Important 2(GAP-01 function 노드 미주입 · GAP-02 FR-09 run step 관측 미구현)·Minor 1(GAP-03 L3 #3 테스트 누락) 전부 해소 · 미해결 0 · 이터레이션 2 · SC 5/5 · FR 10/10 · Decision Record 9/9) | 신규 87(ToolArgumentPolicy 판정·차단문구·is_blocked_message 35 · 어댑터 가드·차단로그·루프방지 8 · 컨텍스트 블록 렌더·절단·norm토글 15 · 컴파일러 주입·순서·역할분리 7 · supervisor task·상태초기화 3경로·wrap_worker append 16 · 생성노드 4종 배선 6) · **회귀 0건**(전체 8670 passed / 58 failed — git stash baseline과 `comm -13` 공집합, 4회 대조) · 백 신규 2(domain/mcp/tool_argument_policy.py + 테스트 6파일) 수정 11 / 프론트 0 / 마이그레이션 0 / 의존성 0 — **커스텀 에이전트의 MCP 스크래핑 워커가 컨텍스트를 잃고 `https://www.example.com/financial-market-2026-09-03` 같은 실재하지 않는 URL을 서버로 보내던 문제**. Option C — domain 정책 분리 + 기존 prompt_rendering 재사용. ★ **유실이 3겹이 아니라 4겹이었다** — Plan은 ①에이전트 프롬프트(supervisor 노드에만) ②워커 역할(라우팅 목록 문자열로만) ③작업 지시(SupervisorDecision에 필드 없음) 3축을 특정했는데, Design에서 함수 계약을 적다가 **④ `ensure_user_tail`이 tail=user면 no-op**(message_normalization.py:35-37)이라 첫 워커 호출(끝이 사용자 질문)에서 기존 범용 지시문 `"당신의 역할에 해당하는 작업을 수행하세요"`조차 **한 번도 전달된 적이 없었음**을 발견. task를 `instruction=` 인자로만 넘겼다면 이번 수정도 동일하게 무력화됐다 → 무조건 append(`_build_worker_input`) + ensure_user_tail은 prefill 방어만, `test_appends_task_when_tail_is_user_message`로 고정. **설계 문서에 계약을 적어보는 행위 자체가 결함 탐지 수단이다.** ★ **TDD가 '깨진 테스트 = 버그'가 아님을 드러냄** — `test_generic_worker_without_tz_has_no_system_prompt` 등 3건은 회귀가 아니라 FR-02가 **의도적으로 바꾼 계약**(워커에 system_prompt 미전달 → 항상 전달). 각 테스트에 변경 근거를 주석으로 남기고 새 계약으로 갱신. ★ **Gap 분석이 스스로 오판했고 Act가 정정** — `analysis` 노드를 '블록 미주입'으로 적었으나 실제로는 이미 `workflow.supervisor_prompt`를 받고 있었고(`_create_analysis_node(llm, worker_id, supervisor_prompt)`) **역할만** 빠져 있었다. 분기 순서(`continue` 위치)만 보고 각 팩토리 시그니처를 확인하지 않은 탓 — **"이 노드가 X를 받는가"는 호출부 인자를 직접 봐야 한다.** ★ **레이어를 지키면서 관측을 확보(FR-09)** — `MCPToolAdapter`는 infrastructure라 application의 `track_step`을 부를 수 없다. domain 상수 `BLOCKED_PREFIX`를 접점으로 삼아 [infra 차단문자열 반환 → ToolMessage로 react 트레이스 → app `_blocked_step_summary()`가 `is_blocked_message()`로 식별 → `STEP_OUTPUT_SUMMARY_KEY` → 기존 `_wrap_step`의 `result.pop()`이 소비] 경로 구성. **infrastructure→application 참조 0건, 신규 플럼빙 0개.** ★ **생성 노드 4종은 프롬프트 훅 자체가 없었다** — LLM 호출이 컴파일러가 아니라 주입된 생성기(`composer.compose`/`generator.generate`) 내부에서 일어나 지시를 실을 자리가 없음. 각 **공개 메서드에 `worker_context_block: str = ""` 키워드 추가**(기본값으로 미배선 호출부 무영향, 도메인 포트 무변경)로 해결. presentation만 예외 — 프롬프트가 `SlidePlannerPort`(도메인 포트) 구현체 안이라 포트를 건드리면 어댑터·테스트까지 파급 → use case가 `instruction` 앞단에 붙이는 방식 채택. ★ **search가 숨은 동일 위험이었다** — rewrite/plan LLM이 **실제 외부 검색어를 작성**하는데 `context_block = datetime + user`뿐이라 에이전트 맥락이 없었다. `worker_context_block` 파라미터 신설(`search_pipeline.py` + `deep_search/workflow.py`, AD-1 시그니처 동형 계약 유지). ★ **의도적 스펙 축소 1건** — Plan FR-07이 열거한 `localhost`/`127.0.0.1`을 차단 목록에서 **제외**. 사내 로컬 대상 스크래핑 오탐 위험 > 실익이고, 차단은 MCP 서버 접속 주소가 아닌 **도구 인자의 URL 값**에만 적용한다는 원칙과도 맞지 않음. Design §3.2에 근거 + `test_returns_false_for_localhost`로 계약 고정. 호스트 매칭은 정확·서브도메인 일치만(`myexample.com`·`example.com.evil.co.kr` 오탐 방지). 함정: `analysis`·생성 노드는 도구가 없어 `[도구 사용 규범]`이 무관한 노이즈 → `include_tool_norm=False` 토글 신설 · `excel_export`는 function 노드라 `create_agent` 미호출이라 '두 번째 react 워커' 테스트 대역으로 못 씀(`mcp:srv:summarize`로 교체) · MCP 워커는 `create_all_async`(비동기) 경로라 테스트 대역이 동기 MagicMock이면 워커가 통째로 `failed_worker_ids`로 빠짐 · `wrapped`가 43줄이 되어 40줄 규칙 위반 → `_build_worker_input` 모듈 함수로 분리 · `worker_task`는 상태에 남으므로 **3경로(워커 라우팅/FINISH·무효/강제 라우팅)에서 명시적 초기화** 필요 — 안 하면 다음 턴 워커가 낡은 지시 수신. **사고 2건**: ① baseline 대조를 위해 `git stash` 후 두 번째 전체 스위트(≈6분)가 10분 타임아웃에 걸려 `stash pop` 미실행 → **작업분이 stash에 갇힘**(즉시 복구, 이후 비교 범위를 실패 파일 10개로 축소). **stash 상태에서 장시간 명령 금지.** ② 문자열 치환 스크립트가 `

` 이스케이프 문제로 조용히 실패해 `_plan_sheets` 시그니처 미적용 → excel 테스트 14건 붕괴. 일부 치환에만 `assert count == 1`을 붙인 것이 원인 — **예외 없이 적용할 것.** 미이관: **O-2 MCP 서버 도구 스키마 검증(다음 사이클 1순위)** — `MCPToolAdapter.args_schema = MCPToolInput`이 **모든 MCP 도구에 공통 `{arguments: dict}`**라 LLM은 도구별 `inputSchema`를 못 본다(이전 사이클 fix-mcp-tool-call-not-reaching-server가 이미 1순위로 남긴 항목과 동일 근원). `call_client.py:48`이 `inputSchema`를 읽고는 있으나 어댑터로 전달되지 않음. "URL 결정 주체 = MCP 서버"라는 이번 Plan의 전제가 여기서 미검증 상태 · O-1 워커가 `user_context_block` 미수신(주입값이 `workflow.supervisor_prompt`(:461)이고 supervisor가 받는 `effective_supervisor_prompt`(:312)가 아님 — '내 부서 기준으로' 류 요청에서 필요) · O-3 `MAX_AGENT_PROMPT_CHARS=2000` 실측 미조정 · 사전 존재 실패 58건(parser/retriever/elasticsearch/api) 중 `test_run_agent_use_case_observability.py` 5건은 **단독 통과·전체 실패**하는 테스트 격리 문제 — 회귀 판정 신뢰도를 흐리므로 별개 이슈 권장 | [worker-context-injection/](./worker-context-injection/) |
| mcp-tool-category-routing | 2026-09-03 | 98.0%(Check 94.4% → Act 98.0% · Structural 100% / Functional 95% / Contract 100% · 정적 전용 공식(실 MCP 계측은 했으나 스킬 정의 L1(HTTP)·L2/L3(Playwright) 미실행이라 보수적 적용) · Critical 0 · Important 3(Gap-01 react 상한 관측 불가 · Gap-02 함수 40줄 위반 · Gap-03 SUCCESS 실측 부재) 전부 해소 · Minor 2(Gap-04 §2.2 다이어그램 표기 · Gap-05 예외 계약 미기재) 미이관 · 이터레이션 1 · SC 8/8 · FR 13/14 완전+1 축소 · Decision Record 11/11) | 신규 194(ToolCategoryPolicy 28 · ToolCallBudgetPolicy 19 · collect_pipeline 31(실패 6분기·규약 5케이스·40줄 AST) · WorkerRunCapHooks 10 · compiler 카테고리·상한·wiki예외 19 · 도구호출 관측 11 · update_metadata UC 13 · 라우터 14 · sync 보존 3 · repository/adapter/registry 확장 · 프론트 AdminToolsPage 8) · **회귀 0건**(백 8670→8823 passed / 58 failed 불변 — git stash baseline과 `comm` 양방향 공집합, **4회 대조**; 프론트 9 failed도 stash 대조로 baseline 확인) · tsc exit 0 · 백 신규 5(collect_pipeline·worker_run_cap_hooks·update_metadata_use_case·session_scoped·V069, 프로덕션 653줄) 수정 12 / 프론트 수정 6 / 마이그레이션 1(V069) / 의존성 0 — **MCP 도구가 전부 `"action"`으로 떨어져 react 루프를 타는 바람에 스크랩 1회 요청에 도구가 4~5회 호출되고, 산출이 '수집한 근거'가 아니라 워커 LLM의 분석문으로 나오던 문제**. Option C — collect 전용 노드 신설, generator 하드코딩 분기 무수정. ★ **직전 두 사이클이 연속으로 '다음 사이클 1순위'로 남긴 항목을 해소** — `fix-mcp-tool-call-not-reaching-server`의 'MCP 도구별 inputSchema 노출'과 `worker-context-injection`의 'O-2 도구 스키마 검증'이 같은 근원이었고, `MCPToolAdapter.args_schema = MCPToolInput`(모든 MCP 도구 공통 `{arguments: dict}`)이 collect 인자 생성의 전제를 무너뜨려 **이번엔 우회 불가**였다. `mcp_input_schema` 필드 신설 + `tool_registry`에서 `inputSchema` 전달(D-14)로 해결, **실서버 3개 도구 스키마 전달 확인**. ★ **설계 §2.2가 `args_schema`를 쓴다고 적었는데 실제로는 제네릭 래퍼였다** — Design이 인프라 코드를 열어보지 않은 실수(이전 사이클 `err.response.status` 함정과 동형). Do module-2에서 발견해 사용자 승인 후 범위 확대. ★ **기존 테스트가 내 구현의 진짜 결함을 잡았다** — `inputSchema`를 `or {}`로만 처리했더니 MagicMock 픽스처가 dict가 아닌 값을 흘려보내 pydantic 검증 실패, **해당 서버의 도구 로딩 전체가 무너짐**(§6.2 격리 철학 정면 위배). `_as_schema_dict()` 정규화로 '스키마 미상'으로 낮춤 — 비정형 응답을 주는 실서버에서 같은 사고가 났을 것. ★ **FR-11 ↔ FR-14 충돌을 Do 중에 발견** — `tavily_search`·`internal_document_search`는 `TOOL_REGISTRY`가 이미 `category="search"`라 FR-11(search+collect 런당 1회)을 그대로 구현하면 **관리자가 아무것도 지정하지 않아도 기존 검색 에이전트 동작이 바뀐다**. 사용자 결정으로 collect 한정 축소(D-12), Plan/Design 양쪽 FR-11 개정. ★ **엔드포인트 설계가 기존 관례와 충돌** — §4.1의 `PATCH /{tool_id}/metadata`는 `mcp:{uuid}:{tool}`의 콜론 때문에 path 세그먼트로 부적합. 기존 `PATCH /builtin`이 **정확히 같은 이유로 이미 body 방식**(`tool_catalog_router.py:47` 주석)이라 body로 통일(D-13). ★ **실측이 운영 DB의 시한폭탄을 찾아냈다(Gap-06)** — 스크랩 도구 3개에 `category='search'`가 설정돼 있었다. 이 사이클 전에는 `_resolve_category`가 카탈로그를 안 읽어 **무해**했으나, 변경으로 활성화되면 search 노드가 `{"query":…}`를 보내고 실서버 required는 `url`/`urls`라 **호출 자체가 성립하지 않는다**. 사용자 승인 후 `UpdateToolMetadataUseCase` 경로(도메인 검증 경유)로 `collect` 변경. **Plan 단계에 운영 데이터를 한 번만 조회했어도 발견됐을 문제.** ★ **Gap-02에서 측정 도구 자체가 틀렸다** — 함수 길이 AST 측정이 중첩 함수(`collect_node`)를 이중 계산해 노드 팩토리를 항상 위반으로 잡았다(기존 `create_search_pipeline_node`도 같은 이유로 49). 중첩 제외로 교정하고 **같은 잣대를 기존 `search_pipeline.py`에도 적용해 통과함을 테스트로 고정** (`test_measurement_matches_existing_peer_module`) — 새 기준을 신규 코드에만 자의적으로 들이대지 않는다는 근거. ★ **Gap-03 실측**: 실 `Scrap MCP`(localhost:8002)에 `MCPToolAdapter._arun` 후킹 — 플레이스홀더 URL **0회 차단** / 실제 URL **정확히 1회**, 인자 `{'url': 'https://www.python.org/'}`, 산출은 **도구 원본 JSON**(2705자, title/text)으로 LLM 종합문이 아님을 실증. ★ **바퀴 재발명 회피** — 호출 상한은 langchain 내장 `ToolCallLimitMiddleware(run_limit, exit_behavior='continue')`, 재라우팅 억제는 기존 `SupervisorHooks.skip_workers` 확장점(`visualization_done` 선례) 재사용 → supervisor 코어 수정 0. ★ **부분 갱신 의미론을 3계층에서 일관 구분** — `category: null`(미분류로 되돌리기)과 필드 생략(변경 없음)을 프론트 payload 선택구성 / 라우터 `model_fields_set` / UC·repo `UNSET` 센티널로 각각 고정, 계층마다 테스트 보유. 함정: `MCPToolAdapter`는 `{"arguments": {...}}` 래핑이 필요하고 내부 도구는 평평 → `_build_payload`가 `mcp_tool_name` 유무로 분기 · 카탈로그는 내부 도구를 `internal:{id}`로, `agent_tool`은 `{id}`로 저장(`_normalize_tool_id:499`)해 `_catalog_key`로 키 정규화 안 하면 내부 도구가 영영 조회 안 됨 · `upsert_by_tool_id` 보존은 코드를 '추가하지 않음'으로 성립해 SQL 컴파일 파라미터 테스트로만 계약화 가능 · 기존 `test_workflow_compiler_middleware.py` 2건이 미들웨어 리스트 길이를 정확히 검증해 깨짐 — 느슨하게 고치지 않고 새 계약을 명시적으로 재작성(+`id()` 집합으로 워커 간 인스턴스 비공유까지 추가 검증) · 미존재를 `ValueError`로 두면 400/404를 못 가려 `LookupError`로 분리(`test_lookup_error_is_not_value_error`가 전제 고정) · `worker_run_cap_hooks.py`의 `ToolCallLimitMiddleware` 언급은 주석뿐(D-11 격리 유지). **사고**: gap-detector 서브에이전트가 **30턴 한도에서 본문 없이(`대기 중.`) 종료, 173k 토큰 소모** — 독립 검증이 사라져 Check가 구현자 자기검증이 됐고 자기확증 편향을 Analysis §0에 한계로 명시. 판정 근거를 전부 grep/AST/테스트 실행 결과 등 **기계적 재확인 가능 형태**로 제시해 보정. **서브에이전트 출력은 파일로 받도록 지시할 것**(이전 사이클 교훈 재발). 미이관: **Gap-07 레거시 서버 단위 워커(다음 사이클 1순위)** — 실제 운영 에이전트가 `agent_tool.tool_id='mcp_081c6fe7-…'`(서버 단위)라 카탈로그 키와 매칭되지 않아 **react 경로 유지 → collect 혜택 없음**(D-04상 서버 단위엔 collect 지정 불가). react 상한으로 4~5회→최대 2회는 되지만 **'분석까지 해버린다'는 증상은 이 경로에서 그대로 남는다**. 사용자 결정으로 현 상태 수용 · `generate` 카테고리 + `compile()` tool_id 하드코딩 분기 4종 리팩토링(Design Option B, Plan Out of Scope) · MCP sync 시 카테고리 자동 추론 · 실 LLM 기반 collect 인자 산출 검증(실측 시 LLM은 스텁) · Gap-04(§2.2 다이어그램이 아직 `args_schema` 표기)·Gap-05(예외 타입 계약 §4.2 미기재) · 사전 존재 실패 백 58건·프론트 9건(`test_run_agent_use_case_observability.py` 5건 단독통과·전체실패 격리 문제는 직전 사이클에서도 지적됨) | [mcp-tool-category-routing/](./mcp-tool-category-routing/) |
| jobs-page-revamp | 2026-09-04 | 96.5%(Check 94.7% → Act 96.5% · Structural 98% / Functional 96→99% / Contract 95→99% / Runtime 92% · 런타임 포함 공식 · Critical 0 · Important 2(I1 `GET /api/v1/schedules` 설계문서 미기재 → Design v0.2 사후 명세 · I2 FR-13 새로고침이 벨 배지 미갱신 → `invalidateUnseenCount` 추가) 둘 다 Act 즉시 해소 · Minor 8 중 M1(TOCTOU)·M4(죽은 상수) 해소, 6건 문서화 · 이터레이션 1 · SC 7/8(SC-7만 부분 — 저장소 선행 타입에러, 본 기능 파일은 0) · FR 16/16 · UI 체크리스트 17.25/18) | 백엔드 150 passed(신규 test_period 8 · test_delete_use_cases 8 · test_list_my_schedules 3 · soft-delete 필터 파라미터화 7 · UNION 조립 8 · TOCTOU 회귀 1) / 프론트 42 passed(JobsPage 27 · NotificationBell 11 · ChatPage bg 4) · ESLint 0 · `vite build` 성공 · 런타임 L1 인가 6/6 401 + OpenAPI 9경로 파라미터 일치 + L2 브라우저(렌더·탭전환·빈상태·콘솔에러 0) · **회귀 0건**(백 58 failed·프론트 9 failed는 HEAD worktree/git stash baseline과 대조해 선행 실패 확인) · 백 신규 4(V070 마이그레이션 · period.py · delete_use_cases.py · list_my_schedules_use_case.py) 수정 8 / 프론트 신규 6(JobFilterBar · JobHistoryTable · JobPagination · StatusBadge · ScheduleDefinitionTable + 테스트) 수정 8 / 마이그레이션 1(V070, 적용 확인) — **작업함을 `agent_background_job` ∪ `agent_schedule_run` 통합 이력 테이블로 개편하고 상태·유형·기간 3단 필터 + `{items,total}` 페이지네이션 + `deleted_at` 소프트 삭제(행 삭제·완료 일괄 정리) 제공**. Option C — 기존 모듈 내 `union_all`. ★ Option A(프론트 병합)를 택했으면 두 목록의 offset이 어긋나 20건 초과 시 항목 누락·중복이 확정 — `total` 합산도 불가해 '1/1 페이지' 표시 자체가 성립하지 않는다. Option B(도메인 모듈 신설)는 이력 소스가 2개뿐인 시점에 과잉 추상화(CLAUDE.md §6). 정렬은 `occurred_at DESC, id ASC` — 보조 키 없으면 동시각 항목이 페이지 경계에서 흔들림. ★ **설계 §10.2 '메서드 전수 점검표'가 이번 사이클 최고 산출물** — 프로젝트 첫 soft delete라 필터 필요 7개와 **의도적 제외 3개(워커 경로 claim_queued/finish/reconcile)를 근거와 함께** 표로 못 박아 감사 100%. `_active()` 헬퍼로 필터를 구조적으로 강제하고 파라미터화 테스트로 고정. ★ **Impact Analysis 누락이 앱 전체를 죽임** — Plan §6.2에 `GET /jobs` 소비처를 2곳으로 적었으나 실제 3곳, `ChatPage`의 `useJobList` import가 남아 `MISSING_EXPORT`로 화이트스크린. grep을 `head`로 자른 채 문서화한 것이 원인이고 **사용자가 발견**. ★ **검증 명령이 아무것도 검증하지 않았음** — 루트 `tsconfig.json`이 `"files": []` + project references라 `tsc --noEmit`은 0개 파일 검사. 3세션 동안 근거 없는 '타입 에러 0' 보고. 올바른 명령은 `tsc -b`이며 `vite build` 1회면 즉시 잡혔을 사고(단위 테스트는 훅을 모킹해 구조적으로 못 잡음). ★ KST 경계는 Application(`period.py`)에서 UTC naive로 환산 — UTC 날짜와 KST 날짜가 갈리는 시각·월요일 00:30 경계값 테스트로 고정. ★ module-5에서 스케줄 CRUD가 에이전트별(`/agents/{id}/schedules`)이라 가로지르는 조회 불가 → `GET /api/v1/schedules` 신설 (`/schedule-runs` 선례 따라 background_job_router 배치). ★ NotificationBell은 `type=manual` 고정 — `count_unseen`이 수동 job만 세므로 통합 목록을 그대로 쓰면 배지 숫자와 드롭다운이 어긋남. ★ 이월: 인증 토큰 미확보 + 테이블 0행으로 **L1 데이터 경로(필터·페이징·삭제 실제 응답) 미검증**, 성능 p95 미측정 | docs/archive/2026-09/jobs-page-revamp/ |
| agent-update-tool-editing | 2026-09-06 | 98%(Check 1회 · 이터레이션 0 · Structural 100% / Functional 95% / Contract 100% · Runtime 미포함(정적 전용 공식) · Critical 0 · Minor 2(G-1 설계 §6.1 MCP 미배선 전용 에러 메시지 미구현 → `Unknown tool_id` 폴백, 422 매핑·안전성은 충족 / G-2 설계 §2.2 순서표와 코드 불일치 — 코드가 정책 검증을 종속정리보다 **앞**에 둬 fail-fast, 문서를 코드에 맞춰 갱신 권장) · Info 1(G-3 수정 경로에 exclude_builtin_tool_ids 채널이 없어 생성 시 제외했던 빌트인이 도구 편집 저장 시 재주입 — 승인된 의미론) · SC 7/7 · FR 13/13) | 신규 59(백엔드 48: 워커빌더 17 · update 도구편집 22(T-01~T-18+α) · 스키마계약 5 · 도메인 replace_tool_workers 4 / 프론트 11: agentToolPayload 5 · AgentBuilderPage 6(F-01~F-04)) · 영향권 1056 passed(application/domain/infrastructure agent_builder + router) · 프론트 전체 1197 passed · tsc 신규파일 에러 0 · **회귀 0건**(전체 스위트 58 failed·프론트 9 failed는 모두 사전 존재 — `git stash push src/api/main.py` 로 커밋 원본 대조 시 오히려 10 failed로 더 나빴고, 실패 파일들이 agentBuilder/agentToolPayload 를 import 하지 않음을 확인) · **실서버 L1 6/6**(임시 인스턴스 :8010 · 원 결함 요청이 **422 → HTTP 200** · 워커+tool_config 영속 · 빌트인 재주입 · flow_hint 갱신 · 도구 제거 · tool_configs 단독 422 · tool_ids 미전송 무변경 — 임시 에이전트 DELETE 204 정리) · 백 신규 5(worker_skeleton_builder.py 223줄 + 테스트 4) 수정 6 / 프론트 신규 2 수정 3 / **마이그레이션 0** / 의존성 0 — **수정(PATCH) API 에 tool_ids·tool_configs 를 도입해 도구 워커를 목표 상태로 재구성, 그 뒤 문서/발표자료 바인딩이 새 워커에 설정을 주입**. Option C — 생성 경로의 빌드 규칙을 worker_skeleton_builder 로 **행위보존 추출**해 create/update 가 정규화·MCP 해석·빌트인 주입·worker_id 생성을 한 벌로 공유(create -211줄). ★ Option A(update 안에 재구현)를 택했으면 위키가 이미 경고한 함정 —`applyToolsToForm` 주석의 "두 벌로 구현하면 한쪽만 고쳐져 조용히 어긋난다" — 을 그대로 재현. ★ 원 결함은 발표자료 422 였지만 **진짜 범위는 도구 편집 전체 부재**였고(UpdateAgentRequest 에 tool_ids 필드 자체가 없음), 발표자료만 후속 검증이 있어 에러로 드러났을 뿐 다른 모든 도구 변경은 저장 성공 메시지와 함께 조용히 유실되고 있었다. ★ 의미론 매트릭스에서 `tool_ids=None + tool_configs=값 → 422` 를 명시 거부로 못 박음 — 도구 목록 없이 설정만 오는 요청이 정확히 이번 버그의 형태라 조용히 무시하면 재발한다. ★ clamp 정책 2분기 — 명시적 visibility 요청은 위반 시 422 거부(기존 계약), 도구 변경으로 인한 부수적 축소는 자동 clamp + 응답 통지(visibility_clamped). 전자를 clamp 로 바꾸면 "공개로 바꿔달라"가 조용히 무시되고, 후자를 거부로 하면 도구 하나 붙이려다 저장이 막힌다. ★ **구현 중 발견해 함께 고친 결함 4건** — ① repository.update() 가 flow_hint 미저장(위키 "repo 누락 시 조용히 미저장" 함정의 재현 — 도구 바뀌어도 supervisor 프롬프트가 옛 도구 체인 참조) ② worker_id 생성이 3곳 중 1곳만 sanitize_tool_name 경유(MCP 콜론 ID 시 LangGraph 노드명 파손 위험) ③ 빌트인이 MAX_TOOLS 상한 잠식(edit 폼이 빌트인을 일반 칩으로 매핑 → 설계 §5.1 의 프리필 제외 대신 **전송 시점 필터** buildToolIdsForSave 로 대체, edit 칩 UI 보존) ④ clamp 결과 department 인데 department_id 부재 시 도메인 불변식 위반 조합 → private 까지 하향. ★ **QA 중 stale 프로세스 오진** — `--reload` 인데도 사용자 dev 서버(8000) 워커가 16:08:47 코드에 고정돼 1차 L1 에서 빌트인 소실·flow_hint 정체라는 **거짓 결함**을 관측, 새 프로세스(:8010)에서 재실행하자 둘 다 정상 → 배포 시 uvicorn 명시적 재기동 필요. ★ **gap-detector 2회 산출 실패**(본문 유실 → 파일 미생성)로 Check 단계 독립 검증 미확보, 자기검증으로 대체하고 Analysis §0 에 명시 고지 | docs/archive/2026-09/agent-update-tool-editing/ |
