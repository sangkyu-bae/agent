# Archive Index — 2026-06

> 이 디렉토리는 PDCA 사이클이 완료된 피처 문서를 보관합니다.

| 피처 | 완료일 | Match Rate | 테스트 | 경로 |
|------|--------|-----------|--------|------|
| self-hosted-qwen-endpoint | 2026-06-28 | 100% | 신규 20 + 회귀 40 + router 4 = 64 pass, tsc 0 error, 소스 8(+43/-8) + 마이그레이션 1(V035) + 테스트 4(신규 3+보강 1) + 프론트 2 files — 폐쇄망 GPU vLLM(OpenAI 호환) Qwen 연결 지원(LLM-MODEL-REG-002). 근본원인: `LLMFactory._create_openai`가 `ChatOpenAI`에 `base_url` 미전달 → 항상 api.openai.com만 호출, 엔티티/ORM/스키마에 엔드포인트 필드 부재. 해결: **신규 provider 없이** `provider="openai"` 재사용 + 모델별 `base_url`(VARCHAR 500, NULL ALTER) 한 축만 additive 추가. `_create_openai`/`_create_ollama`가 base_url 있으면 LangChain 클라이언트에 전달, `_resolve_api_key(allow_empty=bool(base_url))`로 인증 없는 vLLM은 `"EMPTY"` 더미키 통과. 엔드포인트는 환경변수 아닌 **DB 모델별 저장**(멀티 모델/서버 대응), api_key는 기존 `api_key_env` 참조 유지. Create/Update UseCase가 `request.base_url or None`로 빈문자열→None 정규화(self-host 해제). 풀스택 `/api-cotract`로 프론트 `LlmModel.base_url?` + MSW mock 동기화(등록 폼 UI는 읽기전용이라 후속). ★ "vLLM OpenAI 호환 → provider 분기/어댑터 0, base_url 한 칼럼만으로 폐쇄망 흡수", ★ "self-host 엔드포인트는 비밀 아니므로 평문 DB·인증키는 env 분리" 교훈. gap-detector 100%(미구현/회귀 갭 0). 프론트 등록 폼 base_url 입력 UI·폐쇄망 내부 실연결 검증(streaming usage_metadata R1/R4)·임베딩 self-host는 후속 | [→](./self-hosted-qwen-endpoint/) |
| fix-agent-builder-tool-selection | 2026-06-01 | 100% | 백엔드 30 + 프론트 7 pass (9 new), 6 files (+240/-5), 0 migrations — `/agent-builder` 생성 시 선택 도구 미전송 버그 풀스택 수정 (`CreateAgentRequest.tool_ids` 신설 + `_build_skeleton_from_tool_ids` 우선순위 분기 + 프론트 `form.tools`→`tool_ids` 전송 + MCP 생성단계 양방향 가드), ★ "state 정상이어도 ①요청 매핑 ②수신 필드 둘 다 확인" 교훈, MCP 생성-시 연결은 후속 과제 분리 | [→](./fix-agent-builder-tool-selection/) |
| fix-ws-auth-context-missing | 2026-06-03 | 100% | 신규 6 + 회귀 12 pass, 핵심 3 + 테스트 4 files, 0 migrations — WebSocket(`/ws/agent`, `/ws/chat`)에서 `verify_ws_token`이 `User`까지만 만들어 system prompt에 `[현재 사용자 정보]` 블록 누락 + 권한 Tool 무력화되던 버그 수정 (`WsAuthContextResolver` 신설 — 전용 단기 세션 `async with` + `session→UseCase` 빌더 주입으로 DDD 준수, 두 WS가 `auth_ctx` + `viewer_department_ids` 전달, 조립 실패 시 `public_anonymous()` fail-closed), ★ "transport별 인가 경로(HTTP/SSE/WS) 일관성 — 한 곳만 컨텍스트 조립 누락돼도 권한 격차 발생" 교훈, dev 서버 LangSmith trace 수동 검증은 후속 | [→](./fix-ws-auth-context-missing/) |
| agent-run-langsmith-per-agent-project | 2026-06-04 | 100% | 신규 9 + 회귀 41 pass, 핵심 2 + 테스트 2 files, 0 migrations — 에이전트 실행 LangSmith 추적을 **에이전트별 프로젝트(`agent-{이름}`)** 로 분리 + `run_name`/tags/metadata에 `agent_name` 보강 (공통 `stream()`→HTTP/SSE/WS 일괄). 전역 `os.environ["LANGSMITH_PROJECT"]` 변경(동시성 race) 대신 per-run `LangChainTracer(project_name=...)`를 `graph_config["callbacks"]`에 주입 — langchain_core가 명시적 tracer 존재 시 전역 auto-tracer를 억제(중복 없음). tracer 생성은 infra 헬퍼로 캡슐화(DDD). ★ "공유 `stream()` 1곳 수정 = 전 transport 커버 + per-run tracer로 전역 env race 회피" 교훈, dev LangSmith 수동 검증 + stale stream-router 테스트 정리는 후속 | [→](./agent-run-langsmith-per-agent-project/) |
| rag-score-threshold-filter | 2026-06-04 | 100% | 신규 16 케이스 pass (반복 0회, 첫 시도 100%), 6 src + .env.example + 테스트 4 files, 0 migrations — Agent RAG 하이브리드 검색이 점수 하한 없이 RRF top_k를 무조건 반환해 무관 문서가 혼입되던 문제 해결. RRF 병합 **이전** `HybridSearchUseCase._fetch_vector`에서 벡터 코사인 유사도(0~1) `>= threshold` hit만 통과시키는 단일 필터 지점 추가. 임계값은 전역 env(`RAG_VECTOR_SCORE_THRESHOLD`) + 에이전트별 `RagToolConfig.score_threshold` 오버라이드, `ToolFactory._resolve_score_threshold`가 주입. ★ "`None`(전역 기본) vs `0.0`(명시적 비활성) 의미 분리로 3-state 표현 + 기본 0.0 opt-in으로 회귀 0", ★ "Plan 질문 단계에서 RRF 점수의 비직관성을 사전 파악해 필터 기준을 벡터 코사인으로 확정" 교훈. Multi-Query 경로 전파·권장 기본값 산정은 후속 | [→](./rag-score-threshold-filter/) |
| analysis-chart-router | 2026-06-05 | 94% | 신규 25 (domain 10 + app 12 + infra 3) / 대상 68 pass / 광범위 회귀 291 pass, 소스 5 + 테스트 3 신규 + 5 수정, 0 migrations — 분석 노드 직후 **시각화 vs 텍스트 판단만 하는 라우터 노드** 신설(범위 "라우팅만"으로 축소, ChartSpec·chart_builder·프론트는 후속 Out of Scope). 하이브리드 판단: `VisualizationRoutingPolicy`(키워드+숫자≥4 휴리스틱)로 명시요청→visualize/신호없음→text 즉시 결정(LLM 회피), 애매구간만 `VisualizationClassifierInterface`(LLM) 위임 → `state.viz_decision` 기록. Supervisor(`category=="analysis"` 워커 직후 chart_router→quality_gate)·Excel(complete→chart_router→END, classifier=None 휴리스틱만) 공용, `_extract_question/_analysis_text`가 messages/user_query 양 state 형태 지원. DDD 3계층(domain 정책·포트·enum / application 노드 / infra LLM 어댑터, domain→app/infra 임포트 0). ★ "범위를 '라우팅만'으로 못박아 `viz_decision`+conditional edge 주석 부착지점이라는 **무결합 Seam** 확보→후속 chart_builder 독립 부착 가능", ★ "기존 Excel `execute_code` 경로 유지(비파괴)로 회귀 0 트레이드오프(G1)" 교훈. ChartSpec·chart_builder·분기 부착·프론트 동기화(`/api-contract-sync`)는 후속 | [→](./analysis-chart-router/) |
| analysis-node-agent | 2026-06-04 | 100% | 신규 12 케이스 (분석 노드 8 + 컴파일 그래프 3 + 레지스트리 1), 관련 61 pass / 전체 agent_builder 381 pass 0 fail, 소스 9 files, 0 migrations — Supervisor 그래프에 `category="analysis"` 전용 노드 추가. 엑셀 첨부 시 기존 `ExcelAnalysisWorkflow`(자가교정·할루시네이션) 래핑, 없으면 검색결과(있으면)/전체 대화 문맥(없으면) LLM 분석 후 결과만 반환 → `quality_gate→supervisor` 복귀. `ToolCategory+="analysis"` + `SupervisorState.attachments` + `_create_analysis_node`/`_run_excel_analysis`/`_analyze_context` + `Callable` getter 주입(None이면 문맥 분석 graceful fallback). ★ "DI를 워크플로우 직접 import 대신 getter로 역의존 → 결합 최소+부분배포 안전", ★ "취약한 `isinstance(.., lambda)` 함수노드 판별을 `function_node_ids` 명시 집합으로 개선" 교훈. Check 95%(GAP-1/2 테스트 미구현)→Act-1 그래프 레벨 테스트 2건 추가→100%. 엑셀 파일 HTTP 업로드/전달 표준 계약(`RunAgentRequest.attachments` 통로만 마련)·sub_agent attachments 전달은 후속(`/api-contract-sync`) | [→](./analysis-node-agent/) |
| excel-analysis-routing-cleanup | 2026-06-06 | 100% | 신규 5 (domain 2 + infra 1 + 테스트 2) + 수정 8, 테스트 64 pass, 0 migrations — Excel 분석 워크플로우의 **죽은 코드-실행 차트 경로 완전 제거** + 라우팅 정합성 회복. 샌드박스가 matplotlib 금지(수치 모듈만 허용)라 `execute_code`로 차트 생성이 실제 불가했고 `execute_code→END`로 분리된 `chart_router`를 우회하던 문제. ① 코드 생성/실행 노드·상태(`needs_code_execution`/`code_to_execute`/`code_output`)·메서드(`_execute_code_node`/`_detect_code`/`_extract_code`) 제거 → 모든 완료 경로를 `chart_router`로 일원화, ② 웹검색 판단을 `[SEARCH]` 태그 regex → structured(`SearchDecisionInterface`/`WebSearchDecision` + `with_structured_output`, 실패시 False graceful)로 전환(답변 텍스트는 유지), ③ 검색결정은 `web_search_results` 미존재 시에만 1회 호출(retry가 web_search 강제하므로 비용 1회+루프방지). 연쇄정리: `AnalysisResult`/`analysis_router` 코드필드 제거, `main.py` DI(`SandboxExecutor`→`LLMSearchDecisionAdapter`), supervisor `_run_excel_analysis` 입력 dict 정리. `SandboxExecutor`/`python_code_executor`는 커스텀 에이전트 정식 도구라 **유지**(워크플로우 의존만 제거). ★ "샌드박스 허용 모듈 화이트리스트가 차트-코드 경로를 죽게 한 근본원인 — 실행환경 제약 선확인", ★ "무관 기존 버그(router langsmith import 누락)를 아키텍처적으로 올바른 위치(application)로 수정해 verify-architecture 위반 회피" 교훈. 실제 차트 빌드(viz_decision→ChartConfig) 연결(N1)·프론트 Excel 렌더(N2)는 후속 | [→](./excel-analysis-routing-cleanup/) |
| supervisor-attachment-routing | 2026-06-07 | 100% | 신규 14 (TC-1~5) + 회귀 80 pass (격리 11 pass), 구현 3 + 테스트 1 files, 0 migrations — 커스텀 에이전트(`data_analysis` 워커)에 **엑셀 첨부 + 데이터 질의** 시 supervisor가 `FINISH`+"권한 없음"으로 거부하고 `analysis_node`에 진입조차 못 하던 치명 결함 수정. 근본원인: supervisor 의사결정 prompt가 `state["attachments"]`를 미주입 → LLM이 엑셀 존재를 모른 채 "데이터 없음→권한 없음" 추론 → `FINISH+answer` 단락으로 워커 edge 미트리거. 해결 이중방어: ① `_render_attachment_block`로 decision prompt에 첨부 인지 블록+거부 억제 문구 주입(C-1), ② `AttachmentRoutingHooks.force_worker`가 엑셀 첨부+analysis워커 시 **LLM 우회 결정적 라우팅**(`last_worker_id` 가드로 1회만, 루프방지)(C-2), ③ compile()에서 `DefaultHooks`일 때만 첨부훅 연결(외부주입 존중)(C-3). 아키텍처/시그니처/DB/API 무변경. ★ "노드(analysis)는 attachments를 보는데 라우팅 결정자(supervisor)는 못 보는 **정보 비대칭**이 거부의 근본 — 결정자에게도 동일 컨텍스트 노출 필요", ★ "확률적 LLM 판단 + 결정적 훅 이중방어로 거부 회귀 차단" 교훈. 차트 렌더링("그래프로 그려줘", supervisor 경로 미지원)·"내 휴가일수" 도메인 결합·TC-6 통합테스트는 후속(F-1~F-5) | [→](./supervisor-attachment-routing/) |
| fix-chat-reasoning-object-render | 2026-06-08 | 100% | 신규 BE 14 (helper 10 + agent 2 + general chat 2) + FE 2 pass, 1 src 신규 + 2 src + 2 hook 수정 + 5 테스트, 0 migrations — `/chatpage` WS 추론 토큰이 content block list 모델에서 `[object Object]`로 표시되던 출력 결함 풀스택 수정. 근본원인: LangChain `on_chat_model_stream` chunk.content가 provider/구성에 따라 `str` 또는 block list(`[{type,text}]`)인데 백엔드 매핑(`_map_chat_stream`/`_map_token`)이 정규화 없이 payload `chunk`에 적재 → 프론트 `tokens + chunk` 문자열 결합으로 `[object Object]` 노출. 해결: domain 순수 함수 `coerce_message_text()`(str 통과/list 평탄화/그외 "") 신설해 백엔드 2곳 정규화(빈문자열 스킵 가드 보존) + 프론트 2 hook `typeof === 'string'` 가드(2차 안전망). STEP_REASONING은 이미 문자열 보장이라 무관(원인=token chunk 확정). ★ "LLM content 다형성(str|block list)은 WS/DB/UI 경계에서 단일 정규화 지점 필수", ★ "백엔드 근본수정+프론트 방어 이중화로 계약 재위반 재발 방지" 교훈. WS payload pydantic `chunk:str` 강제검증·non-text block 가시화는 후속 | [→](./fix-chat-reasoning-object-render/) |
| supervisor-chart-builder-node | 2026-06-08 | 99% | 신규 BE 13 (chart_builder_node 5 + workflow_compiler 배선 2 + skip_workers 2 + excel 배선 2 + ANSWER_COMPLETED charts 2) + FE 2 pass, 2 src 신규 + 13 수정, 0 migrations — `chart-builder`(General Chat 전용)·`analysis-chart-router`(viz 판단만)에 이어 **Supervisor/Excel 경로의 비어있던 차트 생성 노드** 구현. 근본문제: 분석 워커 후 `chart_router`가 `viz_decision`만 기록하고 실제 Chart.js 생성 노드가 없어, supervisor LLM이 "그래프 그려줘" 미완료로 판단→분석 워커를 `max_iterations(10)`까지 반복 호출하는 **무한루프**. 해결: 기존 `LangChainChartBuilder` 재사용한 `chart_builder` 노드 신설 → `chart_router`를 conditional edge(`visualize→chart_builder→quality_gate`, `text→quality_gate`)로 전환. **루프 차단 핵심**: chart_builder가 `messages`/`last_worker_id` 불변(quality_gate가 직전 분석 AIMessage 평가→재시도 회피) + `visualization_done` 플래그로 `AttachmentRoutingHooks.skip_workers`가 분석 워커를 **결정적 제외**→supervisor FINISH. 차트는 `ANSWER_COMPLETED.charts`(General Chat 계약 재사용, ws_adapter payload 패스스루로 ws_router 무변경)로 전달. Supervisor는 compile() 내 per-run llm으로 빌더 생성(classifier 패턴 일치), Excel은 `_default_llm_model` 로드 후 생성(`claude_client` 미사용 — startup 순서 조정). `chart_max_count=0`/`chart_builder=None` 하위호환. 프론트 `websocket.ts`→`useAgentRunStream`→`ChatPage`로 `charts` 전달, 기존 `MessageBubble`/`ChartRenderer` 재사용(WS→message.charts 연결은 그동안 미완이던 "후속" 완성). ★ "차트 생성과 루프 제어 분리 — 노드는 state.charts만 채우고 결정적 skip 가드로 LLM 판단 비의존 루프 차단", ★ "이미 존재하는 빌더를 재사용(신규 로직 0)하고 cross-repo로 프론트 미완 지점을 발견해 범위 확정" 교훈. gap-detector 97%→§11-4 ANSWER_COMPLETED charts 테스트 + `.env.example` CHART_MAX_COUNT 보강→99%. 중첩 supervisor→excel 이중 차트 생성 가드(설계 §14)·차트 영속화는 후속 | [→](./supervisor-chart-builder-node/) |
| analyze_with_claude | 2026-06-09 | 100% | 신규 BE 17 (sanitizer 11 + guide 6) + excel +2 / 회귀 compiler 28 + excel 16 pass, 신규 2 + 변경 2 files, 0 migrations — 분석 노드(`analyze_with_claude` / `_analyze_node`)의 system prompt가 "데이터에 근거하라" 2줄뿐이라 "사용자별 남은 휴가 그래프 그려줘" 류 질문에 **분석 텍스트+차트 JSON을 함께** 뱉어, 이미 존재하는 `chart_router→chart_builder` 차트 전담 경로와 책임 중복 + 하류(할루시네이션 평가·라우팅 숫자 휴리스틱) 오염. 해결: ① 공용 `ANALYSIS_OUTPUT_GUIDE`(application 상수)로 출력 형식·범위 엄격화 — 자연어만/코드블록·JSON 절대금지/그래프 요청도 차트 직접생성 대신 "배상규 5일, 김철수 3일" 항목별 수치만 자연어 나열/파이프라인 메타설명 답변 노출 금지/요청 범위만, ② domain 순수정책 `AnalysisOutputSanitizer`(모듈 싱글톤 `ANALYSIS_OUTPUT_SANITIZER`)로 새어나온 펜스 코드블록+균형 중괄호 JSON 객체+객체배열 제거(수치배열 `[1,2,3]`은 차트 원재료라 보존, 키 신호 `"x":`+균형괄호 게이트로 오탐 방지), ③ excel `_analyze_node`+supervisor `_analyze_context` **양쪽 공용 적용**(저장 전 strip). 그래프 구조·응답 스키마 불변(프론트 영향 0). ★ "차트 생성과 분석을 프롬프트+순수정책으로 분리해 기존 chart_router/chart_builder 경로 의도를 코드로 못박음 — 아키텍처 변경 0으로 회귀 차단", ★ "raw JSON 제거 시 수치배열 보존+키신호 게이트로 차트 원재료 손상 없이 오탐 최소화" 교훈. supervisor `_analyze_context` 통합테스트·Design §3-1 배열우선 순서 동기화·새니타이즈 빈도 로깅은 후속 | [→](./analyze_with_claude/) |
| chart-builder | 2026-06-06 | 100% | 신규 25 (domain 11 + infra 6 + app 8) pass, 소스 3 신규 + 5 수정 + 테스트 4 신규, 0 migrations — `analysis-chart-router`(viz 판단만)와 `idt_front chat-chart-rendering`(렌더 완료) 사이 비어있던 **중간 연결** 구현. 분석/답변 텍스트에서 LLM `with_structured_output`으로 수치 추출 → **Chart.js 네이티브 config(JSON)** 생성, General Chat `chat_answer_completed.charts`(이미 프론트 wired)에 주입. `ChartConfig`=프론트 `ChartPayload`(`{type,data,options}`, 화이트리스트 bar/line/pie/doughnut/scatter/radar). 핵심: **추출↔표현 분리** — LLM은 데이터(`ChartDraft`)만, 색상·options는 domain `ChartStylePolicy`가 결정론적 조립(LLM에 hex 색상 위임 회피). `_maybe_build_charts`가 기존 `VisualizationRoutingPolicy`/`Classifier` 재사용해 visualize 판정시만 빌드, 실패/비-visualize/미주입 시 `charts=[]` graceful. DI는 REST+WS 공유 팩토리(`create_general_chat_use_case_factory`) 1곳, 상한 `settings.chart_max_count=3`. DDD domain→infra 참조 0. ★ "추출(LLM/infra)과 표현(색상·options/domain Policy) 분리로 LLM 불안정성 격리", ★ "프론트 계약이 이미 General Chat에만 wired임을 cross-repo 조사로 발견해 범위를 General Chat 우선으로 확정" 교훈. gap-detector 98%→`_build_chart_context` tools_used 미사용을 의도된 단순화로 설계 정정→100%. 프론트 REST 타입 동기화(`/api-contract-sync`)·Excel/Supervisor 연동은 후속 | [→](./chart-builder/) |
| analyze-user-context | 2026-06-10 | 96.2% | 신규 BE 4 클래스(프롬프트 회귀·블록 우선순위·use case auth_ctx·supervisor 2분기) pass, 소스 3 + 테스트 1 files, 0 migrations — 분석 노드(`analyze_with_claude`/`_analyze_node`) 프롬프트에 로그인 사용자 신원(이름·대표부서·역할·권한 라벨) 블록을 prepend해 "나의 휴가 며칠?" 류 1인칭 자기지칭 질의를 분석 경로가 처리 가능하게 함. general_chat에서 이미 검증된 `render_user_context_block` 재사용(신규 렌더러 0, 화이트리스트 자동 상속 → 사번·이메일·user_id 누출 0). 블록 소스 우선순위 = state `user_context_block`(명시 주입) → 없으면 `get_current_auth_context()` ContextVar 폴백(Defense in Depth), 미인증 시 "" → 회귀 0. 두 경로 적용: ② supervisor(`_run_excel_analysis` initial state + `_analyze_context` system prompt prepend, ContextVar는 `run_agent_use_case:209`에서 이미 세팅) 실질 수정, ① 단독 엑셀 use case는 `*, auth_ctx=None` forward-compat 키워드만 추가하고 `analysis_router`는 미변경(D2 — 익명 허용 유지). 그래프 구조·응답 스키마 불변(프론트·api-contract 불필요). ★ "분석 경로와 대화 경로의 사용자 컨텍스트 일관성 — 화이트리스트 렌더러 재사용으로 노출 표면 0", ★ "state 명시 주입 + ContextVar 폴백 이중화로 두 진입 경로 공용" 교훈. gap-detector 96.2%(테스트가 Design 명시 3파일 대신 단일 `test_analyze_user_context.py` 통합 — 커버리지 완전, 파일명만 상이). 경로① 라우터 인증 결합(`AssembleAuthContextUseCase`)·supervisor 블록계산 헬퍼 추출은 후속 | [→](./analyze-user-context/) |
| chat-chart-persistence | 2026-06-10 | 100% | 신규 22 케이스(domain 7 + infra 4 + app 8 + interfaces 2 + frontend 2) pass, BE 442 + interfaces 21 + FE 28 pass, BE 9 + FE 3 = 12 files, **1 migration(V031)** — chart_builder가 만든 차트(`state["charts"]`)가 `ANSWER_COMPLETED` 스트리밍 이벤트로만 전달되고 assistant 메시지 저장엔 텍스트만 영속화돼 **채팅방 재진입 시 차트 영구 소실**(답변은 "아래 차트 참고"인데 차트 없음)되던 풀스택 결함 수정. `conversation_message.charts` JSON nullable 컬럼(V031) 신설 → Agent Run(`_save_assistant_message(charts=state.charts or None)`)·General Chat(**차트 생성→저장 순서 변경** D4 + `_persist_messages(charts)`) 양 경로 영속화, 이력 API 2종(`MessageItemAPI.charts`)→프론트 `toMessage` 매핑→기존 `MessageBubble` 재사용(렌더 코드 0). N개 배열 통째 직렬화(생성단 `chart_max_count`가 유일 상한, 저장 상한 0 D3). 빈 배열 금지→None(엔티티 `__post_init__` 검증 + 저장부 `charts or None` 이중 D2), frozen dataclass 맨뒤 기본값 None으로 기존 호출부 무변경(D6). **charts는 표시 전용 메타 — LLM 컨텍스트/요약 미투입(D7, 회귀 방지 테스트 고정)**, user 메시지 charts=None(D8), 스트리밍 스키마 무변경(D9). ★ "스트리밍으로 본 화면=재진입 복원 화면 일관성 — 차트가 일회성 효과가 아닌 대화 기록의 일부", ★ "General Chat 저장이 차트 생성보다 먼저인 구조적 문제를 Design에서 선포착해 순서 역전으로 해결" 교훈. gap-detector 100%(Gap 0). **운영 배포 전 V031 `ALTER TABLE` DB 적용 필수**(파일만 생성), 과거 row charts는 NULL(소급 복원 없음), Excel Standalone 경로는 범위 외 | [→](./chat-chart-persistence/) |
| chart-context-continuity (Phase 1) | 2026-06-10 | 100% (Phase 1) | 신규 43 (caption 8 + followup 12 + edit_draft 6 + transformer 7 + use_case 10) + 회귀 1,723 pass, 신규 3 + 수정 6 files, 0 migrations — 차트 생성 후 "해당 그래프에 색깔 넣어줘"·"파이로 바꿔줘" 류 **후속 지시를 AI가 인식 못하던** 멀티턴 단절 해결(General Chat 경로). 근본원인: 차트가 `conversation_message.charts`에 저장돼도 D7("표시 전용, LLM 컨텍스트 미투입") 결정으로 후속 턴 컨텍스트에 차트 존재 자체가 안 보임. 해결 4요소: ① **D7→D7-rev1 개정**(full config 미투입 유지 + 캡션 1줄만 투입, `ChartCaptionPolicy` ≤200자, 요약 본문 미포함) → "해당 그래프" 지시어 해석 가능, ② `ChartFollowupPolicy` 휴리스틱(차트명사+지시어/편집동사, LLM 무의존, 애매시 NONE 보수적)으로 편집 의도 1차 판정, ③ 신규 포트 `ChartTransformerInterface`+`LangChainChartTransformer`(기존 차트 config+지시→새 config, structured output, 8KB 절단, "LLM은 데이터만/색상은 `ChartStylePolicy`" 원칙 유지하되 명시 색상만 `ChartEditSeriesDraft.color` 오버라이드)로 변환, ④ use_case `_try_chart_edit` 분기가 **ReAct 에이전트 우회**(도구 호출 불필요→비용↓), transformer 미주입/저장차트 없음/의도 NONE/변환 빈결과/예외 **5중 graceful 폴백**으로 일반 경로 복귀. 응답 계약(`charts: list[dict]`, ANSWER_COMPLETED payload) 무변경 → **프론트 작업 0**, `tools_used=["chart_transformer"]` 관측성. DDD 3계층(domain 정책·포트 / infra LLM 어댑터 / application 분기, domain→infra 참조 0). gap-detector Phase 1 **100%**(Gap 0, 차이는 빈 draft 필터·널안전 등 상향호환 2건뿐). ★ "이미 저장된 데이터(charts)를 '꺼내 쓰는 경로'만 없던 문제 — 저장≠활용, 컨텍스트 투입 정책이 핵심", ★ "편집 경로 ReAct 우회 + 5중 폴백으로 신기능이 기존 대화 흐름을 절대 막지 않도록 보수적 설계" 교훈. **Phase 2/3는 의도된 deferred**(엑셀/문서 분석 데이터 영속화 `analysis_artifact`+V032·분석 후속 차트 재사용) — 선행 `excel-chart-routing-dedup`(차트 파이프라인 일원화) 완료 대기 | [→](./chart-context-continuity/) |
| search-node-query-pipeline | 2026-06-10 | 96.4% | 신규 36 (policy 10 + pipeline 22 + wiring 4) + 회귀 316 pass, 신규 1(`search_pipeline.py`) + 수정 7 files, 0 migrations — Supervisor **search 노드가 `messages[-1]` 대화체 원문**("…실업률 정보로 월별 그래프 그려줄 수 있니?")을 그대로 검색 쿼리로 사용해 무관 결과 혼입 + 검증·압축 없이 원문 전체가 final_answer/analysis 컨텍스트로 유입되던 문제 해결. search 노드 **내부** 4단계 파이프라인: ① 경량 LLM(config `SEARCH_PIPELINE_PROVIDER/MODEL_NAME` 기본 openai/gpt-4o-mini, 인스턴스당 1회 생성·캐시, 생성실패시 per-run LLM fallback D3) 쿼리 재작성(시각화 요구 제거+지시어 치환, 실패시 원본 질문) → ② 검색 → ③ structured 관련성 검증(`SearchResultVerdict`), 부적합시 improved_query 재검색 — **총 3회 한도, 마지막 시도 validate 생략(D1)** 으로 최악 LLM 4회(NFR ≤5 개선), 도구 예외는 validate 생략 즉시 재시도(D4, 일시장애 자동복구) → ④ `SEARCH_COMPRESS_THRESHOLD`(4,000자) 초과시만 압축(수치·날짜·단위·출처 보존 프롬프트). **5중 graceful fallback**(LLM 생성/rewrite/도구/validate/compress)으로 그래프 비중단. 메시지 규약(`[{worker_id} 검색결과]` prefix + `is_search_result`/`is_worker_output`/`latest_user_question`)을 search_pipeline로 **중앙화**(D2, compiler 중복 정의 삭제·alias import), 한도·임계는 domain `SearchPipelinePolicy` 순수규칙, SupervisorState 키 무추가(D6 — attempts/query는 step output_summary로만 관측). 부수: quality_gate 피드백(`[품질검증 실패]`)이 `latest_user_question`에서 실제 질문으로 오인되던 잠재 버그 해소(D5, prefix 상수화로 분석 노드 공용 수혜). sub_agent 내부 search 워커도 compile 재귀로 자동 적용. ★ "함수형 노드 내부 파이프라인으로 그래프 구조·라우팅·quality_gate 0 변경 — 회귀 차단하며 검색 품질 개선", ★ "마지막 시도 validate 생략(어차피 채택) + 검색 실패 메시지 압축 제외 등 무의미 LLM 호출 제거" 교훈. gap-detector 96.4%(High/Medium Gap 0, Low 3 — D3 문구 설계 동기화로 해소, 나머지 무영향 보류). 검색 품질 실측 모니터링(step summary `query=/attempts=/compressed=` 로그)·임계값 튜닝·ollama 등 로컬 모델 지연 검증은 후속 | [→](./search-node-query-pipeline/) |
| supervisor-viz-routing | 2026-06-11 | 100% | 신규 15 (Hook TC-1~7 + supervisor 통합 TC-8 + prompt 블록 TC-9~11 + 회귀/단위 4) + 회귀 396 pass (agent_builder 322 + visualization 74), 소스 3 + 테스트 1 files, 0 migrations, **반복 0회** — search+data_analysis 에이전트에 "2026년 평균기온 그래프 그려줘" 류 **첨부 없는 시각화 요청** 시 supervisor LLM이 검색 결과만으로 FINISH → 분석 워커 미경유 → 차트 경로(analysis→chart_router→chart_builder)가 분석 워커 뒤에만 배선돼 차트가 아예 생성되지 않던 결함 수정. 분석 워커 결정적 강제가 엑셀 첨부(`AttachmentRoutingHooks._ROUTABLE_TYPES`)에만 존재했던 것을 **시각화 의도 트리거로 확장**: `viz_policy` 옵셔널 주입(None=기존 동작 100% 하위호환) + "시각화 의도(`VisualizationRoutingPolicy.explicit_request`) + 검색결과 존재(`is_search_result`) + 분석 미실행 + `visualization_done` False" 4조건 강제(D1). decision prompt에 `[시각화 안내]` 블록(차트는 분석 워커 경유 필수, 검색→분석 순서, 검색 결과만으로 FINISH 금지)을 viz 키워드 감지 시에만 삽입(D2, `_render_attachment_block` 패턴). 검색결과 없으면 Hook 침묵(D3 — 데이터 없는 강제 분석의 환각 차트 방지, prompt 유도만), search 워커 강제는 비범위(D4). 루프 방지: `last_worker_id` 가드 + `visualization_done` 명시 가드(엑셀 강제에도 신규 적용) + 기존 `skip_workers` 3중 방어. 부수: supervisor_node except 블록 `exception=` 누락(LOG-001) 수정. 그래프 토폴로지·chart_router·chart_builder·분석노드·final_answer·API 스키마 모두 무변경(재사용만). ★ "강제 발동 조건과 chart_router의 즉시 visualize 판정이 **동일 도메인 정책 메서드**(`explicit_request`)라 강제한 분석은 반드시 차트까지 도달 — '강제했는데 차트 안 나오는' 상태가 구조적으로 불가능", ★ "기존 결정적 라우팅 패턴(첨부 Hook + 인지 블록)을 트리거만 확장해 Open Question 4건 사전 확정(D1~D4) → 반복 0회·Match Rate 100%" 교훈. 검색결과 없는 시각화 요청의 품질(대화 문맥 fallback)·분석 2회 가능성(max_iterations 수용)은 후속 | [→](./supervisor-viz-routing/) |
| fix-anthropic-prefill-error | 2026-06-11 | 100% | 신규 17 (normalization TC-01~07 9건 + supervisor TC-08/09 + compiler TC-10~12b 4건 + claude_client TC-13/14) + 회귀 505 pass (agent_builder 337 + llm/general_chat 168), 소스 4(신규 1) + 테스트 4(신규 1) files, 0 migrations, **반복 0회** — Anthropic(claude-sonnet-4-6) provider로 supervisor 멀티에이전트 실행 시 `400 "This model does not support assistant message prefill"`로 워커 1회 경유 후 2차 판단부터 죽던 결함 수정. 근본원인: Claude 4.6+는 메시지 배열이 assistant로 끝나는 요청(=prefill)을 400 거부하는데, ① supervisor_node가 결정 프롬프트를 **배열 끝 system**으로 append → langchain_anthropic이 system을 top-level 파라미터로 호이스팅 → 실제 배열이 워커 `AIMessage(name=...)`로 끝남, ② `_wrap_worker` 워커 연쇄 호출·`_analyze_context`·`final_answer_node`도 동일 패턴. OpenAI는 assistant-last 허용이라 Anthropic 전환 시에만 발현된 provider 종속 버그. 해결: "LLM 호출 직전 배열은 user로 끝나야 한다" 불변식을 `message_normalization.ensure_user_tail`(assistant-last면 지시 HumanMessage **비파괴 append**, dict/LangChain 양형태 role 추출, 빈 배열 D5 처리) 공통 헬퍼로 중앙화 → 4개 호출 지점 적용 + supervisor는 결정 프롬프트를 system 선두로 이동(텍스트 무변경, 캐시 prefix 안정성 부수효과). `ClaudeClient._build_messages`에 warning+continuation 최후 방어선(D3 — 예외 대신 가용성). provider 분기 없음(user-last는 전 provider 유효 → OpenAI/Ollama 무해, 1차 판단·통상 경로 no-op). ★ "provider별 메시지 규칙 차이는 호출부 산재 수정이 아닌 application 공통 정규화 헬퍼 한 곳에서 흡수 — 향후 모델 추가 시 재발 방지", ★ "langchain_anthropic의 SystemMessage 호이스팅이 '끝에 system 붙이기' 패턴을 침묵 속에 prefill로 변환 — 메시지 role 배치는 변환 후 와이어 포맷 기준으로 검증" 교훈. 실 Anthropic 수동 스모크(서버 기동)·Opus 4.7+/Fable 5 업그레이드 시 `temperature` 400 이슈(LLMFactory/ClaudeClient)는 후속 | [→](./fix-anthropic-prefill-error/) |
| naver-search-mcp-integration | 2026-06-16 | 100% | 신규 5 테스트파일 90 pass (domain policies/masking + security cipher + smithery_url 47, loader streamable+SSE 8, app+repo+router 35; 격리 실행), 신규 3 소스(`security/secret_cipher.py` Fernet `SecretCipher`·`mcp_registry/smithery_url.py` `build_streamable_http`·**V032 migration**) + 9 확장, **1 migration(V032)** — Smithery 호스팅 **Naver Search MCP(Streamable HTTP)** 를 `mcp_registry` 등록→로드→호출 경로에 연동. 기존 파이프라인이 **SSE 하드코딩**(`register_use_case:49`)·**인증/config 주입 불가**(loader SSE 고정)·**시크릿 컬럼 부재**라 `api_key`/`profile` 쿼리 + `NAVER_CLIENT_ID/SECRET` config를 요구하는 Smithery 서버를 못 붙이던 문제 해결. 선행 `mcp-http-call-module`이 제공한 `MCPTransport.STREAMABLE_HTTP`/`StreamableHTTPServerConfig`/factory(3-tuple) 전제 위에서: ① 도메인 `MCPTransportType.STREAMABLE_HTTP` + `auth_config`/`server_config` 필드 + `mask_secrets`/`masked_auth`/`masked_server_config`(중첩 헤더 재귀 마스킹), ② Policy `validate_transport`(화이트리스트)·`validate_auth`(streamable_http는 api_key 필수), ③ DB `auth_config_enc`/`server_config_enc` nullable Text(V032, 기존 행 무영향), ④ Request/UseCase de-hardcode → `request.transport` 사용 + Response 마스킹, ⑤ `MCPToolLoader` transport 분기로 `build_streamable_http`(/mcp 보정 + api_key/profile 쿼리 + base64 JSON `config` + 헤더) 조립, ⑥ Repository **경계 암복호화**(`_to_model` 암호화/`_to_entity` 복호화, `cipher=None`이면 enc 미접근 → SSE 100% 호환), ⑦ main.py `_mcp_cipher()` + 4개 repo 지점 주입, `config.mcp_secret_key`. **비파괴**: transport 기본 "sse"·Optional 필드·nullable 컬럼·cipher None 허용. **DDD 100%**: domain은 stdlib(dataclasses/enum/urllib)만, 암복호화는 infra(Repository) 경계에만 격리, UseCase/loader는 평문 dict만. **보안**: Fernet 대칭암호화·응답 마스킹·로그 `request_id`만(시크릿 0 노출)·하드코딩 0. gap-detector 97%→**100%**(하이진 갭 2건 즉시 해소: `cryptography>=42.0.0` 직접 의존성 + `.env.example` `MCP_SECRET_KEY` 문서화). ★ "SSE 전용 등록 경로를 transport 선택 + 시크릿 주입형으로 일반화 — Smithery 마켓플레이스 임의 Streamable HTTP MCP 연결 토대", ★ "암복호화를 Repository 경계 1곳에만 격리해 domain 순수성·UseCase 평문 단순성 동시 확보" 교훈. router 교차실행 산발 실패 = 기존 Windows ProactorEventLoop teardown flakiness(회귀 아님, 격리 결정적 pass). **운영 배포 전 `MCP_SECRET_KEY`(.env) 설정 + V032 적용 필수**. **후속**: 실 Smithery E2E(`qualifiedName`·config 전달규칙 실측)·MCP 등록 프론트 UI·시크릿 KMS/Vault·다중 Naver tool 전수검증 | [→](./naver-search-mcp-integration/) |
| mcp-http-call-module | 2026-06-16 | 97% | 신규 67 (domain 27 + factory 7 + call_client 7 + 회귀 26 등, 격리 실행 68 pass) + ruff·verify-architecture/logging/tdd 전부 PASS, 소스 2 신규(`policy.MCPRetryPolicy`·`call_client.MCPCallClient`) + 2 확장(value_objects·client_factory) + 테스트 4, 0 migrations, **반복 0회** — 흩어진 MCP 호출(매 호출 세션 생성하는 `MCPToolAdapter._arun`, SSE 단일 timeout, 재시도·Bearer인증 표준 부재, **Streamable HTTP 미지원**, LangChain 결합으로 호출코어 단독테스트 불가)을 **LangChain 비의존 순수 호출코어 `MCPCallClient`** 로 수렴. 도메인에 `MCPTransport.STREAMABLE_HTTP`·`StreamableHTTPServerConfig`·세분화 타임아웃 `MCPTimeoutConfig`(connect/read/total)·`MCPAuthConfig`(Bearer+커스텀, `to_headers`)·경량 VO `MCPToolDescriptor`·재시도정책 `MCPRetryPolicy`(`compute_backoff` 지수백오프+상한, `is_retryable` 분류) 추가. Factory는 `streamablehttp_client` **3-tuple**(`(read,write,get_session_id)`—SSE의 2-tuple과 분리) 언패킹 + `_merge_headers`(정적→auth 오버레이, 충돌시 auth 우선) + timeout 주입(connect→`timeout`/read→`sse_read_timeout`) + 4-transport **dict 디스패치**로 if중첩 평탄화. 코어는 호출당 stateless 세션 + `asyncio.wait_for(total)` + `_execute` 재시도 루프 + `_should_retry` **스테이지 구분**(연결단계 항상 재시도 후보 / tool실행은 `retry_tool_execution=True` 옵트인 — 비멱등 부작용 차단) + `request_id`·`elapsed_ms` 구조화 로깅, 도구 `isError`는 예외 대신 `is_error` 결과 반환. **비파괴**: `MCPServerConfig` 필드 추가만 + `create_session` 신규 인자 keyword-only 기본값 → `tool_adapter.py:72` 등 기존 소비자 무변경. ★ "분산된 호출 로직을 단일책임 주입형 코어로 수렴 — transport교체·타임아웃/재시도 정책변경이 호출부 무수정으로 가능, pytest/LangGraph 양쪽 재사용", ★ "`streamablehttp_client`가 SSE와 달리 3-tuple yield — 와이어 시그니처를 SDK 실측으로 고정해 Risk 사전제거" 교훈. gap-detector 97%(누락 0, Info 2 = config-timeout 우선순위·list_tools 재시도 설계 미명세 → 설계문서 보강으로 해소, Minor 1 = elapsed_ms 로깅 → Report 전 반영). Windows ProactorEventLoop teardown flakiness로 다중 async파일 동시실행시 teardown ERROR 산발 → 파일단위 격리실행 전부 pass. **후속(별도 PDCA)**: `MCPToolAdapter`/`Registry`를 코어 소비로 리팩토링(LangGraph 통합)·`MCPToolLoader` transport DB선택 확장·동기 호출래퍼/세션 풀링·`retry_tool_execution=True` 성공경로 테스트 보강 | [→](./mcp-http-call-module/) |
| skill-builder | 2026-06-25 | 97% | 신규 BE 54 (domain policies+schemas + infra repo + app use_cases 통합) + FE 11 pass, 검증항목 65 중 일치 63·부분 2(테스트 파일 조직·UI 분리 미적용)·누락 0, **1 migration(V033)** — "에이전트는 만들지만 에이전트에 주입할 **재사용 능력 단위(Skill)**를 저장·관리할 방법이 없다" 문제 해결. `agent_builder`/`mcp_registry`와 동일 Thin DDD 4계층으로 `skill_definition` 도메인 신설(신규 추상화 0). Skill = **instruction(지시문) + script_content(실행 스크립트, 저장 전용·실행 없음)**. `SkillDefinition` 엔티티(visibility private/department/public + soft-delete `status` + fork) + `SkillBuilderPolicy`(name/description/instruction/script/visibility 검증, none+script 충돌 차단) + `SkillVisibilityPolicy`/`SkillForkPolicy`(agent 규칙 복제, 도메인 간 import 회피). V033(`trigger`는 MySQL 예약어→`trigger_text` 컬럼 매핑, ENUM visibility, FK departments ON DELETE SET NULL). API 7종 `/api/v1/skills`(POST·my·list(RBAC)·GET·PUT·DELETE soft·fork) — 라우트 등록순서 `/my`·`/list`를 `/{id}` 앞에. Repository `MySQLBaseRepository` 상속 + `_to_model`/`_to_entity`(cipher 없음·평문 TEXT), commit/rollback 미호출. 프론트 풀스택 `/api-contract-sync`: snake_case 미러 타입 + `skillService`/`useSkills` + `AdminSkillsPage`(목록+검색+인라인 `SkillFormModal` 생성/수정/삭제/Fork, "스크립트는 저장만 됨" 안내) + adminNav. ★ "기존 2개 도메인(mcp 단순 CRUD + agent visibility/fork) 패턴 조합으로 예측가능성·일관성 100% 확보 — 신규 추상화 도입 0", ★ "script_content는 TEXT 저장 전용(eval/exec/subprocess 0)으로 현 phase 코드 실행 위험 제로, 후속 결합은 단계 분리" 교훈. gap-detector 97%(기능/아키텍처 영향 0, 잔여 2건은 테스트 파일 통합·UI 단일파일 인라인 등 조직적 차이). **운영 배포 전 V033 적용 필수**. **후속**: `skill-agent-integration`(Phase A 부착·instruction 주입, 완료)·`skill-script-runtime`(Phase B script 샌드박스 실행) | [→](./skill-builder/) |
| skill-agent-integration (Phase A) | 2026-06-27 | 97% | 신규 BE 27 (domain policies 7 + infra repo + app attach/detach/list + run-injection 4) + FE 6 (useAgentSkills 4 + AgentSkillPanel 2) pass / 회귀 0 (run_agent 26 + AgentBuilderForm 7), BE ~16 + FE ~9 files, **1 migration(V034)** — `skill-builder`가 저장만 하던 Skill을 **에이전트 실행에 결합**(Phase A=instruction 주입, script 실행은 Phase B로 격리). 별도 `agent_skill` 조인 도메인(Thin DDD 4계층) 신설: `AgentSkillLink` VO + `SkillAttachPolicy`(MAX 3·중복 차단) + `SkillInjectionPolicy`(sort_order 병합·`[부착된 스킬:name]` 구분자·40k 총길이 가드·부착 0개=base 불변·빈 instruction 스킵) + repo 인터페이스(attach/detach 멱등/list_links/`list_attached_skills` JOIN status='active'). V034(agent_id·skill_id FK CASCADE×2 + `uq_agent_skill` UNIQUE). UseCase 3종: Attach 권한 = **에이전트 수정(owner/admin) ∧ skill 접근(visibility)** 두 도메인 정책을 application 계층에서 조합(도메인 간 직접 import 회피), Detach 멱등, List 접근검증+max_attachable 노출. ★ **주입 지점(D3)**: `RunAgentUseCase._inject_attached_skills`가 `dataclasses.replace`로 `supervisor_prompt` 병합 — **`WorkflowCompiler` 완전 무수정**(785줄 컴파일러 회귀 0), `agent_skill_repo` optional(None=주입 비활성→기존 동작 100% 유지), compile 내부 `render_user_context_block` prepend가 그대로 최외곽 유지(보안 컨텍스트 우선순위 보존). 라우터 `/{agent_id}/skills` GET/POST/DELETE(200/201/204·403/404/409/422). 프론트 풀스택(`/api-contract-sync`): snake_case 미러 타입 + service/hooks + `AgentSkillPanel`(수정 모드 마운트, max-3 비활성 + "⚠ script 미실행" 안내) + MSW. ★ "주입을 compile **상류**(application)의 순수 프롬프트 변환으로 격리해 거대 컴파일러 회귀 차단 + user_context 최외곽 유지", ★ "Phase A(텍스트 주입, 위험 0)와 Phase B(코드 실행)를 명확히 분리해 단계적 안전 도입" 교훈. gap-detector 97%(기능 갭 0, 3% = 문서-코드 표기차 D-F1 snake_case/D-B1 inline _can_edit_agent/D-B2 role.value, 전부 결과 동일). D4(서브에이전트 미주입)·Phase B(script 실행/샌드박스) 의도적 Out of Scope·`eval`/`exec`/`subprocess` 전무. **운영 배포 전 V034 적용 필수**. **후속(별도 PDCA)**: `skill-script-runtime`(Phase B 샌드박스 실행 + ToolFactory `skill_` 도구화)·문서 정합성 정정·감사 로깅 | [→](./skill-agent-integration/) |
| final-answer-node | 2026-06-10 | 100% | 신규 19 TC(라우팅 3 + 가드 2 + 노드 7 + compile 5 + 관측성 2), agent_builder 283 pass + 프론트 22 pass, 소스 3 + 테스트 5 files, 0 migrations — Supervisor 그래프의 최종 답변 생성 주체가 경로마다 달라(검색 전용 `answer_agent` / supervisor FINISH 직접 작성 / 마지막 워커 raw 출력) **웹서치+분석+차트 멀티 워커 실행 시 결과가 종합되지 않던** 비일관성 해결. 워커 1개+ 실행된 모든 런을 종료 직전 `final_answer` 노드로 **라우팅 함수(`route_to_worker_or_final`) 기반 필수 경유**(supervisor LLM 선택 비의존, `last_worker_id` 유무로 분기) → 검색결과·분석결과·차트 메타를 한 번에 정제. 기존 `answer_agent` 가상 워커(`__virtual__`) 방식 통합 제거. 핵심 결정: D1 워커 실행 시에만 경유(단순 대화는 즉시 END, LLM +0), D3 `final_answer→END` 직행(각 워커가 이미 quality_gate 통과 → 무한루프 위험 0), D4 depth=0만 적용(sub_agent는 원시결과 반환, 토큰 이중정제 방지), DQ1 FINISH draft answer **코드 가드 폐기**(`decision.answer and not last_worker_id` — 프롬프트 지시는 보조), DQ4 `_is_worker_output`(name 보유 AIMessage) name기반 수집으로 `_is_search_result` 문자열매칭 의존 축소, DQ5 `_summarize_charts`로 차트 개수/type/title만 주입+JSON출력 금지(charts 비파괴 — 반환 dict 미포함). final_answer는 `effective_supervisor_prompt`(사용자 컨텍스트 블록 포함) 수신(기존 answer_node 원본 prompt 받던 것 정정). 멀티턴 맥락 보존(`FIX-ANSWER-NODE-MULTITURN-CONTEXT` 계승), WS 토큰은 `coerce_message_text` 경로 무변경. 프론트는 step `kind` 필터라 노드명 변경 기능영향 0(fixture만 정리). ★ "최종 답변 경유를 supervisor LLM '선택'(가상 워커)이 아닌 라우팅 함수로 구조적 보장 — 검색후 FINISH 직행 우회 차단", ★ "단일 책임(최종답변) 노드 수렴으로 그래프 예측가능성 확보 + 가상워커 핵 제거로 컴파일 단순화" 교훈. gap-detector 92%(구현 100%, 테스트 16/19)→누락 TC-F07(사용자 컨텍스트 prepend 회귀)·TC-O01(`_collect_node_names`) 보강→100%. (무관 사전 실패: 미커밋 auth DI 미초기화로 tests/api 28·infra 30, 본 변경 파일과 무관) | [→](./final-answer-node/) |
