# Archive Index — 2026-07

> 이 디렉토리는 PDCA 사이클이 완료된 피처 문서를 보관합니다.

| 피처 | 완료일 | Match Rate | 테스트 | 경로 |
|------|--------|-----------|--------|------|
| recurring-feedback-promotion | 2026-07-21 | 98%(Check 97.6% → 로그 필드 gap 1건 즉시 해소) | 신규 16케이스(도메인 reinforce/add_support 5 + distiller 후보 매칭 5 + 서비스 강화/폴백 6) + 회귀 wiki·eval·memory 301 pass · 기존 12파일 확장 +348/−16 · **신규 파일 0 / 마이그레이션 0 / 프론트 diff 0 / DI 1줄** — **환류에 빈도 차원 추가**: 같은 주제 반복 👎가 별개 draft로 남발되던 것을 **기존 draft 강화**로 전환. distill 판정에 기존 CONVERSATION+DRAFT 후보(id,title, updated_at desc 상한 20 — 기존 find_by_agent dedup 조회 재사용, 추가 조회 0) 전달 → match 시 `add_support`(refs 지지 축적, 지지 수=len(refs), confidence `reinforce_confidence` +0.1/지지 cap 0.95, version+1, **제목·본문·상태 불변**) + `session.begin()` repo.update. `wiki_feedback_reinforce_enabled` 기본 off **독립 opt-in**(off면 candidates=None → 프롬프트·동작 바이트 동일, 기존 테스트 무수정 통과가 기준선 증거). **오매칭 2중 방어+폴백**: distiller `_parse_match_id`(후보 id 집합 밖 환각 id None 강등) + 서비스 `_reinforce`(실재·DRAFT 재검증, 실패 시 warning+신규 draft) — 판정 오류가 최악에도 기존 동작으로 수렴. 동일 메시지 재트리거는 refs dedup 선행 차단(지지 수 인플레 방지). ★ "기존 필드 재해석 — len(source_refs)=빈도, 스키마 없이 지지 수", ★ "오매칭 방어 공식: 입력 후보 집합 1차 + 저장 직전 재검증 + 안전 폴백", ★ 승인 큐 기존 노출(신뢰도·버전·출처)이 그대로 우선순위 신호(신규 화면 0). 이월: E2E(3플래그 on), STEP/CAP 튜닝(오매칭률 실측 후), websearch 환류, agent 경로 평가연동 | [→](./recurring-feedback-promotion/) |
| wiki-feedback-loop | 2026-07-21 | 99%(Check, 불일치 gap 0 — 권고 1건 즉시 해소) | 신규 22케이스(distiller 9 + service 8 + 팬아웃 5) + 회귀 wiki·eval·memory 230 pass · 신규 4파일 546 LOC + 수정 6파일 +168/−15 · **마이그레이션 0 / 프론트 diff 0 / 신규 라우트 0** — **환류 2단계: 개인(memory)→팀(wiki)**. `WikiSourceType.CONVERSATION`("대화 환류 Phase 3", LLM-WIKI-001) **예약석 최초 배선**. comment 있는 👎 저장 시 memory·wiki 팬아웃(`_is_actionable_negative` 순수 조건 + `_kickoff_feedback_fanout`, Q/A 복원 1회 공유, 둘 다 off면 조회 0회) → `FeedbackWikiDistiller`가 팀 일반화 가치 판정+정제 LLM 1회(worthy=false·파싱 실패 None, confidence /100 clamp·비수치 0.5 강등) → `FeedbackWikiService`가 draft 저장(DRAFT 하드코딩=자동 승인 불가, CONVERSATION, refs=[feedback:{message_id}], path="피드백", `refs_key` dedup을 **LLM 호출 전** 수행, `session.begin()` 명시 트랜잭션). `wiki_feedback_draft_enabled` 기본 off — memory 환류와 독립 opt-in. 결정 6건: ①super sentinel 그대로 귀속(WikiPage agent_id 자유 입력 실측) ②신규 인터페이스(기존 distiller 어댑팅 왜곡 회피) ③팬아웃+2번째 optional 의존성 ④path 고정 ⑤LLM 점수 매핑 ⑥프론트 diff 0('conversation' 타입·raw 렌더 기존재). ★ "예약석 채우기 — 설계 때 enum에 잡아둔 자리 덕에 도메인·마이그레이션·프론트 무변경 배선", ★ "스택 브랜치 — 미머지 PR(#42) 위 기능은 기반 feature 브랜치에서 분기", ★ "트리거 조건 순수 함수 분리 → 소비자 추가가 기존 테스트 무수정 통과로 검증". 이월: E2E(양 플래그 on), 반복 이유 빈도 집계 승격, websearch 환류, agent 경로 평가연동 | [→](./wiki-feedback-loop/) |
| eval-feedback-loop | 2026-07-21 | 100%(Check, Act 0회 — Minor 2건 문서 정정만) | 신규 ~20케이스(UseCase 트리거 10 + 서비스 kickoff_feedback 5 + extractor 블록 3 + 프론트 이유 패널 6) + 회귀 백엔드 196 pass(eval·memory·general_chat) + 프론트 51 pass(AdminDashboard+chat 10파일) · 12파일 +549/−29 · **마이그레이션 0 / 신규 라우트 0 / API 계약 무변경** — **측정(eval)→학습(memory) 첫 자동 환류 고리**. 👎 클릭 시 이유 칩 4종(부정확함·질문과 무관·근거 부족·형식/톤 불만)+자유 코멘트 수집(기존 comment VARCHAR(500) 재사용) → **이유 있는 👎만** 해당 턴 Q/A+이유를 부정 맥락으로 메모리 추출 kickoff(`[사용자 평가 신호]` 블록+"추측하지 마세요", 절단 후 부착) → PENDING 승인 합류(active 직행 금지·dedup·cap 동일). `eval_feedback_extraction_enabled` 기본 off, 매 턴 추출과 **독립 opt-in**. 결정 6건: ①Q/A 복원=find_by_session turn-1·USER(실패 시 warning+평가 저장 유지) ②extract() additive feedback_note ③cap 우회 없음 ④provenance=로그만(source_run_id=None) ⑤UseCase 내부 comment 있는 down 전이만(bare/동일 재제출/취소 배제) ⑥이유 UI=MessageFeedback.tsx 확장(훅·서비스·타입 계약 무변경). ★ **rev1 사용자 리뷰가 설계 교정** — "어떤 점이 싫은지 알아야 등록 판단 가능" → bare 👎 추측 추출 제거, 이유를 전제조건으로 상류 차단(할루시네이션 비용을 승인 큐에 전가하지 않음), ★ "필드는 있고 수집 UI만 없던" comment 계약 선행의 배당(프론트 컴포넌트 1개 수정), ★ Do 중 결함 발견·수정: `useSubmitFeedback` 전역 싱글톤 queryClient→`useQueryClient()`(Provider 불일치 캐시 유실, gap-detector "타당·권장"). 이월: E2E(flag on 필요), agent 경로 평가연동, wiki 환류 축(이유 신호 재사용), 전역 queryClient 전수 점검 | [→](./eval-feedback-loop/) |
| agent-eval-gate | 2026-07-20 | 97%(Check) | 백엔드 30 pass(domain 8 + repository 6 + use_cases 7 + eval_router 9) + general_chat 회귀 10 pass(G2 assistant_message_id 검증 2건 추가) · 프론트 6 pass(MessageFeedback 3 + AgentSatisfactionPanel 3) + useChatStream/streamRouting 회귀 그린 · 신규 백엔드 ≈544 LOC + 프론트 ≈219 LOC · 마이그레이션 1(V052) — growing-agent 7원칙의 **측정 게이트** 축 완성. 답변(assistant 메시지)에 👍/👎+코멘트 평가를 부착하는 신호 수집 계층. 이월 결정 4건: ①평가키=conversation message_id ②취소=행 삭제 토글(같은 rating+코멘트 없음 재클릭 시 delete) ③신규 `/api/v1/admin/eval/*` 집계 라우터(admin) ④ANSWER_COMPLETED에 `assistant_message_id` additive 노출(_persist_messages의 폐기되던 save 반환 id 캡처, 실패 시 None=FR-07). message_feedback(V052, UNIQUE(message_id,user_id)+INDEX(agent_id,rating)), EvalPolicy.satisfaction(0건 None), 프론트 feedbackId()로 히스토리 numeric id+스트리밍 assistant_message_id 양방향 해석(placeholder UUID는 자연히 미노출), AdminDashboard 만족도%+최근 부정 위젯. Thin DDD·컨벤션 100%, 회귀 0. ★ "폐기되던 save 반환값 캡처=데이터는 있고 노출 경로만 없던 패턴", ★ "취소=행 삭제로 상태 없음". 배포 전 필수: V052 적용. 이월: E2E 수동검증, agent(비 general-chat) 경로 평가연동 후속 | [→](./agent-eval-gate/) |
| document-template-extractor | 2026-07-02 | 95%(Check) → 99%(Act) | 백엔드 117 pass(document_extractor 관련) + 전체 회귀 그린, 프론트 22 pass(util 8 + sessionStorage/키생성 5 + 패널 9), tsc 0 error, 신규 백엔드 18 + 프론트 5 · 변경 백엔드 8 + 프론트 5 · 마이그레이션 1(V037) — "문서추출기(document_extractor)" 도구 하나에 두 단계. (A) 빌드타임 등록(workflow_compiler 무관): PDF/Word 업로드 → MCP pdf/doc→html → LLM 슬롯 추출·추천 → HITL 확정 → 프론트 {{key}} 토큰화(D2) → 에이전트 생성 트랜잭션 편승 저장(document_template). (B) 런타임 실행(workflow_compiler 경유): 전용 합성 노드가 지정 템플릿 로드 → 누적 컨텍스트로 슬롯 채움(LLM JSON {key:value\|null}, D6) → 순수 토큰 치환(재현성 100%) → MCP html→pdf/doc 파일 생성 → 다운로드 링크. ★ "LLM은 값 결정만·문서 변형은 순수 치환으로 재현성 확보", ★ "미근거=공란+하이라이트(GB6) 환각 금지", ★ "등록/실행 분리", ★ "유니크 인덱스 대신 앱 레벨 정합(active soft-delete 후 insert)으로 재등록 충돌 회피". 확정 결정 D1~D6 + GB6 100% 코드+테스트 반영, 아키텍처·컨벤션 100%. gap-detector 95% → Act로 프론트 G1(sessionStorage 드래프트 R4)·G2(슬롯 수동 추가·라벨 편집) 구현 → 99%. 배포 전 필수: 변환 MCP 실도구 등록 + /verify-mcp-connections PoC(G3), V037 DB 적용(G4), .env DOCUMENT_EXTRACTOR_*_TOOL_ID 설정 | [→](./document-template-extractor/) |
| doc-extractor-preview-highlight | 2026-07-03 | 96%(Check, Act 0회) | 백엔드 117 pass(document_extractor) + agent_builder 380 그린(신규 11: adapter options/warnings 5 + preview 이원화 5 + GB6 표식 1), 프론트 161 pass(agent-builder 영역, 신규 15+: 엔티티/분절/공백 매칭·스케일·D7 제외·FR-06 안내), tsc 0 error · 변경 백엔드 7 + 프론트 4 — 문서추출기 미리보기 **하이라이트 미표시 + HTML 크기 깨짐** 수정. 진단: **MCP 장애 아님** — MCP pdf_to_html text 모드 출력 특성(한글 전부 숫자 엔티티 `&#xae08;` + 폰트 런 span 분절 + 표/괘선 그래픽 소실 + 고정 pt 절대좌표)과 앱의 완전일치 매칭·무스케일 iframe 전제 불일치. 해결(A안 이원화): 미리보기=layout HTML(배경 PNG, dpi 120≈37KiB/page 실측 채택)·skeleton=text HTML 유지(D1), 어댑터 options/metadata.warnings(D2, 실패 시 None 폴백 D3), 프론트 DOMParser 정규화 매칭 재구현(D4 — buildTextIndex/findRange/groupSpans), mark 가시성 CSS 특이도 0,1,2(D5), pt 파싱→transform scale(D6), sessionStorage previewHtml 제외(D7), GB6 `[미기재]` 텍스트 병기(D8 — htmldocx 스타일 소실 실측 대응), config preview_mode/dpi(D9). ★ "PoC 실측이 설계를 확정"(PyMuPDF 엔티티 이스케이프 확증 → D4 필수 입증), ★ "실서버는 arguments 래퍼 거부 — 평면 폴백이 프로덕션 주 경로, options 보존 테스트로 고정", ★ 실서버 html_to_pdf 하이라이트 배경 #FFF3B0 보존 확인. ⚠️ 범위 밖 발견: **html_to_pdf(WeasyPrint) 한글 전부 "견" 글리프**(서버 한글 폰트 부재) → 후속 doc-convert-korean-font 필수 | [→](./doc-extractor-preview-highlight/) |
| nl-agent-composer | 2026-07-04 | 95%(Check, Act 0회 — Gap 2건 Check 내 즉시 수정) | 신규 테스트 38 pass(domain 정책 11 + composer 4 + use case 9 + router 4 + FR-08 mcp 7 + FR-09 프리필 3), 회귀 agent_builder 390 + domain 1,923 그린, verify-architecture/logging/tdd 위반 0 — **자연어 → 내부+MCP 도구 조합 에이전트 초안(무저장) API**. `POST /api/v1/agents/compose` 1회 호출: 후보 수집(내부 TOOL_REGISTRY + MCP tool_catalog 개별 도구, 카탈로그 미동기화 시 mcp_registry 서버 단위 메타 폴백 D2) → `AgentComposer` LLM 1회 structured output(역량 분해+coverage 근거+도구 선택+system_prompt+이름 제안, D3) → 서버 측 보정: 환각 tool_id drop(FR-06) → `mcp:{srv}:{tool}`→`mcp_{srv}` 매핑·서버 단위 병합(min sort_order) → MAX_TOOLS(5)/프롬프트 4000자 clamp(D7) → coverage(full/partial/none) 재산정 + `missing_capabilities[]`(사유+대안). **DB 쓰기 0건** — 저장은 기존 `POST /agents` 최소 확장: `CreateAgentRequest.system_prompt` 프리필(값 있으면 PromptGenerator 스킵, D1/FR-09) + `_build_skeleton_from_tool_ids` async 전환·`mcp_` 분기(FR-08, 기존 `get_tool_meta` ValueError 잠재 버그 해소). ★ "LLM은 제안, 서버가 판정(ComposePolicy 순수 도메인), 사람이 확정" 3단 구조, ★ 생성 공통단(ToolSelector/PromptGenerator)·실행부(workflow_compiler) 무수정 완전 신규 모듈, ★ Plan 단계 코드 리딩으로 저장 경로 mcp_* 거부 버그 선발견. 신규 src 6 + tests 6(1,443줄) · 변경 4(agent_builder schemas/use case, main.py DI, config `composer_max_candidates`) · 마이그레이션 0. 후속: E2E 수동 1회(compose→저장→run), 프론트 프리필 UI + `/api-cotract`(system_prompt 필드), MCP 개별 도구 단위 노출 제어(R1) | [→](./nl-agent-composer/) |
| compose-tool-instructions | 2026-07-05 | 95.7%(Check) → 100%(당일 보완, Act 0회) | 백엔드 447 pass(composer+builder, 신규 8) + 프론트 39+17 pass(신규 8) + tsc 0 error · 변경 백엔드 5 + 프론트 5(신규 유틸 1) — compose 초안의 **도구별 지침 생성 + [적용하기] 도구 미세팅 버그 수정**. ① composer `_WorkerOutput.instruction`(도구별 사용 지침 2~4문장) + system_prompt `[도구 지침]` 섹션 필수화(D1/D4: WorkerInfo 공용 확장, 저장은 프롬프트 병합 — 별도 스키마 없음), MCP 병합 시 instruction "; " 연결. ② 버그 근본 원인 = **도구 ID 이중 네임스페이스**: 카탈로그/폼(`internal:{id}`·`mcp:{srv}:{tool}`) vs 저장(`{id}`·`mcp_{srv}`) — compose 응답(저장 형식)을 폼(카탈로그 형식)에 그대로 주입해 체크 표시·RAG 부수효과 전부 조용히 실패. 해결: 프론트 `mapDraftToolIdsToCatalog` 유틸(D2/D3 — mcp_{srv}는 서버 소속 카탈로그 도구 전체 전개, 미매칭 원본 유지) + 백엔드 `_normalize_tool_id`의 `mcp:{srv}:{tool}`→도구명 오정규화 잠재 결함 수정(D5, `mcp_{srv}` + 동일 서버 중복 병합). ★ "compose 응답의 저장 호환 계약은 유지, 변환은 폼 경계에서" ★ gap 1건(RAG 부수효과 회귀 테스트 누락)은 Check 내 즉시 보완 → 100%. 선행 동일 세션: compose DI 버그 수정(SessionScoped→LlmModelRepository) + LangSmith `agent-composer` 프로젝트 추적 부착. 후속: 프롬프트 규칙 효과 LangSmith trace 실측, MCP 카탈로그 미동기화 폴백 UX | [→](./compose-tool-instructions/) |
| agent-schedule | 2026-07-03 | 96%(Check) → 98%(Act-1) | 백엔드 111 pass(domain 50 + infra 18 + application 26 + api 13 + 포크 회귀 4), verify-architecture/logging/tdd 전부 PASS, 신규 src 19 + 테스트 9 파일 · 마이그레이션 1(V038) · 의존성 croniter>=2.0 — 에이전트 스케줄 기능. **독립 도메인 `agent_schedule`**(별도 테이블 FK)로 CRUD 7종 API + **외부 트리거 실행 엔진**(`POST /api/v1/internal/schedules/trigger`, X-Scheduler-Token, 빈값 503/불일치 401). 반복 유형 once/daily/weekly/cron 4종(에이전트당 최대 10개, cron 최소 간격 10분 가드), **지침(instruction) R9** — `{today}`/`{now}`/`{weekday}` 시점 변수 치환 후 실제 사용자 질문으로 `RunAgentUseCase` 전송(새 세션 + user 메시지 자동 생성, ai_run 관측성 무료 획득). ★ **포크 미복사(R5) 구조 보장** — 포크 2경로(수동/자동)가 필드 화이트리스트 복사라 별도 테이블 스케줄은 자동 배제 + 시그니처 가드 회귀 테스트, ★ claim은 `FOR UPDATE SKIP LOCKED` + 조건부 UPDATE 선점으로 동시 트리거 이중 실행 구조적 차단(once 소진 시 자동 disable), ★ 결과 처리는 `ScheduleRunSink` 포트로 추상화(현재: 이력 테이블 + 새 대화 세션, 추후 알림 sink additive), ★ 트리거 UC는 lifespan 싱글턴이지만 session_factory만 보유(DB-001) — claim/실행/이력 각각 짧은 트랜잭션(RunAgentUseCase 선례), ★ misfire는 백필 없이 1회 실행 후 재계산, off→on 토글 시 next_run_at 재계산(과거 시각 즉시 폭발 방지). Act-1(사용자 A안): last_run_at = 성공/실패 무관 '마지막 실행 시도' 시각 + PLACEHOLDERS 상수화 + 문서 동기화. 배포 전 필수: V038 DB 적용, SCHEDULER_TRIGGER_TOKEN 설정 + 외부 cron 매 분 트리거 등록(.env.example 예시). 후속: agent-schedule-ui(프론트), SKIP LOCKED MySQL 통합 테스트 마킹 | [→](./agent-schedule/) |
| agent-instruction-required | 2026-07-06 | 99%(Check, Act 0회 — Gap 1건 Check 내 즉시 보완) | 백엔드 domain 173 + application 373 pass(핵심 파일 112), 프론트 type-check 0 error + agent-builder 컴포넌트 134 pass(18파일), 변경 파일 lint 신규 에러 0 · 백엔드 수정 7 + 삭제 5(테스트 4 동반) + 프론트 수정 4 — 에이전트 생성/수정 시 **지침(system_prompt) 필수화 + 레거시 자동생성 경로 전면 삭제**. `/agent-builder` "비워두면 AI가 자동 생성" 문구가 안내하던 백엔드 `PromptGenerator`(빈 지침 LLM 자동생성)·`ToolSelector`(도구 LLM 자동선택)·interview 흐름(엔드포인트 3종·유스케이스·세션스토어·Interviewer)을 코드베이스에서 완전 제거 — 자동 구성은 이미 있는 **Fix 에이전트(agent_composer)가 전담**. ① 지침 검증을 domain policy `AgentBuilderPolicy.validate_system_prompt`에 단일화(빈 값/공백 ValueError → 422), create/update 공용(update는 None=변경안함 유지). ② `MIN_TOOLS`/`MIN_WORKERS` 하한 제거로 **도구 0개(순수 대화형) 에이전트 허용** — `workflow_compiler`가 워커 0개 시 `quality_gate` 조건부 등록으로 고아 노드 방지, supervisor `FINISH`+answer 경로로 실행(compile+ainvoke 런타임 테스트로 검증). ③ 프론트 지침 필수 필드(빨간 뱃지)+빈 값 저장 차단 `role="alert"` 인라인 에러, placeholder 생성/수정 통일, create→update **2-call을 1-call로 통합**(생성 본문에 system_prompt 직접 포함). ★ "자동생성 책임을 Fix 에이전트로 일원화 → 예측 가능한 동작 + dead code 제거", ★ 0-worker 실행 경로는 supervisor_nodes 기존 지원(설계 §1.3 코드 사전검증), ★ Gap 1건(설계 §8.1 요구 0-worker ainvoke 런타임 테스트 Do 누락)을 Check에서 즉시 추가 → 99%. ⚠️ **breaking API change**(system_prompt 필수화) → 백엔드·프론트 동일 PR 배포 필수. 후속(선택): `src/claude/task/task-custom-agent-builder.md` 폐기 문서 아카이빙(grep 노이즈 정리) | [→](./agent-instruction-required/) |
| knowledge-base-scoping | 2026-07-07 | 98%(Check, Act 0회 — High/Medium Gap 0건) | 신규 56 pass(domain policy 29 + application 23 + extra_metadata 4) + 라우터 포함 배치 33 pass, 기존 회귀 0건, verify-architecture/tdd 신규코드 위반 0 · 신규 파일 12 + 기존 additive 수정 4 · 마이그레이션 1(V040) — 벡터DB **물리 컬렉션(관리자 전용)/논리 지식베이스(사용자, payload 필터 단위) 분리**를 신규 경로로 병행 도입(기존 경로 무수정, 추후 갈아끼기). 사용자별 물리 컬렉션 난립 → 공유 곤란·권한이 컬렉션 단위로만 → 컬렉션 생성은 `POST /api/v1/admin/collections`(require_role('admin'), 기존 CollectionManagementUseCase 위임)로 제한, 사용자는 `knowledge_base`(kb_id UUID PK) 논리 단위 생성 → 문서 업로드 시 청크 payload에 `kb_id`/`kb_name` 주입해 **Qdrant payload + ES 문서 양쪽 격리**. 메타는 **MySQL 1차 소스 + payload 이중 기록**(추후 payload 단독 필터 대비), 권한은 기존 3단계 스코프(PERSONAL/DEPARTMENT/PUBLIC) 재사용하되 `KnowledgeBasePolicy` 독립 판정(collection_permissions 불연동, PUBLIC 쓰기=소유자+ADMIN만 D8). ★ **필터 키=불변 UUID kb_id**(이름은 kb_name 표시용 동봉 → rename 시 벡터 재태깅 불필요, D1), ★ **물리 컬렉션 배정을 `CollectionAssignerInterface`로 추상화**(현재 UserSelected=사용자 선택+읽기권한 검증, 추후 AdminMapped로 구현체 교체, D6), ★ **업로드 재사용 = `UnifiedUploadRequest.extra_metadata` optional additive 필드** + execute/`_store_to_es` 양곳 `setdefault`(고정키 우선) — 기존 호출·테스트 무영향(D2/D3), ★ ES는 화이트리스트 body라 매핑(kb_id/kb_name keyword)+코드 병합 2곳 필수(Qdrant는 metadata 전체 복사라 자동 전파), ★ **soft delete**(status active/deleted)로 kb_id 추적 보존 — (owner,name) DB 유니크 없이 active 중복만 UseCase 차단(V037 선례). 이번 범위=KB CRUD + 지정 업로드 신규 경로. 의도 편차 3건(create not-found→422 §7.1 명세대로 / find_all_active 추가 ADMIN 전체조회 / D10 payload index·D11 activity log·D12 문서수 집계 이연) 감점 제외. gap-detector 98%(Low 4건=문서 표기 수준). 배포 전 필수: 기존 ES 인덱스 `PUT /{index}/_mapping` kb_id/kb_name keyword 1회 선반영(Design §6.5), V040 DB 적용. 후속: kb-rag-filter(RagToolConfig.metadata_filter에 kb_id 연동), kb-vector-cleanup(삭제 시 Qdrant delete-by-filter+ES delete-by-query), collection-path-migration(기존 경로 차단·백필·구 API deprecated), 프론트엔드 UI | [→](./knowledge-base-scoping/) |
| clause-aware-chunking | 2026-07-07 | 99%(Check, Act 0회 — Low Gap 3건 중 2건 Check 내 즉시 해소) | 신규 테스트 94 pass(domain policy 25 + strategy 19 + profile use case 14 + resolver 8 + kb upload 3 + unified config 5 + admin router 10 + profile router 1 + kb router 9) + 관련 기존 스위트 223 회귀 0건, verify-architecture 5/5 · verify-tdd · verify-logging PASS · 신규 파일 12(1,170 LOC) + additive 수정 8 · 마이그레이션 2(V041 프로파일+시드, V042 KB additive) — 벡터DB 적재를 **①조·항 의미 경계 분할 → ②초과 시 토큰 분할 → ③overlap** 파이프라인으로 전환하는 `ClauseAwareStrategy`를 신규 경로로 병행 도입(기존 parent_child 하드코딩 무수정, opt-in). 문서 구조 무관 토큰 분할로 조·항이 청크 중간에서 잘리던 문제 → 경계 규칙(정규식)은 **`chunking_profile` 테이블에서 관리자 CRUD**(`/api/v1/admin/chunking/profiles`, require_role admin), 토큰/overlap 기본값은 프로파일에 내장(is_default 전역 기본), **KB 단위로 사용자 오버라이드**(use_clause_chunking opt-in + profile_id/size/overlap NULL=late binding). ★ **조=parent·항/호=child 매핑으로 기존 parent_child 메타데이터 계약(chunk_type/parent_id/children_ids/chunk_index/total_chunks) 완전 복제 → 하이브리드 검색 코드 무수정**(D9), ★ **파서가 페이지당 Document 산출 → 조가 페이지 경계 넘음** 발견 → 전략이 페이지 결합+누적 offset으로 page_start/page_end 산출(동일 본문 조 중복도 정확, D8/G1), ★ 업로드 재사용 = `UnifiedUploadRequest.chunking_config` optional additive + `_build_strategy` 분기(None이면 parent_child 완전 불변, 회귀 가드 테스트 D10), ★ 프로파일 삭제/부재 시 default→legacy 폴백으로 **업로드 항상 성공**(FR-07/D11), ★ 관리자 정규식 ReDoS 방어 = re.compile 검증+패턴 200자/규칙 50개 상한(D12), ★ SQL 시드 패턴은 backslash 없이 문자 클래스로 작성(MySQL 이스케이프 회피), ★ 전략은 컴파일된 패턴 주입으로 DB 무접근(Thin DDD). 이번 범위=신규 전략+관리자 CRUD+KB 연동+업로드 opt-in(백엔드만). gap-detector 98%→G1(find 역탐색→누적 offset 리팩터+회귀테스트)·G2(D6 Query무시 OpenAPI description) 즉시 해소 99%, G3(시드 패턴 전각괄호·공백 변형)은 설계가 Do 체크리스트로 명시 이연(프로파일 UPDATE, 코드무관). 배포 전 필수: V041/V042 DB 적용 + 시드 패턴 실제 규정 PDF 검증. 후속: chunking-frontend(관리자 화면+빌더 UI), chunking-default-migration(기본 경로 교체), document-rechunk(재청킹), 표 처리 최적화(D13 이연) | [→](./clause-aware-chunking/) |
| rag-routed-integration | 2026-07-09 | 96%(Check) → 99%(Low 3건 즉시 해소, Act 0회 — High/Medium 0) | 백엔드 신규 17 pass(도구 분기 11 + 팩토리 3 + VO 3, test_tools_routed.py 220 LOC) + agent_builder·rag_agent 전체 686 그린(**기존 도구 테스트 무수정 통과 = 기준선 증명**) + 프론트 RagConfigPanel 9 pass(신규 3)·tsc 0 · 백엔드 수정 5 + 프론트 수정 3, **신규 프로덕션 파일 0 · 마이그레이션 0 · config 0 · search_mode 체계 무수정** — **에이전트 RAG 도구에 라우팅 검색 opt-in 연결(4부작의 첫 실사용처, 풀스택)**. ★ **사용자 피드백으로 Plan 개정**: 최초안(search_mode에 "routed" 값 추가)은 기존 검색 방식을 대체(갈아끼우기)해 교차검증 불가 → **독립 필드 `RagToolConfig.use_routed_search: bool=false`**(use_wiki_first 동형)로 전환 — 에이전트 생성 시 선택→tool_config JSON 저장→도구 분기, 기존 설정과 직교 병존. ★ **강등의 목적지 = 그 에이전트의 기존 search_mode 경로**(범용 폴백 아님) — off 기준선과 강등 동작이 일치해 동일 구성 토글 쌍으로 A/B 교차검증 성립. 강등 4사유 reason 코드(`not_wired`/`filter_incompatible`/`error`/`empty`) warning 관측. ★ **필터 3분류(D4)** — `_apply_auth_filter`가 visibility 또는 viewer_department_ids를 **항상 주입**한다는 코드 확인이 "부재 키 전부 강등→상시 강등" 함정을 사전 차단: `kb_id`→RoutedScope 매핑 / `viewer_department_ids`→무시(기존 hybrid도 실효 없음, 동일 취급) / `visibility`·custom→강등(권한 누수 방향 원천 차단 — visibility 강제 사용자는 항상 기존 경로에서 권한 필터 적용, 테스트 검증). 결과 포맷 `[출처: 문서명 > 조 제목]`+`요약:` 1줄(150자 절단)+조 본문(내장 폴백은 기존 포맷), `collected_sources`·`record_retrieval` 기존 계약 동형(metadata `search=routed`/`from_fallback` — RRF 점수 스케일 구분). multi-query는 routed 성공 시 원 질의만(우회)·강등 시 원래 규칙. RoutedParams는 top_k/rrf_k 파생(신규 설정 0). ToolFactory `routed_retrieval_getter` additive(미들웨어 팩토리는 RAG 비실사용 컨텍스트로 의도적 제외 — not_wired 안전 강등). 프론트 토글 "라우팅 검색 (3계층 요약)"(자동 전환·교차검증 안내), 기존 검색 모드 라디오 불변. Gap 3건(전부 Low): D2 문구 정정·error 강등 exception= 추가·VO 테스트 보강 — Check 내 해소. ⚠️ 제약: Update가 tool_config 미갱신이라 토글은 **생성 시에만** 설정 가능(후속 agent-tool-config-update). 후속: **교차검증 실측(토글 on/off 에이전트 쌍)**, routing-quality-eval(RAGAS), section-summary-backfill(강등률 기반) | [→](./rag-routed-integration/) |
| summary-routed-retrieval | 2026-07-09 | 95%(Check) → 99%(Medium 1 + Low 3 즉시 해소, Act 0회) | 신규 43 pass(정책 17 + 오케스트레이터 6 + 단계 구현 11 + 필터 확장 5 + 라우터 4, 667 LOC) + 회귀 0(기존 검색 hybrid/retrieval/collection_search·retriever·vector·요약 파이프라인 237+74 그린) · 신규 12파일(776 LOC: domain 4·app 2·infra 5·api 1) + additive 수정 3 · **마이그레이션 0 / config 0 / 기존 검색 수정 = 동작 보존 필터 확장 1쌍** — **검색 파이프라인 4부작 완결: 3계층(document_summary→section_summary→rawchunk) 하강 라우팅 리트리버** 신규 API. `POST /api/v1/retrieval/routed`: 임베딩 1회 → ①문서 요약 벡터 top-K(kb_id 필터, 가드 bypass) → ②선별 문서 내 섹션 요약 벡터(document_id MatchAny)+ES BM25(`summary_text^1.5`/`summary_keywords`) **기존 RRFFusionPolicy 무수정 재사용** 병합 top-N → ③`section_ref`를 **ES ids query 1회**로 조(parent) 본문 확장 → 부족 시 기존 HybridSearchUseCase 폴백(getter 주입, 조 단위 dedup — chunk_id/parent_id 매칭, 실패 시 라우팅 결과 보존). **LLM 0회 결정론**(임베딩 1회 + Qdrant 2 + ES 2 IO 상한), 응답에 라우팅 근거(문서/섹션 요약·단계별 점수·source)와 `fallback_used`/후보 수 관측 동봉(튜닝·RAGAS 입력). ★ **단계 3포트(DocumentRouter/SectionRouter/ChunkExpander) + 순수 VO 계약** — "각각 분할과 변경에 용이하게" 요구를 인터페이스로 구현, fake 3포트 교체 테스트가 수용 기준, ★ **선행 사이클 설계의 배당** — 결정적 uuid5(2단계 D5)가 요약 계층 Qdrant point id=ES `_id` 3자 일치를 보장해 RRF 병합 키 자연 정합(source="both" 검증), rawchunk의 point id≠chunk_id 함정은 ES `_id` 직조회로 회피, 가드 bypass(3단계 D12)가 라우터 필터를 그대로 통과, ★ `SearchFilter.metadata_any` additive(다중 값 MatchAny) — equality·요약 가드 불변 회귀 테스트 2종 고정, ★ 섹션 라우터 소스별 graceful 강등(벡터/BM25 한쪽 실패 시 유지), ★ 점수 체계 주의: RRF 스케일과 폴백 점수는 절대 비교 불가(혼합 정렬 필요 시 정규화 재설계). Gap 4건: D7 응답 구조(routing 래퍼→평탄 필드, 코드=진실 문서 갱신)·D1 시그니처·D12 fallback_used 로그·execute 40줄(`_descend` 분리) — Check 내 전부 해소. 후속: **E2E 실측(K/N/weights 튜닝)**, rag-routed-integration(에이전트 RAG 도구 opt-in), routing-quality-eval(RAGAS 비교), section-summary-backfill(fallback_used 관측 기반), document-router-hybrid/LLM 리랭커(포트 교체 지점) | [→](./summary-routed-retrieval/) |
| document-summary-routing | 2026-07-09 | 98%(Check) → 99%(Low Gap 3건 즉시 해소, Act 0회 — 데이터 계약/흐름 100%) | 관련 스위트 110 pass(신규/확장 ~25: step 11 + 정책 8 + 체이닝 4 + 극단 케이스) + 회귀 0(2단계 전체 + KB/프로파일/라우터 77 + 격리 영향권 320 그린) · 신규 1파일(`document_summary_step.py` 322 LOC) + additive 수정 12 · **마이그레이션 0 / 신규 API 0 / ES 매핑 변경 0**(설계 목표 gap-detector 코드 확인) — 문서 등록 파이프라인 **3단계: 섹션 요약 전량 집계 → 문서 단위 요약 자동 체이닝. 1·2·3단계 3부작 완결(rawchunk→section_summary→document_summary 3계층 라우팅 데이터 완성)**. 섹션 요약 잡 completed 전이 직전 러너가 `DocumentSummaryStep`(optional 주입, None=기존 동작 완전 불변) 실행: 섹션 요약 전량(제목+3줄, chunk_index 순) → cap(24000자) 이내 LLM 1회 / 초과 시 연속 구간 배치 중간 요약(5줄)→최종의 2계층 map-reduce(배치 10개 초과는 명시 실패 — 조용한 탈락 금지) → 동일 컬렉션 `chunk_type='document_summary'` 1 point/문서(`uuid5("document-summary:{document_id}")` 멱등 upsert, ES 먼저→Qdrant 마지막) + ES 기존 요약 필드 재사용. 문서 키워드 = 섹션 키워드 빈도 집계 상위 15(`aggregate_keywords`, **LLM 0회 결정론적**). completed 의미 = 섹션 전량+문서 요약 성공(error prefix "document summary failed:"로 단계 구분). ★ **재시도 free win** — 별도 코드 0줄: 기존 done_refs 스킵+failed==0 재진입이 "섹션 LLM 0회 + 문서 요약만 재생성"을 자동 성립(2단계의 '완료 판정=point 존재' 결정의 배당, 테스트로 고정), ★ **"잡 확장이지 신규 잡이 아니다"** — 상태 머신·재시도 API·stale 판정 전부 재사용해 변경 표면 최소화, ★ 요약의 요약이라 LLM 비용이 문서 크기와 무관하게 상수화, ★ 격리 가드를 요약 타입 집합(`_SUMMARY_CHUNK_TYPES` + MatchAny 1조건)으로 일반화 — 이후 계층 추가 시 집합 원소만, 명시 필터 bypass 유지, ★ `parse_summary_json` 공용 승격(섹션 요약자 위임 — 동작 불변). Gap 3건(전부 Low): FR-11 llm_model_id 로그·중간 요약 줄 수 상수화·단일 블록>cap 방어 절단 — Check 내 즉시 해소. 운영: 추가 설정 불필요(2단계 V043/V044 + 프로파일 요약 모델 지정만으로 3단계까지 자동). 후속: **summary-routed-retrieval(계층 하강 라우팅 검색 — 착수 조건 충족)**, section-summary-frontend, section-summary-backfill, routing-quality-eval(RAGAS) | [→](./document-summary-routing/) |
| card-section-summary | 2026-07-08 | 98%(Check) → 99%(경미 Gap 3건 즉시 해소, Act 0회) | 신규 75 pass(도메인 정책 23 + 러너/런처/조회 31 + 인프라 15 + 킥오프 5 + 라우터/프로파일 추가분) + 관련 기존 스위트 485 회귀 0건(사전 실패 8건은 stash 검증으로 판별), verify-architecture 신규 위반 0 · verify-logging · verify-tdd PASS · 신규 파일 14(Python 12개 1,211 LOC + V043/V044) + additive 수정 15 — 문서 등록 파이프라인 **2단계: 카드 섹션(=clause-aware parent 조) 키워드+3줄 요약 백그라운드 생성·저장**. 업로드 완료 후 인프로세스 잡(`asyncio.create_task` 코드베이스 최초 도입)이 섹션당 LLM 1회(structured output→JSON 프롬프트 폴백 1회 재시도, vLLM 대응) 호출 → 동일 Qdrant 컬렉션 `chunk_type='section_summary'`(결정적 uuid5 ID 멱등 upsert, `section_ref`=원 parent chunk_id로 rawchunk 연결) + ES 전용 필드(`summary_text`/`summary_keywords`, content/morph_* 미기재 → BM25 구조적 미노출) 저장. 요약 LLM은 `chunking_profile.summary_llm_model_id`(V043, NULL=비활성 opt-in)로 관리자 지정 — 기존 `llm_model`(base_url vLLM) 인프라 재사용, 저장 시 존재+활성 422 검증. 잡은 문서당 1행(V044, UNIQUE(document_id), 실행 스냅샷) + 상태 API `GET .../documents/{id}/section-summary`(진행률·is_stale) + `POST .../retry`(202/409, **완료 섹션은 Qdrant point 존재로 판정해 실패분만 재처리** — 섹션별 상태 테이블 없이 멱등 재시도). ★ **Plan 가정 반증이 설계를 바꿈**: "Qdrant는 child 필터로 자동 격리" 가정이 코드 확인(하이브리드 벡터 측 무필터)에서 틀림 판명 → 경로별 수정 대신 **Qdrant 어댑터 `search_by_vector` 단일 초크포인트 must_not 가드**(명시 `chunk_type=section_summary` 필터 시 해제, 기존 데이터에 요약 없어 동작 보존), ★ "ES 먼저→Qdrant 마지막(완료 마커)" + 결정적 ID 조합 = 상태 최소 저장 멱등 재시도 패턴, ★ 장수명 러너/런처는 `SessionScopedSectionSummaryJobStore`(호출별 독립 짧은 세션, DB-001)로 세션 관리 캡슐화, ★ 카운트 UPDATE의 updated_at이 heartbeat 겸 stale 판정 근거(서버 재시작 고아 복구), ★ `SectionSource` 인터페이스로 섹션 단위(조→장·절) 교체 지점 확보. KB 업로드 `execute` 2→3튜플(킥오프 정보, 호출부 전수 확인 후 변경) + 응답 `section_summary` 필드. doc_browse 열람 제외 post-filter. config 4종(동시성 3/입력 절단 12000자/stale 600s/섹션 상한 500). gap-detector 98% → G1(payload 상위집합)·G2(analyzer 오기) 문서 정정 + G3(FR-11 profile_id 로그) 코드 수정 → 99%. 배포 전 필수: V043/V044 적용(ES 필드는 기동 시 put_mapping 자동). 후속: document-summary-routing(3단계 문서 요약→1차 라우팅), summary-routed-retrieval(요약 검색→section_ref rawchunk 확장+키워드 검색), section-summary-frontend(프로파일 모델 UI+진행 상태 표시, `/api-cotract`), section-summary-backfill(기존 문서 일괄+cron 보정) | [→](./card-section-summary/) |
| agent-recursion-limit | 2026-07-08 | 98%(Check, Act 0회 — 기능 Gap 0건, Low 3건=테스트 배치/커버리지 뉘앙스) | 신규 테스트 38 pass(IterationLimitPolicy 18 + 실행 경로 20) + agent_builder 도메인/앱/인프라 637 + API 라우터 23 = 660/660, 회귀 0, TDD Red→Green 준수 · 마이그레이션 1(V045) + 소스 15 수정(도메인 2·앱 11·인프라 2) + 테스트 5 파일 — 에이전트 그래프 실행 **반복 한도의 에이전트별 설정 + 한도 도달 시 조기 답변** 도입. 문제: LangGraph `recursion_limit` 미설정(시스템 기본 25스텝)이 자체 가드(하드코딩 10, 반복당 3~5스텝 소비)보다 **먼저 발동해 GraphRecursionError → RUN_FAILED**(답변 불능) + 한도 도달 시 수집 정보 전부 폐기. 해결: `agent_definition.max_iterations`(V045, 기본 25, 10~1000)를 단일 소스로 `SupervisorConfig` 주입 + `graph_config["recursion_limit"] = ×10+20` 파생 설정(state 가드 선발동 보장) → 도달 시 supervisor가 `limit_reached` 플래그 → `route_to_worker_or_final`이 워커 미실행이라도 `final_answer` 경유 → 안내 지시 포함 답변 + `ANSWER_COMPLETED.limit_reached` payload 플래그(True만 부착, charts 선례 동형·이벤트 카탈로그 무변경) → RUN_COMPLETED. ★ **`IterationLimitPolicy` 상수 1곳 집약**(DEFAULT/MIN/MAX/파생 계수/sub-agent 계수 — "문제 시 즉시 변경" 요구 반영), ★ sub-agent는 부모 절반(하한 5) 상속 + **서브 그래프 `ainvoke`도 config 미전달로 동일 결함 보유 발견 → recursion_limit 파생 전달**, ★ GraphRecursionError 최후 안전망 — 축적 메시지(`_StreamState.final_messages`)로 강등 답변 시도, 불가 시에만 RUN_FAILED("오류 대신 답변" 원칙), ★ token_limit 가드는 현행 유지(범위 분리). API는 create(기본 25)/update(None=변경 안 함)/get 응답 additive + fork/auto-fork 승계. 배포 전 필수: V045 DB 적용. 후속: Studio UI 입력 필드(idt_front)+`/api-cotract`, (선택) 라우터 422 통합 테스트·token_limit 동일 조기 답변 검토 | [→](./agent-recursion-limit/) |
| kb-rag-filter | 2026-07-10 | 92.9%(Check) → 97.6%(Act-1) | 백엔드 신규 36 pass(Do 24: VO 4 + ToolFactory 5 + create 8 + update 5 + schemas·전구간 / Act-1 12: TestCanReadRef 7 + create 403 3 + update 403 2) + agent_builder/knowledge_base 740 그린(MCPRouting 3건 기지 Windows 이벤트루프 이슈 제외), 프론트 신규 11 포함 287 pass(38파일) + tsc 신규 에러 0 · 백엔드 수정 8 + 프론트 수정 7 · **마이그레이션 0 / 신규 API 0 / 검색 경로 무수정** — **agent-builder 내부문서검색 도구에 논리 지식베이스(KB) 필터 opt-in 연동(knowledge-base-scoping 후속 1, 풀스택)**. 물리 컬렉션 드롭다운만 노출되어 "단일 컬렉션 + kb_id 메타데이터 격리" 구조의 소비자 연결이 누락 → `RagToolConfig.kb_id` first-class 필드(D5 무검증 VO) + 프론트 KB 드롭다운을 **기존 컬렉션 드롭다운과 병행 신설(독립 opt-in, 교차검증 기준선 보존)** — KB 선택 시 컬렉션 disabled+안내, 비공개 KB scope 자동제한 안내, 요약 배지 KB 라벨 우선. 저장 시점: KB 존재(400)/읽기권한(403) 검증 + 물리 컬렉션 canonicalize(D1: `tool_config.collection_name ← kb.collection_name`) + KB scope→visibility clamp(D7: kb 워커는 물리 컬렉션 perm 조회 제외, kb_repo 미주입 시 명시 에러). 런타임: `ToolFactory._merge_kb_filter`가 kb_id를 metadata_filter에 우선 병합(D2/D6) — 도구·검색 경로(hybrid payload 필터/routed `_ROUTED_SCOPE_KEYS` RoutedScope) 완전 무수정 재사용. ★ **저장시점 canonicalize 결정 근거 = 코드 증거 3종**(sync `ToolFactory.create`·KB 이름변경 API 부재·clamp이 어차피 저장 시 KB 조회), ★ Act-1(G1): `KnowledgeBasePolicy.can_read_ref` 신설(can_read 위임 — 규칙 단일 소스) + `_resolve_kbs`/`_lookup_kb_scopes` 권한 검증(admin bypass, 타인 PERSONAL/미소속 DEPARTMENT → PermissionError→403) + update 라우터 403 detail `str(e)` 표면화, TDD Red 10건→Green. ⚠️ 잔여 G2: E2E 수동검증 pending(KB A/B 격리 검색 + Qdrant payload kb_id 실측 — 백엔드+Qdrant/ES 기동 필요). 비고: update는 visibility 변경 시에만 KB 재검증(도구 재배선 없음 — 수용). 후속: kb-management-ui(KB CRUD 화면), collection-picker-retirement(컬렉션 드롭다운 은퇴+마이그레이션), kb-orphan-cleanup(soft-delete KB 벡터 정리) | [→](./kb-rag-filter/) |
| retrieval-observability | 2026-07-10 | 100%(Check, Act 0회 — 41/41, 차단성 Gap 0 · advisory 1건 타입힌트 즉시 반영) | 신규 5파일 40+ pass(tracker 확장 + per_query_hits + tool 컨텍스트 + general_chat 라이프사이클 + 조회 API) + 회귀 domain/application 3,628 그린(tests/api 간헐 WinError 10014는 stash 기준선 동일 재현 = 기존 환경 이슈) · 프로덕션 15파일(신규 2: V046, 조회 UseCase) · 마이그레이션 1(V046 nullable 8컬럼 additive) — **질문별 검색 근거 기록: general_chat 관측성 배선 + rewrite 쿼리 추적 + 스키마 확장 + 메시지 기준 조회 API**. "어떤 질문에서 벡터/하이브리드 검색이 어떤 문서를 뽑았는지" 기록 부재 → 조사 결과 **신규 구축이 아니라 배선 갭**(agent-run-observability가 `ai_retrieval_source`+`RunTracker` 기구축, general_chat만 tracker 미주입 사각지대). ① `get_run_tracker()` lazy singleton(main.py) — agent_builder·general_chat(HTTP/WS 공용) 동일 인스턴스, general_chat tool+UC 주입. ② general_chat ai_run 라이프사이클: **D2 deferred attach**(start_run user_message_id=NULL → 메시지 저장 후 `attach_user_message` UPDATE — 대화 저장 시점·turn_index 불변 + FK 락 Error 1205 원천 회피), D3 sentinel `agent_id="general-chat"`(FK 없음), D1 chart-edit 분기 이후 run open + start_run 실패 시 degraded 진행, D4 UsageCallback config 부착(LLM 비용 기록 부수 획득). ③ rewrite 추적: `MultiQueryResult.per_query_hits` 신설(쿼리별 hit id, 기존 state에서 버려지던 값 회수) → tool이 fused hit별 `search_query`=첫 기여 쿼리 + `metadata.matched_queries` 태깅(D6, 행 폭증하는 쿼리×hit 쌍 기각). ④ V046: search_query/query_source(original\|multi_query)/search_mode(hybrid\|bm25_only\|vector_only\|routed, D9 직교)/bm25·vector 개별 점수·rank/fusion_source — **개별 점수는 `HybridSearchResult`에 이미 있었으나 기록 시 유실되던 값**(getattr 전달만, D7 routed는 NULL 유지 — 점수 스케일 혼합 금지). ⑤ `GET /api/v1/conversations/messages/{id}/retrievals`(agent_run_router, D8): 소유 검증(본인/admin, 404/403) → run별 → 재작성 쿼리별 그룹 + LangSmith run URL. ★ "관측성 작업의 상당 부분은 새 계산이 아니라 기존 값의 전달 경로 확보", ★ additive-only(전 컬럼 nullable·kwargs optional — 기존 agent_run 경로 테스트 무수정 통과), ★ best-effort 계약 유지(기록 실패는 warning만, 채팅 차단 없음). 배포 전 필수: **V046 DB 적용**. 후속: collection_search/wiki/hybrid 직접 API 배선, 프론트 근거 표시 UI(+`/api-cotract`), 보존 정책 | [→](./retrieval-observability/) |
| kb-management-ui | 2026-07-10 | 93.8%(Check) → 96.9%(Check 내 3건 즉시 해소, Act 0회) | 백엔드 신규 12 pass(UseCase 6 + 업로드 kb_id 2 + repo 4) + 회귀 506+53 그린(workflow_compiler 35 에러는 기지 Windows 이벤트루프 교차실행 이슈, 격리 통과), 프론트 신규 15 pass(목록 6 + 생성모달 3 + 상세 6) + 회귀 20 + tsc 0 · 백엔드 신규 2 + 수정 8 + **마이그레이션 1(V047)** · 프론트 신규 9 + 수정 7 — **지식베이스(KB) 관리 화면 `/knowledge-bases`(knowledge-base-scoping·kb-rag-filter 후속, 풀스택 1일 사이클)**. 백엔드 KB API 완비·agent-builder KB *선택* UI 존재에도 KB를 *만들고 문서를 넣는* 화면 부재 → 독립 페이지(목록·생성·삭제·상세 문서목록·업로드) + 사이드바 항목. 사용자 확정 4건: 범위=CRUD+업로드 / rename 제외(PATCH 부재) / use_clause_chunking 토글만(프로파일 목록 admin 전용→late-binding) / 독립 페이지. **유일 신규 API `GET /knowledge-bases/{kb_id}/documents`** — KB가 물리 컬렉션 공유라 컬렉션 문서목록 재사용 시 타 KB 문서 노출(Plan 조사 발견) → D1: 문서목록 진실소스 MySQL `document_metadata`에 V047 kb_id nullable 컬럼(ES 집계안 기각: filename/created_at 부재+페이지네이션 부적합), D2: unified upload 저장부 1줄(`extra_metadata.get("kb_id")`, 일반 업로드 None 불변 회귀가드), D3: 권한=기존 `KnowledgeBaseUseCase.get()` 위임(403/404 상속, 규칙 단일소스 — kb-rag-filter Act-1 원칙 계승), D4: V047 이전 kb_id NULL 문서 미표시(기지 한계, API description 명기). 프론트는 CollectionPage/UploadDocumentModal 선례 복제(공용 Modal/ConfirmDialog/Dropdown·인라인 에러·KB_ERROR_MAP), mutation 시 `.all` invalidate로 agent-builder 드롭다운 자동 동기화(FR-07), D5: `KnowledgeBaseInfo` 제자리 additive(무회귀), D7: 사이드바 startsWith 활성, D8: 삭제 owner/admin gating. ★ Check 내 즉시 해소 3건: scope 라벨 정합(코드=진실소스로 설계 정정 + `KB_SCOPE_LABELS` dead code 제거)·DetailPage disableClose/섹션요약 뱃지 테스트 2건 추가·repo update 경로 kb_id 방어 1줄. ⚠️ 배포 전 필수: **V047 DB 적용**. 잔여 G3(기지): E2E 수동검증 — kb-rag-filter G2와 묶어 백엔드+Qdrant/ES 기동 시 일괄 실측. 후속: kb-rename(PATCH), kb-document-management(문서 삭제·청크), section-summary-status-ui(진행률 폴링/재시도), collection-picker-retirement, kb-orphan-cleanup | [→](./kb-management-ui/) |
| agent-builder-edit-mapping | 2026-07-13 | 97%(Check, Act 0회 — 유일 Gap Low 1건=페이지 프라임 통합테스트) | 백엔드 신규 6 pass(domain apply_update 2 + TestUpdateLlmModel 4) + domain 199/app 409/infra 36 그린(교차실행 에러는 기지 pytest-randomly+이벤트루프 이슈, `-p no:randomly` 전체 통과), 프론트 신규 19 pass(agentDetailMapping 유틸 14 + LeftConfigPanel 라벨/배너 5) + Studio 회귀 19 + tsc 변경파일 0(기존 101건 무관·1건 BASE_FORM schedules 해소) · 백엔드 수정 5 + 프론트 신규 1 + 수정 3 · **마이그레이션 0** — **/agent-builder 수정 화면 매핑 정합 + 모델 변경 저장(FR-5)**. 수정 진입 시 ①모델이 DB id(UUID) 노출(`llm_model_id`를 model_name 기준 조회 폼에 무변환 대입) ②도구함 0건(카탈로그 `internal:{id}`·`mcp:{srv}:{tool}` vs 저장 `{id}`·`mcp_{srv}` 이중 네임스페이스 무변환 — compose 경로만 변환 존재) ③RAG tool_config 폐기 ④모델 변경 silent discard(UpdateAgentRequest에 필드 자체 부재). 해결: 순수 함수 `mapDetailToForm(detail, models, catalogTools)`(utils/agentDetailMapping.ts 신규)로 프라임 일원화 — 모델 역매핑(실패 시 raw 유지+`(미등록 모델)` 라벨), 기존 `mapDraftToolIdsToCatalog` 재사용, RAG worker tool_config DEFAULT 머지 복원, 서브에이전트/스킬 회귀 고정(조사 결과 정상). 프라임은 쿼리 3종 settled 후 `primedAgentRef` 1회 가드(재시도 성공 후 미재프라임=편집 보호, 기지 한계). FR-5: `UpdateAgentRequest.llm_model_id`(None=무변경) + UseCase `_validate_llm_model`(create와 동일 규칙) + domain `apply_update` 파라미터 + **repository update() 컬럼 대입 1줄**(화이트리스트 구조라 누락 시 조용히 미저장 — 설계 조사에서 선발견) + main.py DI. ★ "수정 가능 필드 추가 = 스키마+apply_update+repo update()+DI 4곳 세트", ★ tool_id 네임스페이스 경계 변환 규칙을 수정 화면까지 일관 적용, ★ FR-6(도구 워커 교체 저장)은 별도 feature 분리 — 수정 모드 도구함 배너("도구 구성 변경은 아직 저장되지 않습니다")로 유예 방어. 잔여: 서버 기동 후 수동 E2E(모델/도구/RAG 표시 + 모델 변경 저장 왕복), (선택) 프라임 effect 통합 테스트. 후속: agent-builder-edit-tools-save(FR-6) | [→](./agent-builder-edit-mapping/) |
| kb-content-browser | 2026-07-14 | 97.4%(Check, Act 0회 — Missing/Changed 0, Partial 2건 설계문서 갱신으로 즉시 정리) | 백엔드 신규+회귀 69 pass(assembler 동등성 + guard 6 + UseCase 3종 + 라우터 16, WinError 10014 환경 산발은 지정 재실행 전건 통과) + 프론트 신규 20 + 기존 KB 3 pass + tsc 0 error · 백엔드 신규 7 + 수정 3 · 프론트 신규 8 + 수정 6 · **마이그레이션 0 / 기존 API·파이프라인 무수정** — **KB 저장 내용(3계층: 문서 요약→섹션 요약→parent/child 청크) 드릴다운 뷰어(kb-management-ui 후속, 풀스택 1일 사이클)**. KB 단위 저장은 완성됐지만 무엇이 어떻게 저장됐는지 확인할 화면 부재 + 요약 본문은 조회 API 자체가 없음(잡 진행률만) → KB 라우터 하위 조회 API 3종(`/summary`·`/section-summaries`·`/chunks`) 신규. ★ **사용자 결정 2건이 설계 축**: ① 조회 소스를 한쪽 선택 대신 `source=qdrant\|es` 파라미터+프론트 토글로 **두 저장소를 나눠서 교차 검증**(ES 우선-Qdrant 마지막 이중쓰기 정합성 눈검증 도구화), ② 청크 q 검색도 선택 저장소 기준 — 조사에서 **Qdrant 풀텍스트 payload 인덱스 부재 발견** → ES=match(nori)/Qdrant=scroll 후 contains로 분기, 응답 `search_mode`로 UI 배지 안내. ★ **chunk_assembler 공용 헬퍼 추출** — GetChunksUseCase의 전략감지/계층조립을 payload dict 기반으로 일반화(시그니처·응답 무변경, 기존 테스트 12건 무수정 통과 = 동등성 보증), Qdrant point/ES hit 양쪽 재사용. ★ KB 격리 = `KbDocumentGuard`(KnowledgeBaseUseCase.get 위임 403/404 + document_metadata.kb_id 일치, NULL/불일치 404 — V047 kb_id 기준). ★ 요약 미생성은 404 아닌 `exists:false`(상태 구분), ES 섹션요약 body에 chunk_index 없음→0 폴백, 요약 본문 다중 키 폴백(summary_text/summary/content)으로 저장소 필드차 흡수. 섹션요약 잡 상태 API **첫 프론트 연동**(pending/processing 5초 폴링 + 실패/stale 재시도 버튼). 3계층 모두 ES 전역 단일 인덱스 "documents"에 kb_id 색인 확인(KB 업로드는 docs_{collection} 경로 안 탐). 잔여: 브라우저 E2E 실측 → kb-pipeline-e2e-pending ⑤ 이월. 후속 후보: KB 전체 횡단 검색, Qdrant 풀텍스트 인덱스 | [→](./kb-content-browser/) |
| kb-custom-chunking | 2026-07-15 | 96%(Check) → 98%(🔴 1건 즉시 보강, Act 0회) | 백엔드 신규+확장 ~72 pass(도메인 config/policy 40 + can_manage_settings 3 + UseCase custom/update 11 + resolver custom 5 + 라우터 6 + **repo JSON왕복/화이트리스트 7 신설**) + 기존 회귀 그린(WinError 10014 환경 산발은 N회 합집합+assertion 0으로 판별), 프론트 신규 21 pass + tsc 0 · 백엔드 신규 3 + 수정 8 · 프론트 신규 4 + 수정 6 · **마이그레이션 1(V048)** — **KB 단위 커스텀 청킹: 전략 5종 + chunk_size/overlap/parent/min + 경계 정규식 사용자 설정(clause-aware-chunking·kb-management-ui 후속, 풀스택 2일)**. 조항 토글 외 청킹 제어 불가·비조항 KB는 parent_child 2000/500/50 하드코딩 → **독립 opt-in**(`use_custom_chunking` TINYINT + `custom_chunking_config` JSON, 조항 필드 4개 의미 불변 — 사용자 확정: 정규식까지/KB 단위/신규 업로드부터/독립 필드). ★ **신규 청킹 엔진 0줄** — factory가 이미 5전략 kwargs 지원, `boundary_pattern`은 clause_aware 엔진 재사용(D3), 정규식 상한(≤200자/≤50개/re.compile)은 ChunkingProfilePolicy 상수 재사용(D5/D11). ★ **KB update 경로 최초 도입** — 조사에서 KB에 update 자체가 없음을 발견(D7) → `PATCH /knowledge-bases/{kb_id}/chunking`(owner/ADMIN `can_manage_settings`, 전체 교체) + repo `update_chunking()` **청킹 6컬럼 화이트리스트**(agent-builder-edit-mapping "4곳 세트" 선례 — SQLAlchemy 문 검사 테스트로 고정). ★ 검증 V-01~V-07 domain 집약: 전략별 금지 파라미터 매트릭스(semantic+overlap>0 명시 422, D12), 상호배타(V-07)는 백엔드 422 + 프론트 **radio 3택**(기본/조항/커스텀) 원천 차단, 정규식 실패는 패턴 원문 포함 422. ★ resolver custom→clause→legacy 순서, 손상 config는 **legacy 폴백+warning**(업로드 항상 성공 — clause FR-07 관례). 프론트: `customChunkingForm.ts` 유틸(검증/변환 공유) + `CustomChunkingFields`(전략별 동적 필드+규칙 편집기+인라인 정규식 에러) + `KbChunkingSettingsCard`(요약+수정 모달+"기존 문서 미적용" 안내 고정), 에러 표시는 ApiError.message(authClient가 detail 변환 — axios 분기 불필요). vitest는 threads에 `--no-file-parallelism` 추가 필요 발견. 잔여 Partial 2(403 한글 문구·테스트 파일명 — 기능 무영향). ⚠️ 배포 전 필수: **V048 DB 적용** + E2E 실측 3종(커스텀 실측/legacy 회귀/기존 문서 불변 — kb-pipeline-e2e-pending 합류). 후속: kb-chunking-preview(preview API 재사용), kb-rechunk-reindex, 403 문구 정합 | [→](./kb-custom-chunking/) |
| chunking-profile-admin-ui | 2026-07-17 | 95%(Check, Act 0회 — Gap 2건 전부 Low=테스트 커버리지) | 프론트 신규 19 pass(service 5 + BoundaryRulesEditor 5 + 페이지 8 + adminNav 1, `--pool=threads`) + 전체 625 pass/8 fail(전건 기지 사전 실패 — 신규 회귀 0) + ESLint·신규파일 tsc 0 · 프론트 신규 7 + 수정 5 · **백엔드 변경 0 / 마이그레이션 0 / 계약 변경 0** — **청킹 프로파일 관리자 화면 `/admin/chunking-profiles`(clause-aware-chunking·card-section-summary 후속, 프론트 전용 3일)**. 조항 요약·키워드 추출 LLM(`summary_llm_model_id`)과 경계 규칙은 프로파일에 저장되는데 관리 UI 부재 — admin CRUD API(`/api/v1/admin/chunking/profiles`)는 완비였으나 프론트 호출처 0곳(Swagger/DB 직접 수정으로만 운영) → 기존 API 5종 그대로 배선. ★ **PUT 전체 교체 대응(D2)** — 목록 응답 전체 프리필 → 전체 바디 전송, "이름만 바꿔도 나머지 필드 보존" P3 회귀 가드 테스트로 고정(부분 수정 API 부재 시 프론트 표준 패턴). ★ **참조 무결성 UI 방어(D3)** — 요약 LLM 드롭다운은 `useLlmModels(true)` 1회 조회로 활성만 노출하되, 수정 대상이 비활성/미등록 모델 참조 시 "(비활성)"/"(등록 정보 없음)" 옵션 유지로 저장 시 조용한 값 소실 차단. ★ AdminLlmModelsPage 검증 패턴 복제(모달 1회 초기화·getErrorMessage·3분기 상태·invalidateOn404)로 설계~구현 단축. BoundaryRulesEditor(pattern/priority/level 행 편집 + isValidRegex 실시간 검증), 기본 지정은 전용 엔드포인트 액션 버튼+폼 체크박스 병행(D7), 삭제 시 "참조 KB는 기본 프로파일로 폴백" 고지(D8). userEvent `[` 키 디스크립터 파싱 → 정규식 입력 테스트는 fireEvent.change(jsdom gotcha 추가). 잔여: G1 422 detail 테스트(Low), G2 수동 E2E(요약 LLM 지정→section_summary_job 확인 — kb-pipeline-e2e-pending 합류). 후속: kb-profile-selector(KB 폼 프로파일 드롭다운), 요약 잡 모니터링 UI, 정규식 프리뷰 | [→](./chunking-profile-admin-ui/) |
| kb-excel-upload | 2026-07-17 | 97.6%(Check, Act 0회 — 구조 기준 100%, 코드 조치 Gap 0건) | 백엔드 신규 35 pass(domain 포맷판정/예외/직렬화 + infra 어댑터/라우팅파서 31 + 유스케이스 엑셀분기 4) + 라우터 422 1 + 프론트 모달 3 pass(`--pool=threads`) + 회귀 405+ 그린(pymupdf4llm 21건은 stash 검증 기존 실패·이벤트루프 산발 1건 격리 통과) · 백엔드 신규 5 + 수정 6 · 프론트 수정 1 + 테스트 신규 6 · **마이그레이션 0 / 라우터 수정 0줄 / 기존 PDF 경로 무변경** — **KB 문서 업로드 파이프라인 엑셀(.xlsx/.xls) 지원(kb-custom-chunking 후속, 풀스택 1일)**. KB 업로드 파서가 `fitz.open(filetype="pdf")` 하드코딩 + 별도 엑셀 경로(`/api/v1/excel/upload`)는 기본 컬렉션 고정으로 KB 권한·kb_id 메타데이터·ES 이중 저장과 단절 → **확장자 라우팅 파서 additive 위임 구조**: `ExtensionRoutingParser`(pdf→기존 주입 파서 그대로 위임, xlsx/xls→`ExcelDocumentParserAdapter`) + 포맷 판정 단일 진실원(`domain/parser/supported_formats.py` — 후속 포맷 추가 시 1곳 수정). ★ **ValueError 편승 422 패턴** — `UnsupportedFileFormatError(ValueError)` 설계로 KB `_raise_http` 폴백 + unified `except ValueError` 양쪽 라우터 무수정 매핑, ★ 어댑터는 시트=Document 1개·PDF와 동일 `DocumentMetadata` 계약(page=시트 순번, total_pages=시트 수 — 다운스트림 청킹/ES/브라우저 의존 키 유지) + sheet_name/row_count, ★ 시트 직렬화는 `ExcelUploadUseCase` 기존 로직을 **domain 순수 함수로 추출 공유**(이중화 해소, 형식 고정 테스트), ★ 엑셀은 clause/custom 청킹 우회(parent_child 기본 + warning 로그)·섹션 요약 스킵(조항 전제 기능 오동작 차단), ★ 행 상한 `kb_excel_max_rows_per_sheet=20000` 초과·손상·빈 엑셀 전부 원인 포함 422(조용한 절단 기각), ★ 프론트 accept `.pdf,.xlsx,.xls` 정합화(기존 .docx/.txt/.md는 애초 미동작 잠재 버그 동시 해소), ★ jsdom gotcha: userEvent.upload는 accept 위반 파일 조용히 무시 — 거부 테스트는 accept 정합 파일+서버 422로 구성. 잔여: FR-09 런타임 동등성 + 수동 E2E(실 xlsx→Qdrant sheet_name→검색 히트→콘텐츠 브라우저) — kb-pipeline-e2e-pending 합류(V047·V048 선행). 후속: kb-multi-format-upload(docx 등), 표 구조 인지 청킹, 기존 엑셀 경로 정리 | [→](./kb-excel-upload/) |
| analysis-source-preservation | 2026-07-07 | 98%(Check, Act 0회 — Gap 1건 Check 내 코드수정) | 신규 테스트 20 pass(정책 raw+select_recent 10, analysis_node 방출/튜플반환 6, run_agent 캡처/병합/재주입 4) + agent_builder/conversation/general_chat 회귀 그린(격리 실행), 마이그레이션 0(V039 재사용) · 백엔드 수정 7 — 엑셀 분석 스냅샷에 **파싱된 원천 데이터(행/값) 병행 저장**. 선행 analysis-data-continuity는 분석 **결과 텍스트**(파생)만 스냅샷에 저장해, 원본 엑셀(TTL 1h 임시)이 소실되면 후속 턴 "분기별로 다시"류 재분석이 불가했던 문제 해결. `ExcelAnalysisWorkflow`가 계산하고도 버리던 `ExcelData.to_dict()`(시트/columns/rows)를 `_run_excel_analysis` **(text, raw) 튜플 반환** → `analysis_node`가 `kind="raw_source"`로 **SupervisorState.analysis_source 채널(charts 상태채널 동형)** 노출 → `run_agent_use_case._map_chain_end` 캡처(charts 옆) → `_collect_snapshot` 병합. 원천은 `AnalysisSnapshotPolicy.render_raw_source`가 **압축 표 텍스트(헤더+행, 행 샘플링 max_rows=200 + 총행수 표기)**로 직렬화, **kind별 독립 budget**(raw 6000/8000, 비-raw 기존 4000/8000)로 상한 → 기존 analysis-data-continuity 동작 불변. 재주입·재분석은 **신규 그래프 배선 0** — raw_source도 `format_search_result` 규약 AIMessage가 되어 후속 턴 `_analyze_context` context 분기가 데이터로 재집계. ★ "결과가 아닌 원천을 보존 → 멀티턴 분석이 1회성 요약에서 반복 재분석 가능 워킹셋으로", ★ "원천은 메시지가 아니라 state 채널로 흘린다(charts 미러링)", ★ Gap 1건(설계 §3.3 요구 `select_recent`의 kind별 budget 분리가 `_snapshot_size` 단일 합산으로 미구현)을 Check에서 `_snapshot_sizes` 튜플 helper로 코드 수정 → 98%. 스코프=엑셀 분기(RunAgentUseCase)만(검색/MCP는 이미 원천 저장). 한계: '보유 원천의 재분석'이지 '없던 데이터(다른 사용자 등) 확보' 아님. config 3종(analysis_snapshot_raw_source_max_chars/total/max_rows). 후속: 라이브 E2E 1턴(턴1 엑셀→턴2 재집계) + LangSmith 토큰 실측, pii-masking-integration(원천 개인정보 보호) | [→](./analysis-source-preservation/) |
| admin-dashboard | 2026-07-18 | 94.4%(Check, Act 0회 — 백엔드 100%, 잔여 갭=프론트 테스트 자산 3건) | 백엔드 신규 28 pass(UC 5 + 헬스 어댑터 5(병렬/타임아웃 격리) + 집계 repo SQLite 4 + 라우터 14, 전부 선행 작성) + 프론트 신규 12 + 회귀 8 pass(`--pool=threads`) + tsc 0 error · 백엔드 신규 11 + main.py 배선 · 프론트 신규 12 + 수정 5 · **마이그레이션 0 / 기존 API·화면 무수정(additive)** — **관리자 운영 현황 대시보드 `/admin/dashboard`(풀스택 1일 사이클)**. 문서 적재량·KB 수·질문 횟수 확인에 DB 직접 조회 필요 → 신규 4 엔드포인트(`/api/v1/admin/dashboard/{stats,kb-breakdown,recent-documents,health}`, `require_role("admin")`)로 적재/사용자 누적 현황 + KB별 LEFT JOIN 분포(0건 KB 노출) + 최근 문서 + MySQL/Qdrant/ES 헬스만 신설. ★ **D1: 기간 지표(질문 수·성공률·토큰·비용·추이·최근 실패)는 기존 `/admin/usage/*`·`/admin/runs` API 100% 재사용** — Design 실코드 검증에서 usage summary가 완전 커버 확인 → stats API에서 from/to 자체 제거(수치 이중 구현 원천 차단, AdminAgentRunsPage와 수치 항상 일치), ★ 헬스 = checks dict 주입 어댑터 + `asyncio.gather`+`wait_for(3s)` 컴포넌트별 격리 — 저장소 1개 다운도 HTTP 200 부분 성공(장애 상황에서 더 필요한 화면이 죽지 않음), ★ 프론트 재사용 3종(PeriodFilter 자유 날짜 범위·SummaryCards·TimeseriesChart) + `Card` named export additive 추출(기존 회귀 0), 수동 새로고침=도메인 invalidate(사용자 확정), ★ SQLite 테스트 함정 2건 확보: KB FK 참조 테이블 선행 생성 필요 + `document_metadata` BigInteger PK는 id 명시, ★ findByText 다중 매칭(동일 KB명 두 위젯 노출)은 findAllByText. 잔여(선택): 프론트 services/hooks MSW·컴포넌트 3종 테스트 보강(간접 커버 중), 위젯 isError 분기, 헬스·stats 실측 E2E — kb-pipeline-e2e-pending 합류. 후속 후보: 자동 폴링 옵트인, Qdrant/ES 적재량 실측 대사, 임계치 경보 | [→](./admin-dashboard/) |
| kb-retrieval-test | 2026-07-18 | 100%(Check, Act 0회 — 차단 Gap 0건, cosmetic 2건은 설계 표기 정정으로 해소) | 백엔드 신규 28 pass(KbSearch UC 10 + 히스토리 UC 3 + repo SQLite 5 + 라우터 9, 전부 선행 작성) + 회귀 195 그린(컬렉션 검색 44 + KB application 108 + KB 라우터 32 + 브라우저 11), 프론트 17 pass + tsc 0 error · 신규 백엔드 5 + 프론트 5 · additive 수정 백엔드 6 + 프론트 8 · 마이그레이션 1(V049) — **KB 상세 페이지에 상태 요약 카드 4종 + 리트리버 테스트(KB 단위 하이브리드 검색·히스토리) 추가(풀스택)**. 컬렉션 검색 대용 시 같은 물리 컬렉션 공유 타 KB 문서 혼입 → `POST /knowledge-bases/{kb_id}/search`(body `document_id` 옵션 스코프) + `GET .../search-history` 신설, metadata_filter `{collection_name, kb_id[, document_id]}`로 Qdrant(SearchFilter)+ES(term filter) 양쪽 격리 — ES 매핑 `kb_id: keyword` 기존재가 전제(V047 이전 kb_id NULL 문서는 의도된 검색 제외). ★ **컬렉션 UC 미호출**: 컬렉션 권한 이중 검사 회피 위해 내부 스택만 재사용, 권한은 `KnowledgeBaseUseCase.get`(403/404)+`KbDocumentGuard`(문서 소속 404) 위임, ★ **임베딩 해석 공용 헬퍼 추출**(`embedding_resolver.py`) — 기존 UC는 위임만(시그니처·테스트 무변경, 회귀로 증명), ★ 히스토리 = `search_history.kb_id` nullable additive(V049) + `save(kb_id=None)` 기본값 + `find_by_user_and_kb`(보조정렬 id desc) — 컬렉션 경로 무영향, ★ **SQLite variant PK**(`BigInteger().with_variant(Integer,"sqlite")`)로 repo 실세션 통합테스트 확보, ★ 프론트는 컬렉션 컴포넌트(HybridSearchPanel/SearchResultList) 재사용 + KB 전용 히스토리 패널 신설(기존 패널 무변경), 검색 스코프(`searchScopeDoc`)는 드릴다운 선택과 **독립 state**(D10 — 행 클릭마다 스코프 바뀌는 결합 예방), 상태 카드는 chunk_count 간이 판정(컬렉션 동일, 오류 실측은 후속). Plan 리스크 2건(activity_log 임베딩 해석·ES kb_id 필터)을 Design 실코드 검증으로 해소해 Do 재작업 0회. 배포 전 필수: **V049 DB 적용**(미적용 시 히스토리 저장만 조용히 실패). 후속: 같은 컬렉션 공유 KB 2개 격리 수동 E2E(공통 체크리스트), 문서별 저장 상태 실측 오류 카운트, 히스토리 패널 공용화, KB 문서 목록 페이지네이션 UI | [→](./kb-retrieval-test/) |
| wiki-user-facing | 2026-07-18 | 97.1%(Check) → 100%(갭 2건 당일 보강, Act 0회) | 백엔드 위키 스위트 143 pass(신규 ~40케이스: 정책 path/human_source_ref/can_manage·human_write 14·tree 4·라우터 POST/tree/인가) + 기존 회귀 0, 프론트 신규 18 + 회귀 8 pass, tsc/eslint 클린 — LLM Wiki 사용자 노출 3축: (A) 소유자 직접 작성 POST /wiki(source_type=human 예약석, 즉시 approved, 출처 불변식은 human:{user_id}로 완화 없이 충족, 인가는 can_manage: admin 전부/소유자는 human만·approve류 admin 전용 무회귀), (B) path 가상 폴더(V051) + GET /wiki/tree 서버 그룹핑 + 프론트 / split 중첩 렌더(buildFolderTree), distill 기본 path=컬렉션명, (C) SourceCitation 위키 근거 배지 → /knowledge/{chunk_id} 문서 뷰(백엔드 무변경 — chunk_id=article id 활용). ★ tree 라우트는 /{id}보다 먼저 선언(FastAPI·MSW 공통) — 선언 순서 회귀 테스트로 고정, ★ editor 기록은 body가 아닌 인증 사용자, ★ 라우트 권한 완화 + 유스케이스 인가 조합으로 admin 무회귀 보장. 배포 전 필수: V051(wiki_article.path). E2E(작성→wiki-first 검색→배지)는 Qdrant 실기동 이월 — KB 공통 체크리스트. 후속: agent-workspace-view·fix-wiki-distill-dedup·org 스코프 결정 | [→](./wiki-user-facing/) |
| agent-memory | 2026-07-20 | 98%(Check, Act 0회 — Gap Low 1건=모델 SMALLINT vs DDL TINYINT 무해) | 백엔드 신규 57 pass(정책 16 + repo SQLite 7 + CRUD 13 + 조립기 6 + 라우터 9 + general_chat 주입 6, 전부 선행 작성) + 기존 general_chat **무수정 통과 = 회귀 0 증명**, 프론트 신규 10(훅 4 + SettingsPage 6) + wiki 회귀 7 pass + tsc 0 · 신규 백엔드 10 + 프론트 4 · additive 수정 백엔드 3(general_chat/config/main) + 프론트 5 · 마이그레이션 1(V050) — **사용자 메모리 Phase 1: 수동 등록 CRUD + General Chat 시스템 프롬프트 상주 주입(growing-agent 메모리 축 1단계, 풀스택 당일 사이클)**. 소속·용어·선호를 매 대화 재설명 + AI 기억의 열람·삭제 불가 → `agent_memory`(V050, Phase 2/3 컬럼 tier/scope/status/source_run_id/confidence 선반영) + `/api/v1/memories` CRUD(401/**타인·미존재 404 은닉**/422) + `/settings` "AI가 기억하는 내용"(타입 뱃지·카운터 N/30·인라인 수정·상한 안내·422 표면화, 빈 스텁 페이지 신설). 주입: `_create_agent(tools, auth_ctx, memory_block="")` → `user_ctx + memory_block + _SYSTEM_PROMPT`(agent-user-context prepend 선례에 한 항 삽입), stream()에서 agent 생성 직전 1회 조립. ★ **optional 의존성(None 기본) 3연승**(chart→tracker→memory_assembler) — 기존 테스트 무수정 통과가 회귀 0의 증명, ★ MemoryContextAssembler = RunScopedWikiSearch 동형 session_factory per-call + 실패 시 "" 격리(warning, 채팅 무영향) + 정렬(프로필>용어>선호>참고, 동순위 최신) + 한글 1자≈1토큰 보수 근사 800자 예산 절단, ★ 함정 3건: `datetime.min.timestamp()` Windows OSError→이중 안정 정렬 / 프론트 422는 authApiClient가 detail을 ApiError(message)로 변환→err.message / 한글 테스트 마커 "다"가 헤더 "다음은"과 충돌. gap-detector 제기 G2(FR-07 스택트레이스)는 실코드 반박 해소(StructuredLogger가 exception=→exc_info 튜플 변환). 배포 전 필수: **V050 DB 적용**. E2E(등록→주입 trace→삭제 후 미주입)는 KB 공통 체크리스트 이월. 후속: Phase 2 자동 추출+승인 게이트(스키마 준비됨), Phase 3 org 스코프+Tier 1 온디맨드 | [→](./agent-memory/) |
| agent-memory-extraction | 2026-07-20 | 97%(Check) → 100%(테스트 갭 3건 당일 보강, Act 0회 — 구현 결함 0) | 백엔드 신규/확장 108 pass(정책 26 + repo 10 + 추출기 9 + CRUD 21 + 조립기 6 + 추출 서비스 11 + 라우터 16 + 주입/kickoff 9) + general_chat 회귀 25/25 전체 통과, 프론트 17 pass(훅 7 + SettingsPage 10) + tsc 0 · 신규 백엔드 3(extractor·extraction_service·application/memory/interfaces) + additive 8 · 프론트 additive 7 · **마이그레이션 0**(Phase 1 예약 컬럼 배당) — **메모리 Phase 2: 대화 자동 추출 + 승인 게이트(Plan→Report 당일 사이클)**. 답변 완료 후 백그라운드 LLM 추출(`_finish_observability` 직후 sync kickoff, launcher 가드 패턴 `_tasks`+done_callback, chart-edit 턴 제외) → 마지막 턴 4000자 절단 → WikiDistiller 동형 추출기(JSON 배열 강제·PII 금지 프롬프트·파싱 실패 []) → dedup(기존 active+pending 프롬프트 제공 + strip 일치 코드 차단, distill 중복 실수 미반복) → **pending 저장**(source_run_id·clamp confidence, active 직행 금지) → `/settings` "승인 대기 N건" 앰버 카드 승인(active 상한 재검증)/거부. `GET /memories?status=` additive + PATCH approve/reject(404 은닉·비pending 422). **기본 off 배포**(`memory_extraction_enabled=False`) — off 시 Phase 1과 100% 동일, pending 상한 도달 시 LLM 호출 자체 스킵. ★ **쓰기 세션은 `async with session.begin():` 필수**(RunTracker 선례) — session_factory만으로는 저장 조용히 유실(읽기 assembler와 다름), ★ repo update() 화이트리스트에 status 추가 — CC 메모리의 "4곳 세트" 교훈이 실결함을 구현 중 차단(영속화 회귀 테스트 고정), ★ FR-06(pending 주입 배제)은 find_active_by_user 무수정으로 구조적 보장, ★ 설계 §4 테스트 플랜을 구현 체크리스트로 쓰면 gap 감점(커버리지 누락) 예방 — 1차 감점 3건 전부 테스트 누락이었음. E2E(on→추출→승인→주입)는 KB 공통 체크리스트 이월. 후속: Phase 3(run 딥링크·pii_masking 연동·org 스코프·만료 배치), 운영 on 전환 결정 | [→](./agent-memory-extraction/) |
| fix-wiki-distill-dedup | 2026-07-20 | 100%(Check, Act 0회 — Gap 0건) | 백엔드 80 pass(위키 정책 40[신규 5: refs_key·is_duplicate] + distill 14[신규 dedup 7: 멱등 재실행·distiller call_count 0·혼합·refs 순서 무관·human 제외·실행 내 중복] + 라우터 26) + 프론트 15 pass + tsc 0 · 수정 백엔드 4 + 프론트 4 · **마이그레이션 0/신규 API 0** — **distill 재실행 중복 생성 수정(§10 기록 부채 해소, Plan→Report 당일)**. `_distill_one` 무조건 uuid4 신규 저장 → 재실행마다 draft 재생성(승인 큐 오염+LLM 중복 비용) → `WikiPolicy.refs_key`(strip+frozenset — 순서·공백 무관) 정체성 키로 기존 DISTILLED 문서와 정확 일치 시 **distiller(LLM) 호출 전 스킵**(FR-02) + 실행 내 생성분 키 추가(실행 내 중복 방어) + `execute` → `(created, skipped)` 튜플(호출부 단일성 grep 실증 — wiki_router 1곳) + `DistillResponse.skipped_count=0` additive(프론트 무수정 동작). ★ 결정 3건: 전 상태 스킵(deprecated 재생성 루프 차단 — 반려 의사 존중)·UseCase set 비교(repo/스키마 무변경 — refs가 JSON list 왕복이라 DB 집합 질의 불가)·부분 겹침은 보수적 신규 취급, ★ human 문서(human:{id}) refs는 비교 제외 — 교차 오염 방지, ★ **기존 테스트 실패 2건 = 신규 동작의 방증**(_group() 기본 refs 동일 → 실행 내 dedup 정확 작동, 고유 refs로 정정), ★ 메모리 축(추출 전 dedup)과 위키 축(정제 전 dedup)이 "LLM 호출 전 스킵" 원칙으로 정렬 — 자동 유입 경로의 기본형 확립. 가이드 §10 한계 2건 갱신(중복 해소 + human 작성 API 낡은 항목 정정). E2E(distill 2회→skipped 확인) KB 공통 이월. 후속: 기존 중복 draft 소급 정리(수동), force 재생성 옵션(요청 시) | [→](./fix-wiki-distill-dedup/) |
| agent-workspace-view | 2026-07-20 | 94%(Check) → ~98%(Low 갭 3건 당일 보강, Act 0회) | 프론트 신규 페이지 8 + AgentKnowledgePage 회귀 6(워크스페이스 링크 1 추가) = 14 pass + tsc 0 · 신규 2(페이지+테스트) + 수정 3(App 라우트·AgentKnowledgePage·AgentDetailModal) · **백엔드 diff 0(프론트 전용)** — **에이전트 구성 폴더형 열람 뷰(투명성 축, Plan→Report 당일)**. 에이전트가 어떤 지침·도구·지식으로 움직이는지 볼 화면이 없어 블랙박스 → `/agents/:id/workspace` 읽기 전용 폴더 트리(지침 마크다운·도구 카탈로그 라벨+RAG 설정·서브에이전트 ref_agent_name·스킬·지식 wiki tree 요약·정보) — **AgentDetail이 이미 전부 노출**(system_prompt·tool_ids·workers·skill_ids·can_edit)이라 신규 API·타입·훅 0, useAgentDetail/useAgentSkills/useWikiTree 기존 훅 화면 조립만. ★ **훅 반환 타입은 서비스가 아니라 훅 선언을 봐야** — useToolCatalog는 CatalogTool[] 배열 직접 반환(응답 객체 아님), 오인 시 도구 섹션 크래시(테스트가 즉시 검출), ★ 읽기 전용(mutation 훅 0)·편집은 agent-builder 링크 분리, ★ 폴더별 독립 강등(agent detail 실패만 페이지 전체 안내, skills 403/tree 실패는 해당 폴더만 — FR-05), ★ tool_id 이중 네임스페이스는 mapDraftToolIdsToCatalog 재사용 + 미매칭 원문 표시, ★ 진입점 2곳(AgentKnowledgePage 헤더·AgentDetailModal) 상호 링크 — 지식 브라우저와 대칭 URL. gap 3건(회귀 테스트 1·소유자 표시·문서 max-w) 당일 해소, max-w는 코드(3xl)가 옳아 설계 정정. growing-agent 투명성 축 마지막 조각(위키=지식은 wiki-user-facing, 나머지 구성은 여기). E2E(실제 에이전트 6폴더 열람+진입점 왕복) 실서버 이월. 후속: index.tsx 258줄 섹션 분리(소프트, 회귀 0) | [→](./agent-workspace-view/) |
| agent-memory-org-scope | 2026-07-20 | 91%(Check, Act 0회 — 백엔드 100%, G1 프론트 부서 UI 이월) | 백엔드 6스위트 93 pass(정책29·repo15·crud27·assembler10·라우터22·주입10, org 신규 ~25) + general_chat 회귀 25/25 + 프론트 훅10·SettingsPage12 + tsc 0 · 백엔드 additive 10 + 프론트 7 · **마이그레이션 0(V050 scope/user_id 예약 슬롯 재사용, 실측 V052+ 0)** — **메모리 Phase 3: 부서(org) 공유 메모리 + 승격(개인→조직, Plan→Report 당일)**. 메모리가 전부 개인 스코프라 부서 공유 지식 미전파 → `scope='org'`+user_id=부서id 신규 행, 개인 메모리 승격(복사·원본 유지)·관리자 직접 작성, General Chat 주입 시 개인∪부서 병합(content 중복 시 개인 유지, `sort_for_injection_scoped` 개인 우선→부서→타입, 단일 합산 캡, 부서는 "(부서 공유)" 라벨). ★ **FR-07 핵심 안전장치: user_id 슬롯을 개인/부서가 공유하므로 find_active_by_user·find_by_user_and_status·count_by_user_and_status에 `scope='user'` 가드 3곳 필수** — 없으면 개인 조회가 org 행에 조용히 오염, 회귀 테스트로 고정, ★ **예약 컬럼 배당 3연속**(Phase 1 선반영 → pending Ph2 → org Ph3 모두 마이그레이션 0), ★ 권한 `_require_dept_membership`(admin 또는 소속 부서, 비소속 403)·승격 중복 409·부서 상한 config 50, ★ general_chat이 `auth_ctx.department_ids`→`build_block(dept_ids=)` 전달(dept_ids 없으면 개인만 회귀). **G1 이월(정직 gap)**: 프론트 `/auth/me`(User 타입)에 department_id 부재 → 부서 작성/승격 UI 미구현(hooks·service·MSW·SettingsPage 읽기 섹션만, 부서 열람은 서버 스코프라 작동). 억지 구현 대신 후속 expose-user-department 분리. E2E(부서 2명 공유·주입) 실서버 이월 | [→](./agent-memory-org-scope/) |
| expose-user-department | 2026-07-20 | 100%(Check, Act 0회 — Gap 0) | 백엔드 6 pass(GetUserDepartmentsUseCase 3 + /me 라우터 3: 부서포함·빈리스트·401) + main import OK + 프론트 SettingsPage 16·훅 10 · 신규 백엔드 2(UC+MeResponse) + 수정 3(auth_router·main DI) + 프론트 수정 5 · **마이그레이션 0** — **`/auth/me` 부서 노출 + org 부서 작성/승격 UI(agent-memory-org-scope G1 후속, Plan→Report 당일)**. Phase 3 백엔드·hooks 완성됐으나 `/me`(UserResponse)에 department_id 부재로 부서 작성/승격 UI 미구현 → 사용자 지정 3결정: ① **get_current_user 유지 + 부서 별도 조회**(get_auth_context 권한조립 미교체 — `find_departments_by_user` 단일 + `list_all` 이름 map 1회 재사용, 미소속이면 부서조회 스킵) ② **다부서 선택 드롭다운**(승격/작성 모두 1개 즉시·2개+ select) ③ **신규 MeResponse**(UserResponse 무변경 — 로그인 등 타 소비자 4곳 보호, /me만 전환). SettingsPage: MemoryItem "부서로 승격"(usePromoteMemory) + OrgSection admin 작성 폼(useCreateOrgMemory) — 미소속·비admin은 미노출(FR-05), 409/422 errorDetail 재사용. ★ **"데이터는 이미 있고 노출 경로만 없던" 패턴 반복**(AuthContext가 부서 조립하나 /me가 UserResponse로 절단 — retrieval-observability·agent-workspace-view 동형), ★ 응답 타입 분리(MeResponse만 변경)로 회귀 0. org-scope G1 완전 해소 — growing-agent "개인→조직" 승격 루프(작성·승격·열람) 완성. E2E(다부서 승격 선택·admin 작성 왕복) 실서버 이월 | [→](./expose-user-department/) |
