# Archive Index — 2026-08

> 이 디렉토리는 PDCA 사이클이 완료된 피처 문서를 보관합니다.

| 피처 | 완료일 | Match Rate | 테스트 | 경로 |
|------|--------|-----------|--------|------|
| runtime-datetime-context | 2026-08-25 | 98%(Check 96% → Act-1 98% — Critical 0 · Important 3(가드 planner 누락 · Design 테스트명 13건 불일치 · 수동 E2E 미실행) + Minor 7 → 1·2·4·5·6·9·10 해소, 7 문서화, 3·8 이월, 이터레이션 1) | 신규 43(clock 6 · config 2 · render 9 · search 파이프라인 3 · 컴파일러 9 · general 3 · rag 2 · excel 2 · 빌드타임 음성 2 · AST 배선 5) · 회귀 58 failed/8119 passed, FAILED 목록 baseline 동일(diff ×3) · ruff 추가 라인 0 · 백 신규 4(domain/agent_run/clock.py + 테스트 3)+수정 13 / 프론트 0 / 마이그레이션 0 / 의존성 tzdata — **런타임 시스템 프롬프트 `[현재 날짜] 2026-08-25 (화)` + 해석 지침 3줄 블록을 순수 함수 `render_datetime_block(tz, now_utc, logger)` 1개 + config `agent_timezone`(Asia/Seoul) 1개로 6개 경로(수퍼바이저/final_answer/analysis · search 파이프라인 rewrite/validate/compress · create_agent 일반 워커(최초 system_prompt) · wiki 워커 · 서브에이전트(include_user_context 무관) · general_chat · RAG · 엑셀 분석)에 prepend, 빌드타임 composer/planner는 AST 가드 격리**. Option C — kwarg 기본 None=블록 생략(픽스처 무수정·회귀 0) + main.py 5곳 AST 배선 테스트로 "조용히 빠짐" 차단, 순서 날짜→사용자→wiki/메모리→본문, 실패는 degraded(''+warning exception=). ★ **Design 코드 조사가 Plan 가정을 뒤집음** — 웹검색(tavily)은 `create_agent`가 아니라 category=search → `create_search_pipeline_node`의 rewrite LLM이 검색어를 쓴다(FR-05 a/b 분할). 놓쳤으면 수퍼바이저에만 날짜가 들어가는 무효 구현. ★ 회귀는 pass 개수가 아니라 정렬된 FAILED 목록 diff(baseline 58건 상시 실패). ★ `ensure_user_tail`은 human-last에 no-op이라 첫 턴 지시 주입 통로가 아님 → 워커는 system_prompt가 유일. 함정: 시스템 Python `ZoneInfoNotFoundError`(tzdata 전이 의존 → 명시) · gap-detector가 `idt/`만 검색해 루트 `docs/wiki/ops/e2e-carryover-checklist.md` 부재 오탐 + 본문 없이 종료(transcript 추출) · Bash heredoc 한글/`
` 깨짐(Edit 도구). 이월: 실서버 E2E R2("천안 오늘 날씨" → rewrite 쿼리에 날짜) · `/wiki update`로 체크리스트 등재 · FR-12 스케줄 기본 tz config 단일화(domain은 config 불가시, use case 주입) · 합성 노드 날짜 블록 · 커밋(무관 변경 다수 → 파일 선별) · tzdata lock 재생성 | [→](./runtime-datetime-context/) |
| blueprint-style-fidelity | 2026-08-23 | 97%(런타임 포함, 정적 94% — Check 1차 SC 8/8 Met · Critical 0 · Important 2(G1 FR-08 카드 소제목 미구현 → descope · OPS-1 라이브 서버 구버전) + Minor 8, Act-1 즉시 조치: G2 공통 장식 상한 경고 · G6 라우터 HTTP v2 PUT 테스트 · G7 align 표지 title만 신뢰(DR-9) · G10 `replace(assets)` + 문서 동기화 G4/G5/G8/§3.3, 이터레이션 1) | 백엔드 blueprint suite 197 pass(신규 53 + 골든 회귀 8: domain VO/직렬화 v1↔v2/정책 Footer·Decoration·Size·Palette·RepeatAsset 21 · application 조립·admin update 승격·reextract 6 · infra 추출기 rects·폰트 패스스루·렌더러 z-order/푸터 1회/align/toc/cover_box/v1 폴백·repo replace 14 · interfaces v2 라운드트립 4 · integration SC-1~8) · 회귀 211 pass(api/db 포함) · 프론트 tsc 클린 · ruff 클린 · **실 DB 레코드 `3bac89cb…` 재추출 성공(실 비전 LLM: v2 · Malgun Gothic · h1 34/h2 24/subtitle 18/h3 16 · accent1 #E07A1F · 로고 (0.83,0.04)+cover_box · 푸터 텍스트/좌표 · 푸터 띠 common 1 · p3 카드+줄무늬 · p6 카드 3+줄무늬 3)** · **백 신규 4(shapes.py · scripts/blueprint_reextract.py · golden_v1.json · test_golden_sample_fidelity.py)+수정 14 / 프론트 타입 1 / 마이그레이션 0(blueprint_json 내부 schema_version 1→2, DDL 무변경)** — **골든 샘플 스타일 충실도: 추출이 버리던 폰트·본문 로고 좌표·푸터·6단 크기·채움 도형을 schema v2 로 보존, 렌더러가 배경→장식→로고→슬롯→푸터(1회) z-order 로 재현, 같은 id 재추출(UseCase+CLI)**. 원인 6건 실데이터 대조(`BLUEPRINT_FONT_DIR` 빈 값→NanumGothic 강제 · RepeatAsset 표지 좌표 채택 · footer 슬롯 LLM 날조+렌더러 페이지번호 중복 · 4단 계층이 18/16/15pt 뭉갬 · fills 추출만 하고 미저장 · accent1=캡션 회색). Option C — `Decoration` VO는 Slot 이 아닌 별도 튜플(DR-1, 신뢰 경계 테스트가 서버 계산 필드로 고정) · footer 는 스키마 유지+생성 프롬프트/SlotContentPolicy/렌더러 3중 차단(DR-2) · 폰트 미설치는 원본명 유지(DR-3, 사용자 결정) · sizes REQUIRED 4키+옵션 h3/subtitle 렌더러 폴백(DR-4) · v1 로드 시 버전 보존·쓰기 경계(update/reextract)에서만 승격(DR-5) · align 만 LLM Draft 추가, 표지 title 만 신뢰(DR-7/9). ★ **설계 임계값은 실데이터로 전부 보정됨** — 푸터 띠 `#F3F4F6` 은 흰색과 거리 18.6(임계 24→12) · PyMuPDF `find_tables` 가 카드 박스를 표로 오탐(제외 기준을 비전 슬롯 포함율 ≥0.8 로) · 차트 막대 규칙은 CHART 슬롯 보유 패턴에만(카드 악센트 줄무늬 0.01 폭 보존) · Design §3.3 subtitle 규칙이 자기 기대값과 모순(h2~body 구간 2개 이상이면 subtitle/h3). ★ **골든 회귀를 DB 스냅샷 패턴(fake vision)으로 고정** → CI 에서 LLM 비결정성 제거, SC 8개 전부 자동 검증. ★ **재추출은 기존 `update()` 로 불가**(에셋 추가 금지·버전 고정) → `Repository.replace()`+`AdminUseCase.reextract()`+`build_admin_use_case()` DI 헬퍼, 스크립트가 `load_dotenv()`+트랜잭션 경계 보유. 함정: interface 스키마 `extra="forbid"` 라 v2 필드 미추가 시 프론트 PUT 400 · Windows `updated_at > created_at` 타이밍 플레이크(`>=`) · `inspect.py` 스크래치 파일명이 stdlib 섀도잉 · Bash heredoc 파이썬 `\s`/`\n` 깨짐(Write 도구) · **포트 8000 을 시스템 Python uvicorn(PID 4104, 20:27)이 점유해 리로드 안 됨 → v2 레코드 422(OPS-1, 사용자 재시작)**. 이월: **OPS-1 venv uvicorn 재시작 후 라이브 L1 재확인**, 카드 소제목 h3 bold(G1)·표 합계행 bold(B-5) 후속 피처, 2번째 골든 샘플 온보딩 시 장식 임계 재점검(G9), 에이전트로 실제 PPT 생성 육안 확인, 커밋·PR(blueprint/multimodal 모듈 전체 미커밋 누적) | [→](./blueprint-style-fidelity/) |
| golden-sample-blueprint | 2026-08-22 | 98%(Check 1차 94% → Act-1 이터레이션 1회 — Critical 1(LLM 슬롯 출력 VO 변환 ValueError가 전체 생성 실패로 전파 → 슬롯 제외+warning) + Important 4(표 색·로고 select UI · `FontCatalogPort.propose_mapping` 계약 · FR-17 UsageCallback 배선) + Minor 5(max_pages 400 · 가역 패턴 제외 토글 · Design 동기화 3) + 런타임 간극 2(degraded L3 E2E · 서사 순서 이동 UI), 재검증 Functional 89→97 · Contract 96→99 · Runtime 95→99, 회귀 diff 0 양쪽 · QA_SKIP(자동 L1/L2/L3+스모크 대체)) | 백엔드 신규 ~250 pass(도메인 VO/strict Draft/정책 5/직렬화/AST 50 · application 추출·관리·생성 UseCase+레지스트리 30 · 인프라 추출기 PDF/PPTX·폰트·프롬프트/LLM·렌더러 재오픈·repo 40 · API admin 17/wiring 2/**L3 E2E 2(정상·degraded)** · 에이전트 빌더 바인딩 3·컴파일러 노드 7) + **실 LLM 스모크 OpenAI PASS(gpt-4o 분류 5/5 + gpt-4o-mini 4장, 4,239 토큰)** · 프론트 신규 21(페이지 10 · 패널 3 · 검증/변환 8) · 회귀 백 7842/58 baseline · 프론트 1116/9 baseline · tsc 199 baseline · **백 신규 ~35+수정 7 / 프론트 신규 14+수정 10 / 마이그레이션 1(V066 `document_blueprint`·`_asset` LONGBLOB) / 의존성 python-pptx** — **Golden Sample → blueprint → presentation_generator 워커 → PPTX(+MCP PDF)**: "형식은 blueprint가 고정, 내용만 LLM". Option C — 신규 도메인 `blueprint` 1 + 포트 3(`SampleExtractorPort`/`PageClassifierPort`/`SlideRendererPort`), 기존 비전 어댑터는 `describe_with(messages, schema)` 1개 추가로 재사용(D8), 변환 어댑터는 `to_pdf_from_pptx` 1개, 비전 모델·동시성·타임아웃은 `multimodal_setting` 재사용(새 설정 키 0, D3). 좌표 0..1 비율 저장·EMU 변환은 렌더러 1곳(D1), 에셋은 TTL 첨부 저장소 대신 LONGBLOB(D2 — Plan FR-09 조정), PPTX 원본은 렌더 없이 도형 휴리스틱 분류·PDF는 페이지 렌더+비전(D4), 슬롯 작성은 슬라이드당 LLM 1회 동시(D5), 차트 bar/line/pie+표(D6), 워커 tool_config 소프트 참조·영속 엔티티 없음(D7 — 빌더 `presentation_generator` 전용 필드+바인딩 헬퍼, 프리필은 워커 tool_config). 팔레트/크기 휴리스틱(배경 근접색 accent 제외 · 글자수 가중 본문 · caption=최소)·반복 이미지 분류(cover/logo/decoration)·`SlidePlanValidationPolicy`(미존재 pattern 재시도 1회·초과 절단)·`SlotContentPolicy`(kind/길이/행/시리즈)는 도메인 Policy. ★ **Design §6.3 degraded 표에 "LLM 출력이 통과하는 모든 변환 단계"를 넣어야 한다** — VO 변환(ChartSpec 길이 검증)이 표에 빠져 Check에서 Critical로 잡힘. ★ **추출 응답 `assets[].data_b64`** — 썸네일만 주면 저장 시 원본 바이트를 되돌려 보낼 수 없는 설계 간극을 module-7에서 발견·보정(v0.7). ★ **gap-detector 에이전트 2회 모두 보고서 전달 전 중단** → transcript JSONL 최장 assistant 텍스트 추출로 복구(에이전트 결과는 파일 Write 규약 권장). 함정: 프론트 CRLF 파일 문자열 치환 스크립트 조용한 실패(LF 정규화 헬퍼) · Bash heredoc 백틱/`
` 깨짐(Write 도구) · 합성 단색 차트 이미지는 비전이 `image_with_notes`로 분류(실 샘플 검증 필요) · 문서생성기 패널도 MCP id가 text input이라 gap-detector의 select 제안은 전제 오류(Design 갱신). 이월: **V064~V066 배포 + `uv sync`(python-pptx) + `uv.lock` 신규 커밋**, 커버리지 수치 측정, 관리자 화면 실 브라우저 E2E, 로컬 vLLM/Ollama 스모크, 실 사내 Golden Sample 검증, 커밋·PR(multimodal-extractor 포함 전부 미커밋), Phase 2(DOCX·다중 샘플·버전 관리), `multimodal-store` Plan 재개 | [→](./golden-sample-blueprint/) |
| multimodal-extractor | 2026-08-21 | 96%(Check 1차 91% → Act-1 이터레이션 1회 — Critical 1(손상 PDF→500) + Important 코드 3(provider 미등록 500 매핑 · §8.2 #10/#11 통합 테스트 · §8.3 #7 supports_vision 바디) + 문서 동기화 3(`/test` 실패=200 ok=false · UNSUPPORTED_MEDIA 415 · FR-20 범위), 재검증 Contract 85→96 · Runtime 90→96, 회귀 diff 0 양쪽 · QA_SKIP(L3 이월)) | 백엔드 신규 ~190 pass(도메인 VO/Draft strict/Policy/AST 31 · 레지스트리·UseCase 동시성/재시도/degraded·Settings·AST 41 · 추출기 합성PDF·어댑터 폴백·프롬프트·repo·썸네일 35 · API admin 18/preview 10/통합 4/wiring 2) + **실 LLM 스모크 OpenAI·Anthropic PASS(strict)** · 프론트 신규 22(훅 7 · 페이지 14 · LlmModels 1) · 회귀 백 7684/58 baseline · 프론트 1094/9 baseline · **백 신규 28+수정 9 / 프론트 신규 10+수정 8 / 마이그레이션 2(V064 `llm_model.supports_vision` · V065 `multimodal_setting`)** — **멀티모달 추출 모듈 Phase 1: PDF 그림·차트·이미지형 표·스캔 페이지 → 관리자 선택 비전 모델로 구조화 해석, 인프로세스 포트 반환(저장은 타 모듈)**. Option C — `ImageExtractorPort`/`VisionDescriberPort` + 확장자·provider 키 레지스트리 2개, `BaseVisionAdapter`가 strict→json→text 폴백·`include_raw` usage 회수 공통 처리하고 벤더 차이는 `build_image_block()` 1메서드(OpenAI image_url / Anthropic source.base64 / 로컬 OpenAI 호환 strict 제외). `DescriptionDraft`(LLM, dict/Any 0) ↔ `MultimodalElement`(서버 계산) 타입 분리를 필드 집합 동등 테스트로 고정, 비용 가드 5종(상한→skipped 표기 · px/면적 · 해시 중복 · 세마포어 · 타임아웃+transient 재시도 1회)은 도메인 Policy/UseCase, 건별 failed는 결과·설정 오류만 예외. 전역 설정 단일 행(소프트 참조 `vision_model_id`, 행 없으면 기본 생성) + `/admin/multimodal` 2탭(설정·연결 테스트·PDF 미리보기 — 썸네일 ≤256px만 노출, 원본 바이트 미노출) + `/admin/llm-models` 비전 체크박스/배지. ★ **실 LLM 스모크를 DoD에 넣어 fake가 못 잡는 결함 회수** — structured 모드 `usage=None`을 스모크가 발견(`include_raw=True`로 수정), Anthropic도 strict 동작 실증(폴백 가정 정정). ★ **verify-architecture가 analyze 전에 레이어 위반 1건(routes→infra thumbnail import) 검출** → `get_multimodal_thumbnailer` DI seam. ★ **합성 PDF fixture(코드 생성)** 로 xref 공유 이미지 중복 후보·필터·degraded를 바이너리 커밋 없이 결정적 재현. 함정: Design §2.2↔§4.2가 `dropped` 에 대해 자기모순(구현은 §4.2 채택) · 기존 `UsageCallback`은 agent run 전용(RunTracker·agent_id 필수)이라 적재 경로 `ai_run` 영속은 Phase 2 · Windows heredoc 한글/이스케이프 깨짐(Write 도구 사용). 이월: **V064+V065 배포 필수**, 로컬(vLLM/Ollama) 비전 스모크(`MM_SMOKE_LOCAL_BASE_URL`/`_MODEL` env 시 `pytest -m llm`), 관리자 화면 실서버 E2E, Minor UI 4(토스트·오류코드·`ApiError.code` 409 분기·`updated_at` tz), **Phase 2 `multimodal-store`**(child 청크·출처 "그림 N(p.12)"·그래프 `multimodal_node` 탈착형·ai_run usage), 추출기 확장(이미지·DOCX/PPTX), 커밋·PR | [→](./multimodal-extractor/) |
| background-jobs | 2026-08-12 | 99.4%(Check 1차 89.0% → Act-1 이터레이션 1회 — High 3(세션 딥링크 3곳·실패 outbound·agent_name) + Medium 7 + Low 4 전부 처리, 재검증 백엔드 100%/프론트 97.9% · 회귀 스팟 7건 무회귀) | 백엔드 신규 82 pass(정책 전이 · repo SKIP LOCKED 컴파일 단언 · enqueue 409/404 · 워커 9시나리오+실패 outbound · 라우터 D14 · §7-7 스케줄 2파일) · 프론트 신규 34 pass(JobsPage 딥링크 · NotificationBell 배지/필터 · ChatInput · ChatPage 통합 입력잠금/409) · tsc 클린 · **백 신규 15+수정 6 / 프론트 신규 10+수정 11 / 마이그레이션 1(V060)** — **통합 백그라운드 작업: ad-hoc job(채팅 이탈 생존) + 인프로세스 스케줄러 틱(외부 cron 제거) + 작업함 2탭 + 벨 폴링 + 세션 딥링크 + outbound 재사용**. DB 큐(`agent_background_job`) + lifespan 워커 싱글턴(poll 5s · schedule tick 30s single-flight · 동시상한 2 · task 참조 보유), `FOR UPDATE SKIP LOCKED` claim→running 동일 tx, 등록자 AuthContext 재조립(D3 — **비활성 `!=APPROVED`면 실행 없이 failed**), reconcile 기동 1회 running→failed + shutdown 취소 시 shield 마킹 이중 방어, 세션 동시성 이중 가드(백 409+프론트 입력 잠금), enqueue 세션 무저장·결과물=세션 대화 답변(D10/D4 — stateless HITL이라 질문도 답변으로 에코). ★ **확인 UX 종착점(딥링크)을 "후속"으로 미루면 게이트에서 잡힌다** — `/chatpage?agentId=&sessionId=` 쿼리를 AgentChatLayout이 소비(에이전트 전환 리셋이 draft 대신 딥링크 세션 선택). ★ 목록 표시 필드는 응답 스키마부터 — repo `list_by_user`가 agent_definition LEFT JOIN 튜플(`(job, agent_name)`) 반환. ★ 삼항 배너는 에러를 가린다 — bgNotice(409 지정 문구) 우선 규칙. 실패 job도 outbound 발송(run-동형 payload `answer=None`이 실패 신호 — 웹훅 스키마 무변경). 이월: **V059·V060 배포 필수**, E2E 수동(등록→브라우저 종료→벨→딥링크 · cron 없이 스케줄 실행 · 재시작 reconcile · 미구독 에이전트 딥링크), 커밋·PR, 관측(useJobList 캐시 키 분리로 벨/작업함 폴링 중복 · GetJob 단건 agent_name=None) | [→](./background-jobs/) |
| doc-generator | 2026-08-11 | 97.2%(Check 1차 통과 85.5/88 — High 0 · Medium 1(G5 함수 길이, 추출기 클로저 팩토리 선례 동형) · Low 6(문서 갱신·1줄 보강), 초과 구현 9건 전부 additive, 이터레이션 0) | 백엔드 신규 77(도메인 정책 28 · 바인딩/create·update·delete·get 편승 27 · repo 6 · generator 13(폴백 체인·입력 절단·커버리지 재시도) · 컴파일러/supervisor 가이드 12) + agent_builder 전체 586 무회귀 · 프론트 신규 17+MSW 왕복 3(생성 전송/미편집 미전송/프리필→교체) · tsc 클린 · **백 신규 15+수정 8 / 프론트 신규 6+수정 4 / 마이그레이션 1(V059)** — **문서생성기: 유형 아웃라인 기반 "조사해서 직접 쓰기" 신규 도구**(추출기 "빈칸 채우기"와 분담·무변경). 문서 유형(이름+설명+섹션 아웃라인, `document_generation_type`)을 에이전트 create/update/get/delete **편승**으로 CRUD(신규 엔드포인트 0, `InvalidGenerationTypeError`의 ValueError 겸용 상속으로 라우터 무변경 400). 런타임은 추출기 동형 **함수형 노드** — ★ **자체 조사 루프 기각(사용자 정정 2회)**: supervisor가 문맥 판단으로 search/analysis 워커만 선행 위임(가이드 블록은 generator+타 워커 공존 시만 주입 · 순서 강제 아님 — 휴가계획서처럼 대화에 정보가 충분하면 바로 생성), 생성 노드는 `_split_fill_context`로 **모든 워커 산출물+대화** 소비. 작성은 LLM 1회+섹션 커버리지(h1~h3 부분 일치) 재시도 1회 → sanitize 재사용 → `DocumentConversionAdapter.to_document` 재사용(MCP 도구 3단 폴백: tool_config→generator 설정→extractor 설정) → 첨부 반환(기존 다운로드 라우트 공용). 출력 기본 DOCX(PDF는 한글 폰트 이슈 경고문만 — 근본 해결 doc-convert-korean-font 별도 트랙). 프론트는 섹션 편집기 패널+모달·요약 배지·detail 프리필(FR-12, GetAgentResponse additive 스냅샷). 함정: DDL COMMENT 문구 내 콤마는 검사기(top-level comma split)가 컬럼 분리로 오탐. 이월: **V059 배포 필수**+변환 MCP 도구 설정, E2E 7건(2경로 실측·LangSmith·다운로드), gap G1/G6/G7 1줄 수정·G2/G3 Design 정정·G4/G5 판단, 커밋·PR | [→](./doc-generator/) |
| agent-webhook-outbound | 2026-08-07 | 93.8%(Check 1차 92.7% → 즉시 조치 4건: G1 create_task 참조 보유 · G2 프론트 422 테스트 · G8 sender except Exception · G9 데드코드, 이터레이션 0 · 누락 기능 0) | 백엔드 웹훅 전체 94(M2 신규: outbound policy 8 · sender MockTransport 6 · dispatch 격리 6 · PATCH 확장 7 · 훅 2곳 5 · 라우터 deliveries/422 5) + 스케줄 회귀 39 · 프론트 settings 30(WebhookSection 15) + 페이지 회귀 5 · tsc 클린 · **백 신규 5+수정 8 / 프론트 신규 0+수정 6 / 마이그레이션 1(V058)** — **웹훅 M2 Outbound: 실행 결과 외부 발송**. 스케줄·inbound 웹훅 실행 완료 시 등록 URL로 M1 동형 HMAC 서명 payload POST(★ 발송 서명이 M1 `verify()`로 재현되는 왕복 테스트 — FR-15 계약 일관성). ★ **앱 수명 싱글턴 디스패처 패턴** — "inbound 비차단 태스크는 응답 후 실행 → 요청 세션 사용 불가"가 D11을 자연 확정, session_factory로 짧은 tx 2개(조회/기록)만 열고 HTTP 발송 구간 세션 미보유(DbScheduleRunSink 선례). 발송 방식 이원화(스케줄=인라인 await·inbound=spawn 비차단+테스트 주입), 재시도 총 4회 백오프 1/2/4s(5xx/연결오류만 · 4xx 즉시 중단 · 2xx만 성공), dispatch 절대 무raise(D16)+훅 이중 방어로 스케줄 이력 success 오염 차단(계약 위반 시나리오까지 단언). PATCH는 `model_fields_set`으로 "미지정 vs null(해제)" 구분(null 해제 시 outbound_enabled 강제 off · URL 없이 활성화 422 · 빈 body 422 유지로 M1 무회귀), 신규 분기는 전용 예외 `WebhookValidationError`(M1 G5 교훈 · 문자열 매칭 무소급). V058 delivery(INSERT only · FK=**agent_definition** — 채널 재발급 후에도 이력 보존). ★ **비차단 태스크 교훈** — `asyncio.create_task` 반환 참조 미보유 시 GC로 실행 중 유실 가능: 예외 격리와 태스크 수명은 별개 체크 항목(`_background_tasks` set+done_callback). 프론트는 outbound 하위 섹션(URL draft 저장/해제·토글·접힘 기본 이력 disclosure — 펼침 시에만 fetch, useScheduleRuns 선례). 이월: **V057+V058 배포 필수**, E2E 수동 일괄(M1 G7 + 수신 서버 왕복·서명 재현·다운 재시도 이력), 커밋·PR(M1+M2 워킹트리 누적 — 마일스톤별 분리), 후속 후보(발송 테스트 버튼·이력 정리 배치·rate limit) | [→](./agent-webhook-outbound/) |
| agent-webhook | 2026-08-07 | 93.3%(Check 1차 통과, M1 기능 결손 0 · 코드 필수 수정 0 — Gap 7건 중 4건 설계 문서 정정·2건 저위험 수용·G7 E2E만 Medium 이월, 이터레이션 0) | 백엔드 신규 58(정책 경계 ±299/301 · rotate 구키 무효 · 관리 5본 403/404/409 · 공개 라우터 무토큰 200 실측 · raw body verbatim · DDL comment) · 프론트 신규 8+갱신 13(WebhookSection 모달/토글/confirm · SettingsPanel 스텁 2종 축소) · 회귀 빌더 페이지·스튜디오 24 pass · tsc 클린 · **백 신규 15+수정 1(main.py) / 프론트 신규 6+수정 4 / 마이그레이션 1(V057)** — **에이전트 외부 오픈 웹훅 M1 Inbound**. 신규 1:1 `agent_webhook` 독립 opt-in + 공개 `POST /api/v1/webhooks/agents/{id}`(JWT 없음, HMAC-SHA256 + 타임스탬프 ±300s·`compare_digest`) + 관리 5본(발급/조회/rotate/토글/삭제) + 설정 탭 Webhook 스텁("Bearer dbuilder_xxx") 실기능 교체. 실행은 스케줄 선례 `_build_run_agent_uc` 공유 + **소유자 User 로드→AssembleAuthContextUseCase 조립 주입**(UI 실행과 동작 동등성, 스케줄의 auth_ctx=None 기존 동작 무변경). ★ **HMAC=시크릿 평문 저장 필수 교훈** — Plan의 "해시 저장" 가정이 서명 재계산과 양립 불가함을 Design에서 발견·정정(D2, MCP api_key V032 선례 수준·유출 시 rotate). ★ **1회 노출의 타입 분리 강제** — 평문은 `WebhookSecretResponse`(발급/rotate)에만, `WebhookConfigResponse`는 hint만(구조적 강제, 리뷰 규칙 아님). 401/404 단일 문구로 존재 비노출(D8), 검증 순서 저비용 선행(D5, LLM 비용은 통과 후). 함정 2건: DDL comment 검사기는 따옴표 내 최상위 콤마를 컬럼으로 오인(문구에 콤마 금지) · jsdom clipboard 읽기 전용(userEvent 스텁 `readText()` 검증). 이월: **V057 배포 필수**, G7 E2E 수동(활성화→curl 서명→ai_run 관측·rotate 후 구키 401), **M2 Outbound**(Design §9 확정본 — V058 delivery·재시도 3회·발송 훅 2곳), rate limit 별도 사이클, 커밋·PR | [→](./agent-webhook/) |
| agent-settings-tab | 2026-08-07 | 97.7%(Check 1차, Missing 0 · Partial 2 전부 수용 — G1 안내 카드 경계 시각 차이 · G2 jsdom number 강제변환 동일 경로, 이터레이션 0) | 프론트 신규·추가 14(SettingsPanel 10 · 매핑 프라임/폴백 2 · 페이지 통합 create/edit 왕복 2) · 관련 3파일 30 pass · 전체 회귀 812 pass(사전 실패 8 제외, 신규 회귀 0) · **프론트 신규 3+수정 9 / 백엔드 0 / 마이그레이션 0** — **빌더 설정 탭 활성화 + Recursion Limit 셀프서비스**. 비활성 placeholder였던 설정 탭을 열고 `agent_definition.max_iterations`(V045, agent-recursion-limit)의 프론트 마지막 마일 배선 — create/update 페이로드 `max_iterations` + edit는 detail 프라임(`?? 25` 방어 폴백). ★ **blur/Enter clamp 로컬 완결 패턴** — 저장 버튼이 탭 밖(StudioHeader)인 구조에서 인라인 에러 끌어올림 대신 "onChange로 올라오는 값은 항상 유효(10~1000)"를 컴포넌트 안에서 보장(로컬 문자열 draft로 타이핑 과도기 보호, 무효 입력은 복원+미호출). 유사 개념 판별 교훈: `model_call_limit` 미들웨어(run당 LLM 호출, 기본 10)와 혼동 지점을 시안 기본값 25 일치로 `max_iterations` 배선 확정. MCP/Webhook/Telegram 3종은 disabled 토글+"준비중" 정적 스텁(상태·페이로드 0, 섹션 단위 교체 예약). 상수 `MAX_ITERATIONS`는 백엔드 IterationLimitPolicy 수동 미러(주석 명기). 이월: 커밋 2건 분리(stale 단언 보정은 builtin-middleware 소관 별도 test 커밋), E2E 수동(500 저장→재조회→재진입 프라임), 스텁 3종 실기능·`model_call_limit` config 오버라이드 후속 | [→](./agent-settings-tab/) |
| builtin-middleware | 2026-08-05 | 95.2%(Check 1차 92.4% → Gap 8건 즉시 조치: G3 카탈로그 enum 전방 호환 가드 · G2 supervisor 수렴 테스트 2건 · G8 v2 주석 · 설계 문서 표류 5건 갱신, 이터레이션 0) | 백엔드 신규 ~63(도메인 병합정책 9 · 빌더/프로바이더/플래그/call_limit 25 · agent_builder 동등성+스냅샷+수정+컴파일러 15 · general_chat 배선 3 · 라우터 5 · repo 8) · 프론트 신규 25(훅 · MiddlewareSection · AdminMiddlewarePage) · 회귀 전 스위트 그린(사전 실패군 제외) · **마이그레이션 1(V056)** — **langchain v1 `create_agent` 전환 + 빌트인 미들웨어 4종(model_retry·tool_retry·model_fallback·model_call_limit) 2단계 제어**. deprecated `create_react_agent`를 워커+General Chat 단일 경로로 대체(동등성 게이트 `test_create_agent_equivalence` 선행, 워커 name 규약은 `_wrap_worker` 재포장이 구조 보장). 제어: `middleware_catalog`(config 단일 소스) + `agent_middleware` 스냅샷(행 존재=적용, config NULL 예약) — is_builtin은 생성 시 주입+폼 전용 `exclude_builtin_middleware_types` opt-out(채팅 초안 경로 구조 차단, builtin-tools 패턴 계승), is_enforced는 실행 시 병합이라 기존 에이전트 즉시 적용. ★ **prepare/instantiate 분리 패턴** — `MiddlewareProvider.prepare()`(compile당 1회 조회·병합·config 해석) + `MiddlewarePlan.instantiate()`(워커별 새 인스턴스, 상태 공유 오염 방지) + D8 격하(미들웨어 실패는 warning, 실행 차단 금지 · fallback은 해석된 BaseChatModel만). 관리자 `GET/PATCH /middleware-catalog`(타입별 config 범위 검증+미등록 폴백 모델 400) + `/admin/middleware` 화면(토글 2종+타입별 필드 편집). 이월: **V056+`pip install -U langchain` 배포 필수**, 수동 E2E(재시도 로그·스트리밍 실측), 커밋·PR, 프론트 페이로드 테스트(G5) | [→](./builtin-middleware/) |
| builtin-tools | 2026-08-01 | 100%(Check 1차 93% → Partial 5건 즉시 보강, 이터레이션 0) | 백엔드 신규 17케이스(sync 시드/왕복 보존 · repo 보존 계약 SQL 검사 · SetBuiltin · 라우터 403/404 · 주입 10케이스) 51 pass · 프론트 신규 3파일+보강(ToolPickerModal 빌트인 4 · AgentBuilderPage 3(초안 적용 불변식 포함) · AdminToolsPage 3) · 회귀 백 506 + 프론트 130 무회귀 · **백 신규 2+수정 12 / 프론트 신규 2+수정 12 / 마이그레이션 1(V054)** — **에이전트 생성 시 자동 주입되는 빌트인 도구 + 관리자 등록/해제**. 런타임 SoT `tool_catalog.is_builtin`(관리자 `PATCH /tool-catalog/builtin`, `/admin/tools` 화면) + `ToolMeta.builtin_default`(프레시 DB sync INSERT 시드) 이원화, upsert UPDATE 분기의 is_builtin 미변경 성질을 SQL SET 절 검사로 **계약 테스트화**(재부팅 보존). 주입은 `CreateAgentUseCase` Step 2.7(정책 검증 후 → MAX_TOOLS 상한 제외, 정규화 dedup, 비활성 MCP 제외+경고 격하, flow_hint 미포함). ★ **opt-out 채널 구조 분리 패턴** — "채팅(Fix)은 못 빼고 사용자는 뺄 수 있다"를 백엔드 `exclude_builtin_tool_ids` 필드 + 프론트 `excludedBuiltinTools` 전용 상태(ToolPickerModal에서만 토글, 초안 적용 경로는 접근 불가 + 초안의 빌트인 혼입 필터)로 구조적으로 보장(프롬프트 방어 아님). 초기 빌트인 wiki_read+wiki_list, 스냅샷 방식(기존 에이전트 소급 없음). 이월: **V054 배포 필수**, 수동 E2E 3종(관리자 토글/채팅 생성 포함/폼 해제), 커밋·PR, 후속 후보(기존 에이전트 백필·config 필요 도구 빌트인화) | [→](./builtin-tools/) |
| excel-generator-node | 2026-08-25 | 98%(Check 91% → Act-1 98% — Critical 0 · Important 3(I-1 store IO 예외 누출 · I-2 LLM 호출 예외 미포착 · I-3 시작 로그 누락) + Minor 5 + 문서화 6 전부 해소, 이터레이션 1) | 신규 37(domain schemas/policies 13 · infra ExcelGenerator 14 · 노드 8 · 라우터 xlsx MIME 1 · 레지스트리 설명 1) · 대상 스위트 822 pass · 신규 실패 0(baseline 대조) · 백 신규 4(domain/excel_generator 3 + infra generator.py)+수정 4(workflow_compiler · main DI · document_extractor_router MIME · tool_registry) / 프론트 0 / 마이그레이션 0 — **excel_export를 document_generator 동형 전용 합성 노드로 격상: 하이브리드 소싱(P1 analysis_source 코드 복사 무손실 > P2 첨부 엑셀 파싱 > P3 LLM 표 구조화 300행 상한), 시트 계획 JSON LLM 1회(카탈로그만 노출), AgentAttachmentStore 저장 → `/api/v1/document-extractor/files/{id}` 다운로드 링크, 모든 실패 안내 노옵**. Option C(실용 균형). 후속: 실서버 수동 E2E · 생성 노드 3종 공통 헬퍼(40줄 규칙) · document_generator 예외 경계 동일 보강 | `excel-generator-node/` |
| deep-search-pipeline | 2026-08-26 | 97%(Check 93% → Act-1 97% — Critical 0 · Important 2(G-01 single 전략 코드 가드 부재 · G-02 Plan §4.2 ≤10줄 기준 vs 실측 +31줄 충돌) 모두 해소, Minor 3(G-03 final_answer 통합테스트 · G-04 verify 스킬 미실행 · G-05 FR-10 토큰 상한) 이월, 이터레이션 1) | 신규 136(L0 provider계약 16 · L1 도메인 38 · L2 노드 47 · L3 통합·배선 35) · 회귀 58 failed/8255 passed, FAILED 목록 baseline 동일(diff ×4) · 백 신규 9파일 1,078줄(domain/deep_search 2 + application/deep_search 5 + __init__ 2) + 수정 3파일 36줄 / `search_pipeline.py` **0줄** / 프론트 0 / 마이그레이션 0 / 의존성 0 — **검색 쿼리 1개 압축을 `요구 분해 → 병렬 검색 → 근거 누적 → 커버리지 검증 → 부족분만 선택적 재검색` 5노드 LangGraph 서브그래프로 대체. 루프 기준을 "검색 횟수"에서 "채워진 fact slot 수"로 전환**. Option C — 원안 7노드에서 Analyzer+Planner(D1)·Evaluator+Replanner(D2)를 각 1콜로 통합해 iterative 최악 LLM 14→9콜, EvidenceStore는 노드가 아닌 state reducer(D5). 종료 4중(COMPLETE > MAX_ITERATION > EXHAUSTED > NO_GAIN, D4) · NO_GAIN은 iteration≥2부터(D3, 1회차 무수확이 전략 전환 가치 최대) · 배선은 config `SEARCH_PIPELINE_MODE`(기본 legacy, 롤백 한 줄) + 웹검색 한정을 컴파일러 분기가 강제(D9). ★ **Fake 기반 테스트의 맹점이 두 번 드러남** — 136개 통과 상태에서 ① 실운영 OpenAI 400(`dict[str,str]`은 strict structured outputs의 열린 맵 금지 위반 → LLM 경계만 key/value 목록으로 교체, 도메인 dict·AD-5 불변, D19) ② `strategy="single"`인데 requirement N개면 그대로 팬아웃(테스트가 픽스처를 검증하고 있었음 → `_cap_single_strategy` 코드 가드, D20). **안전 규칙(비용 상한·권한·한도)은 프롬프트가 아니라 코드가 보증해야 하고, 구조화 출력에는 provider 계약 테스트(L0)를 필수로 둔다.** ★ LangGraph 리듀서는 정확히 `(a,b)->c` — 기본값 있는 3번째 인자면 compile 시 ValueError(D14). ★ 회귀는 pass 개수가 아니라 정렬된 FAILED 목록 diff(baseline 58건 상시 실패). ★ `runtime-datetime-context`가 legacy 팩토리에 `datetime_block`을 추가 → `inspect.signature` 비교 테스트로 "한 줄 교체" 계약을 코드로 고정(D16, Plan R8 동시 해소). 함정: `.env`가 deep이라 `settings` 런타임 값을 검사하던 테스트가 실패(환경이 아닌 `Settings.model_fields[...].default`를 검사해야 함) · Bash heredoc 파싱 실패로 긴 한글 문서는 Write 도구 사용 · `cd` 체인이 세션 cwd를 이동시켜 후속 명령 실패. 이월: 실운영 관측(`stop=no_gain`/`degraded=True`/`fallback=True` 빈도로 Replanner·경량모델·plan 스키마 한계 판정, 보고서 §8.1) · G-03/04/05 · 내부문서검색 확장 · 미배선 `multi_query` 모듈 정리 | [→](./deep-search-pipeline/) |
| blueprint-render-style-fidelity | 2026-08-29 | 100%(Check 1차 98% → Checkpoint 5 즉시 조치 3건 → 100% — Critical 0 · Important 2 전부 해소, 이터레이션 0 · SC 8/8 · DR 9/10+1건 의도적 변경(DR-10 v2) · 설계 시나리오 34/34) | 신규 41(table_borders 5 · chart_builder 13 · pptx_renderer 파라미터 8 · VO 4 · 직렬화 4 · API 스키마 2 · 골든 6 · AST 계약 2 · 추출 기본값 1) · 회귀 381 passed/0 failed · 신규 코드 커버리지 100%(chart_builder · table_borders · schemas/blueprint) · ruff 변경 12파일 0건 · 백 신규 1(renderer/table_borders.py 45줄)+수정 7 / 프론트 0 / 마이그레이션 0 / schema_version 승격 없음 — **렌더러가 소비하지 않던 스타일 정보 4종을 살림**: 차트 데이터 레이블(`_data_labels`) · 축·레이블 단위 서식(`_number_format`) · 표 셀 테두리(oxml `a:lnL/lnR/lnT/lnB`) · 불릿 문단 줄간격·여백. 하드코딩 `#F3F4F6`·`1.0` → `TableStyle.zebra_bg`·`StyleTokens.body_line_spacing` 토큰화(VO 필드 6개 추가, 전부 기본값 → `.get(default)`로 v1 스냅샷 무수정 로드). ★ **설계 단계 API 사전 검증이 리스크 판단을 뒤집음** — Plan은 "차트 API 한계"를 최대 리스크로 봤으나 실측 결과 차트는 python-pptx 네이티브 지원이고 **표 테두리에 API가 없었다** → `table_borders.py` oxml 격리 설계(DR-3, `run_fonts.py`와 함께 2파일로 AST 계약 고정). ★ **SC 8/8·DR 10/10·시나리오 34/34 만점 상태에서 사용자가 보는 결과는 변하지 않았음** — 추출 기본값 실측(`zebra=False`·`border_width_pt=0.0`·`body_space_after_pt=0.0`)으로 드러남. 원인은 개별로는 타당한 결정 셋의 합(Checkpoint 2 범위 + DR-10 `False` 유지 + DR-2 "기본값=현행 동작"). Checkpoint 5에서 DR-10을 v2로 뒤집어 **추출이 골든 실측값으로 켜게** 함(`_DEFAULT_ZEBRA=True`·`_DEFAULT_BORDER_WIDTH_PT=0.75`·`_DEFAULT_LINE_SPACING=1.45`·`_DEFAULT_SPACE_AFTER_PT=33.2`·`chart_label_size_pt=caption`), **VO 기본값(꺼짐)은 하위호환용으로 유지** → 기존 저장 블루프린트 불변·신규 추출분만 원본을 닮음. ★ DR-4 리터럴 서식 `'0.00"%"'` — `'0.00%'`였다면 OOXML 백분율 변환으로 1.62가 162%로 표시. 함정: `StyleSchema`가 `extra="forbid"`라 VO 필드 추가만으로 API 8건 파손, **회귀에서 `tests/api`를 제외해 구현 1~8단계 내내 미발견**(교훈: 회귀는 항상 전체 범위) · 백그라운드 `| tail -3`이 pytest 출력을 자르고 파이프라인이 종료코드를 가려 **측정 전에 수치를 문서에 기록**(교훈: 파일로 받고 종료코드 별도 확인) · 설계 내부 모순 2건(§3.2 가드↔DR-5, §3.1 기본값↔DR-2) — C에 이어 2사이클 연속(교훈: 설계 후 DR↔본문 교차 검증 절차화) · python-pptx가 캐시에 넣는 `<c:formatCode>General</c:formatCode>` 때문에 단언을 `<c:numFmt`로 좁혀야 함. 이월: **두 번째 실사용 PDF 과적합 교차 검증(4사이클 연속 이월 — 골든 실측값이 추출 상수로 코드에 박혀 시급도 상향)** · 스타일 편집 관리 UI(백엔드 필드 준비 완료, 프론트 미구현) · 차트 축·범례 폰트(Out of Scope) · `_snap_slot_boxes` 방어 분기 테스트 1건(사이클 B 잔여) · **사이클 C·D 코드 미커밋**(`b70c4b5`는 A·B만 포함) | [→](./blueprint-render-style-fidelity/) |
| admin-default-llm-routing | 2026-08-31 | 100%(정적 — Structural/Functional/Contract 각 100%, Runtime N/A 환경부재 · Critical 0 · Important 0(G-3·G-4 문서해소, G-7 코드해소) · 이터레이션 0) | 신규 62(캐시 16 · provider 19 · 무효화 11 · 어댑터 12 · 체인통합 4) · 회귀 58 failed/8363 passed, FAILED 목록 baseline 파일별 개수까지 동일 · 백 신규 4파일 462줄+수정 16파일(+665/-97) / 프론트 0 / 마이그레이션 0 / API계약 0 — **관리자가 등록한 기본 LLM을 재시작 없이 보조 LLM 호출 9곳(의도판정·웹검색판단·환각검사·프롬프트합성·메모리추출·위키정제3종·평가QA생성)에 반영**. 아래단에 범용 `CachePort`(domain) + `InMemoryCache`(TTL·상한·clock주입)를 뚫고 그 위에 `UtilityLLMProvider` 2계층 캐시 — **L1=값(CachePort, JSON직렬화, Redis교체대상) / L2=인스턴스(프로세스로컬)**. ★ **LangChain `BaseChatModel`은 직렬화 불가** — 단일 캐시로 만들었으면 Redis 전환 시 전면 재작업(DR-1). ★ **L2 키에 `updated_at` 포함**(DR-3) → L1 갱신이 새 updated_at을 실어오면 L2 키가 자연히 달라져 **L2 무효화 코드 자체가 불필요**. ★ **`ChatOpenAI(model=...)`은 키 없으면 생성자에서 OpenAIError** — 어댑터가 __init__에서 즉시 만들면 NPU 전용 배포가 **부팅 불가**라 provider 주입 시 지연생성 필수(DR-12, 설계 누락을 구현이 메움). ★ 무효화 훅이 pricing 1곳뿐이었음(D-8) — is_default 변경 경로가 비어 있었고, 통합테스트 I4가 그 상태를 재현해 회귀 차단. 함정: 설계 §11.2 '패턴 C 어댑터 무수정'은 구 AD-1(부팅주입) 전제라 런타임 해석에서 무효 → 어댑터 5곳이 아닌 **9곳** 수정 · application→infrastructure 금지로 `repo_builder` 주입(AD-6, MemoryExtractionService 선례). ⚠️ **미검증: V-1 vLLM tool-calling(패턴A/B 5곳 전부 `with_structured_output` — 실패 시 배선 무관하게 전멸) · V-2 실 MySQL+NPU 무재시작 교체 · V-3 패턴C `from_openai` 즉시 ChatOpenAI(키 없는 환경 첫 호출 실패)** · G-5 `.env.example` bkit훅 차단으로 사용자 직접 추가 필요 | [→](./admin-default-llm-routing/) |
